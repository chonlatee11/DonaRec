---
phase: 03-donation-lifecycle-maker-checker-issuance
plan: 03
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - donnarec-api/internal/donation/model.go
  - donnarec-api/internal/donation/errors.go
  - donnarec-api/internal/donation/service.go
  - donnarec-api/internal/donation/handler.go
  - donnarec-api/internal/donation/service_test.go
  - donnarec-api/internal/donation/service_integration_test.go
  - donnarec-api/cmd/server/main.go
autonomous: true
requirements: [FR-07, FR-09, FR-11, FR-29]
must_haves:
  truths:
    - "A Maker can create a donation record; it starts in status=draft"
    - "Maker can edit a record only while draft; submit moves draft → pending_review"
    - "Donor national/tax ID is stored AES-256-GCM encrypted (never plaintext in DB) and masked by default in responses"
    - "Tax ID is mandatory (create fails without it); consent fields are captured per snapshot"
    - "D-44: tax/national ID is mandatory at the API edge — a donation cannot be created (or issued) without it"
    - "D-49: consent is captured in the per-donation snapshot (consent_given flag + consent_at timestamp)"
  artifacts:
    - path: "donnarec-api/internal/donation/service.go"
      provides: "DonationService Create/UpdateDraft/Submit/GetByID/List with PII encrypt + state machine"
      contains: "func (s *DonationService) Create"
    - path: "donnarec-api/internal/donation/handler.go"
      provides: "Gin handlers for create/get/update/submit/list"
      contains: "func (h *DonationHandler) Create"
    - path: "donnarec-api/internal/donation/model.go"
      provides: "request/response structs; DonationResponse exposes DonorTaxIDMasked only"
      contains: "DonorTaxIDMasked"
  key_links:
    - from: "internal/donation/service.go"
      to: "internal/crypto.EncryptField"
      via: "encrypt donor tax id on create"
      pattern: "crypto\\.EncryptField"
    - from: "internal/donation/service.go"
      to: "internal/pii.MaskNationalID"
      via: "mask on read (default)"
      pattern: "pii\\.MaskNationalID"
    - from: "cmd/server/main.go"
      to: "internal/donation handlers"
      via: "maker route group under RequireRoles"
      pattern: "donations"
---

<objective>
Vertical slice แรกของ Flow A: Maker สร้าง/แก้ (draft)/submit donation record พร้อมเก็บ donor PII แบบเข้ารหัส + mask, บังคับเลขภาษี, capture consent. นี่คือ thin end-to-end UI→API→DB ฝั่ง backend (UI ใน 03-08).

Purpose: ส่งมอบความสามารถจริง — หลัง slice นี้ Maker สร้างรายการบริจาคและส่งเข้า review ได้ (FR-07/09/11), donor PII ปลอดภัยตาม PDPA (FR-29/NFR-02), consent ถูกบันทึก (NFR-03). TDD: เขียน test ก่อน implementation.
Output: `internal/donation/{model,errors,service,handler}.go` + maker routes ใน main.go; tests INV-5 (PII), INV-6 (state machine subset), consent, mandatory-taxID เป็นสีเขียว.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-CONTEXT.md
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-PATTERNS.md
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-RESEARCH.md

<interfaces>
<!-- Reuse Phase 1/2 — do NOT reinvent (RESEARCH Don't Hand-Roll). -->
internal/crypto: EncryptField(ctx, kp KeyProvider, plaintext []byte) (ciphertext, wrappedDEK []byte, err error)
internal/crypto: NewEnvKeyProvider() (*EnvKeyProvider, error)  // reads DONAREC_KEK
internal/pii: MaskNationalID(full string) string  // "x-xxxx-xxxxx-x1234"
internal/pii: CanRevealFull(claims auth.KeycloakClaims) bool  // Admin+Checker
internal/audit: AuditEntry{ActorID, ActorEmail, Action, Resource, BeforeJSON, AfterJSON, IPAddress}
internal/audit: NewAuditService(pool, queries, logger); (s).AppendAuditEntryTx(ctx, tx, AuditEntry)
internal/auth: KeycloakClaims{Subject string}; (kc).ActorIdentity() string; (kc).HasRole(role) bool
internal/auth: RoleMaker="maker" RoleChecker="checker" RoleAdmin="admin"; RequireRoles(...)
internal/db: WithTx(ctx, pool, fn func(pgx.Tx) error) error; (q *Queries).WithTx(tx) *Queries
Analog handler: internal/users/handler.go (claims extract, bind+validate, audit_after, no-PII logs)
Analog service: internal/users/service.go (NewUserService(pool, queries, logger); WithTx usage)
sqlc queries (from 03-01): CreateDonation, GetDonationByID, UpdateDraftDonation, SubmitDonation, SearchDonations
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Create donation + PII encrypt/mask + consent + mandatory tax ID (RED→GREEN)</name>
  <files>donnarec-api/internal/donation/model.go, donnarec-api/internal/donation/errors.go, donnarec-api/internal/donation/service.go, donnarec-api/internal/donation/service_integration_test.go, donnarec-api/internal/donation/service_test.go</files>
  <read_first>
    - donnarec-api/internal/users/service.go (service struct + constructor + WithTx)
    - donnarec-api/internal/crypto/envelope.go (EncryptField return contract)
    - donnarec-api/internal/pii/mask.go (MaskNationalID format)
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-PATTERNS.md §"internal/donation/service.go" and §"Pattern C" and §"PII encrypt on write"
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-RESEARCH.md §"Pattern 3: PII Encrypt on Write, Mask on Read" and INV-5
  </read_first>
  <behavior>
    - TestCreateDonation: valid request → record persisted with status='draft', created_by=actor, consent fields stored
    - TestPII_TaxIDStoredEncrypted: raw `SELECT donor_tax_id_enc FROM donations` bytes != plaintext "1234567890123"; donor_tax_id_enc + donor_tax_id_dek non-nil
    - TestPII_MaskDefault: GetByID response DonorTaxIDMasked == "x-xxxx-xxxxx-x0123" (last 4); plaintext never in response struct
    - TestMandatoryTaxID: create with empty tax ID → ErrMissingTaxID (D-44)
    - TestConsentCapture: consent_given/consent_at/consent_text_version/consent_purpose persisted on create (D-49)
  </behavior>
  <action>
    เขียน test เหล่านี้ใน service_integration_test.go / service_test.go ให้ RED ก่อน (แทน scaffold จาก 03-01). แล้ว implement: `model.go` define `CreateDonationRequest` (DonorName, DonorTaxID validate:"required,len=13,numeric" per D-44, DonorAddress, DonorEmail omitempty email, Amount gt 0, DonatedAt, Notes, ConsentGiven, ConsentTextVersion, ConsentPurpose), `DonationResponse` (exposes `DonorTaxIDMasked string` only — NEVER DonorTaxIDEnc), `UpdateDraftRequest`. `errors.go` define package-level sentinels `ErrMissingTaxID`, `ErrInvalidTransition`, `ErrSoDViolation`, `ErrMissingReason`, `ErrForbidden`, `ErrEDonationKeyedConfirmation` (declare all now so later plans reuse). `service.go`: `DonationService` struct {pool, queries *db.Queries, allocator *receiptno.Allocator, auditSvc *audit.AuditService, keyProvider crypto.KeyProvider, logger}; `NewDonationService(...)`. Implement `Create(ctx, req, claims)`: validate tax ID present (ErrMissingTaxID), `crypto.EncryptField(ctx, s.keyProvider, []byte(req.DonorTaxID))` → store enc+dek via `qtx.CreateDonation` inside WithTx, set retain_until/legal_basis from config default, consent fields from req; return DonationResponse with `pii.MaskNationalID`. Implement `GetByID(ctx, id, claims)` returning masked response. NEVER log req.DonorTaxID/DonorName (Pattern C — log donation_id + created_by UUID only).
  </action>
  <verify>
    <automated>cd donnarec-api && go test -count=1 -run 'TestCreateDonation|TestPII_TaxIDStoredEncrypted|TestPII_MaskDefault|TestMandatoryTaxID|TestConsentCapture' ./internal/donation/... -timeout 180s</automated>
  </verify>
  <done>All five tests green; tax ID encrypted at rest; masked in response; tax ID mandatory; consent stored.</done>
  <acceptance_criteria>
    - service.go contains `crypto.EncryptField(` and `pii.MaskNationalID(`
    - model.go DonationResponse has field DonorTaxIDMasked and NO DonorTaxIDEnc field
    - `grep -c "DonorTaxID" donnarec-api/internal/donation/service.go` shows no zap.String logging of it (manual: no `zap.String("tax` patterns)
    - the 5 named tests exit 0
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: State machine guard + Submit + UpdateDraft + List (RED→GREEN)</name>
  <files>donnarec-api/internal/donation/service.go, donnarec-api/internal/donation/service_test.go, donnarec-api/internal/donation/service_integration_test.go</files>
  <read_first>
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-CONTEXT.md §"State machine" (D-45)
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-RESEARCH.md INV-6 (state machine cases) and §"SearchDonations"
    - donnarec-api/internal/donation/service.go (Task 1 output — extend, do not rewrite)
  </read_first>
  <behavior>
    - TestEditDraft: UpdateDraft on a draft succeeds; UpdateDraft on pending_review → ErrInvalidTransition (only drafts editable, FR-09)
    - TestStateMachine_InvalidTransitions: submit on non-draft → ErrInvalidTransition; submit on draft → pending_review (valid). Cover the RESEARCH INV-6 transition table subset that this plan owns (draft→submit valid; pending_review→submit invalid; issued→submit invalid; rejected→submit invalid)
    - TestSubmitMovesToPendingReview: draft → Submit → status=pending_review, submitted_at set
  </behavior>
  <action>
    เขียน tests RED ก่อน. Implement `UpdateDraft(ctx, id, req, claims)` using `qtx.UpdateDraftDonation` (WHERE status='draft' — returns rows-affected 0 → map to ErrInvalidTransition). Implement `Submit(ctx, id, claims)` using `qtx.SubmitDonation` (WHERE status='draft' → pending_review, submitted_at=now(); 0 rows → ErrInvalidTransition). Add a small pure helper `canTransition(from donationStatus, action string) bool` encoding the locked D-45 state machine so handlers/tests share one source of truth (used now for submit/update; approve/return/reject/cancel arms wired in 03-05/03-06). Implement `List(ctx, filter, claims)` calling `qtx.SearchDonations` with all-nil filter defaults (full wiring of search filters lands in 03-06; here return masked rows). Re-encrypt is not needed on update of non-PII fields; if tax ID changes on draft edit, re-run EncryptField. Pattern C logging throughout.
  </action>
  <verify>
    <automated>cd donnarec-api && go test -count=1 -run 'TestEditDraft|TestStateMachine_InvalidTransitions|TestSubmitMovesToPendingReview' ./internal/donation/... -timeout 180s</automated>
  </verify>
  <done>Submit/UpdateDraft enforce draft-only; invalid transitions return ErrInvalidTransition; canTransition helper is the single state-machine source.</done>
  <acceptance_criteria>
    - service.go contains `func (s *DonationService) Submit(` and `func (s *DonationService) UpdateDraft(`
    - service.go contains a `canTransition(` helper referencing pending_review and draft
    - the 3 named tests exit 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Gin handlers + maker route group wiring</name>
  <files>donnarec-api/internal/donation/handler.go, donnarec-api/cmd/server/main.go</files>
  <read_first>
    - donnarec-api/internal/users/handler.go (claims extraction, bind+validate, audit_after, error logging — copy verbatim patterns A/C)
    - donnarec-api/cmd/server/main.go (wiring order pool→queries→services→handlers→setupRouter; route group pattern)
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-PATTERNS.md §"internal/donation/handler.go" and §"cmd/server/main.go" and §"Pattern E"
  </read_first>
  <action>
    `handler.go`: `DonationHandler{svc *DonationService, validate *validator.Validate, logger}` + `NewDonationHandler`. Handlers: `Create`, `GetByID`, `Update`, `Submit`, `List` — each copies the claims-extraction block (Pattern A), binds+validates request (Pattern from users/handler), maps sentinel errors (ErrInvalidTransition→409, ErrMissingTaxID→422, ErrForbidden→403, default→500 with `zap.String("donation_id", id)` only — Pattern C), and sets `c.Set("audit_after", resp)` on success. In `main.go` follow the locked wiring: construct `keyProvider, _ := crypto.NewEnvKeyProvider()`; `allocator := receiptno.NewAllocator(queries)`; `donationSvc := donation.NewDonationService(pool, queries, allocator, auditSvc, keyProvider, logger)`; `donationHandler := donation.NewDonationHandler(donationSvc, logger)`; register an `/api/donations` group guarded by `auth.RequireRoles(auth.RoleMaker, auth.RoleChecker, auth.RoleAdmin)` (all staff) with POST "" (Create), GET "" (List), GET "/:id" (GetByID), PUT "/:id" (Update), POST "/:id/submit" (Submit) per PATTERNS §main.go. Leave checker/cancel/slip/reveal routes for 03-04/05/06 (do not register handlers that don't exist yet).
  </action>
  <verify>
    <automated>cd donnarec-api && go build ./... && go test -count=1 ./internal/donation/... -timeout 240s</automated>
  </verify>
  <done>donation maker routes registered; handlers map sentinel errors to correct status codes; audit_after set; go build + donation package tests pass.</done>
  <acceptance_criteria>
    - handler.go contains `func (h *DonationHandler) Create(` and `c.Set("audit_after"`
    - main.go contains `donation.NewDonationService(` and `auth.RequireRoles(auth.RoleMaker`
    - handler maps ErrInvalidTransition to http.StatusConflict (409)
    - `cd donnarec-api && go build ./...` exits 0 and donation package tests pass
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/donations | untrusted donor data + tax ID cross here |
| service → DB | PII must be ciphertext before it reaches Postgres |
| service → logs | PII must never enter log/error output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | donor_tax_id in DB | mitigate | crypto.EncryptField before INSERT; only donor_tax_id_enc/_dek columns exist (verified TestPII_TaxIDStoredEncrypted) |
| T-03-09 | Information Disclosure | PII in API response | mitigate | DonationResponse exposes DonorTaxIDMasked only; pii.MaskNationalID default (TestPII_MaskDefault) |
| T-03-10 | Information Disclosure | PII in logs/errors | mitigate | Pattern C: log donation_id + created_by UUID only; no zap.String of donor fields |
| T-03-11 | Tampering | mass assignment (bind request → DB model) | mitigate | explicit request structs (CreateDonationRequest); never bind into db model |
| T-03-12 | Elevation of Privilege | non-staff hits donation endpoints | mitigate | RequireRoles(maker/checker/admin) on the route group |
| T-03-13 | Tampering | edit a non-draft record | mitigate | UpdateDraftDonation WHERE status='draft'; 0 rows → ErrInvalidTransition |
</threat_model>

<verification>
- `cd donnarec-api && go build ./...` exits 0
- `cd donnarec-api && go test -count=1 ./internal/donation/... -timeout 300s` passes (non-race)
- raw-DB assertion proves tax ID ciphertext (TestPII_TaxIDStoredEncrypted)
</verification>

<success_criteria>
- Maker can create/edit(draft)/submit a donation; lifecycle draft→pending_review works
- donor tax ID encrypted at rest, masked by default, mandatory; consent captured
- maker routes wired under RBAC; sentinel errors mapped correctly
</success_criteria>

<output>
Create `.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-03-SUMMARY.md` when done
</output>
