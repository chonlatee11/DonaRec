---
phase: 03-donation-lifecycle-maker-checker-issuance
plan: 10
type: execute
wave: 2
depends_on: ["03-09"]
autonomous: false
requirements: [FR-10]
files_modified:
  - donnarec-web/package.json
  - donnarec-web/app/providers.tsx
  - donnarec-web/app/layout.tsx
  - donnarec-web/app/api/bff/donations/route.ts
  - donnarec-web/lib/api.ts
  - donnarec-web/lib/donations.ts
  - donnarec-web/lib/bff.ts
  - donnarec-web/app/donations/page.tsx
  - donnarec-web/components/DonationTable.tsx
  - donnarec-web/components/DonationListView.tsx

must_haves:
  truths:
    - "The donation list screen renders live data from the Go API via a BFF proxy and no longer crashes on undefined.length"
    - "TanStack Query drives the list (client cache + pagination); TanStack Table drives the table"
    - "The Keycloak access token never reaches the browser — the BFF route obtains it via getServerSession and forwards a Bearer to the Go API"
    - "apiFetch unwraps the {data:...} envelope so list/detail callers receive the inner payload"
  artifacts:
    - path: "donnarec-web/app/api/bff/donations/route.ts"
      provides: "GET BFF proxy: getServerSession → forward query string → Go GET /api/donations → return {data:{items,total,page,per_page}}"
      contains: "getServerSession"
    - path: "donnarec-web/app/providers.tsx"
      provides: "QueryClientProvider client wrapper mounted in layout"
      contains: "QueryClientProvider"
    - path: "donnarec-web/components/DonationListView.tsx"
      provides: "client list view using useQuery + TanStack Table against /api/bff/donations"
      contains: "useQuery"
  key_links:
    - from: "components/DonationListView.tsx"
      to: "/api/bff/donations"
      via: "useQuery fetch to the BFF route (client-callable, no token in browser)"
      pattern: "api/bff/donations"
    - from: "app/api/bff/donations/route.ts"
      to: "Go /api/donations"
      via: "getServerSession(authOptions) Bearer forward"
      pattern: "Authorization"
---

<objective>
Deliver the frontend half of the list vertical slice per D-R1 (BFF proxy) and D-R2 (envelope):
add TanStack Query + TanStack Table, mount a `QueryClientProvider`, create the first BFF Route Handler
(`app/api/bff/donations/route.ts`) that obtains the Keycloak token server-side via `getServerSession`
and forwards to the Go API, then migrate the donation LIST screen (Screen 1) to fetch through TanStack
Query and render with TanStack Table against the new `{data:{items,total,page,per_page}}` contract from 03-09.
Also fix `apiFetch` to unwrap the `data` envelope so all callers receive the inner payload.

Purpose: end-to-end working list screen through the real API, token never exposed to the browser.
Output: TanStack infra + BFF pattern + list screen migrated + apiFetch unwrap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONVENTIONS.md
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-CONTEXT-remediation.md
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-UI-SPEC.md
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-09-PLAN.md

<interfaces>
<!-- Contract produced by 03-09 (the list slice's backend). Consume this shape. -->
Go GET /api/donations[?name&status&from&to&receipt_no&page] →
  { "data": { "items": DonationListItem[], "total": number, "page": number, "per_page": 20 } }
DonationListItem = { id, status, donor_name, amount /* string */, donated_at /* YYYY-MM-DD */,
                     receipt_formatted /* string|null */, created_by /* display name */, created_by_id /* UUID */ }

<!-- Existing FE assets to reuse / align. From the real codebase. -->
lib/api.ts: apiFetch<T>(path, opts) attaches Bearer from getServerSession(authOptions); returns res.json() RAW
  (does NOT unwrap `data`). getAccessToken() reads session.accessToken. API_BASE_URL = NEXT_PUBLIC_API_BASE_URL || http://localhost:8000.
lib/auth.ts: authOptions (NextAuth); session.accessToken holds the Keycloak access token.
lib/donations.ts: DonationListResponse currently { donations, total, page, per_page } (WRONG key `donations`);
  searchDonations(filter) builds URLSearchParams (name/status/from/to/receipt_no/page). DonationStatus union.
app/layout.tsx: RootLayout wraps NextIntlClientProvider → AuthSessionProvider → AppShell. Add providers here.
app/donations/page.tsx: Server Component; calls searchDonations server-side; passes result.donations to DonationTable
  (result.donations is undefined under the old bare-array bug → crash). DonationFilterBar drives URL query params.
components/DonationTable.tsx: client table; props donations/total/page/perPage/currentFilter/viewerId; StatusBadge; shadcn Pagination.
package.json: next ^15.3.4, next-auth ^4.24.11, react ^19 — TanStack NOT present yet.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Nothing yet — package legitimacy gate before installing new npm dependencies (no RESEARCH.md Package Legitimacy Audit exists for this remediation slice, so the two TanStack packages are treated as [ASSUMED]).</what-built>
  <how-to-verify>
    Confirm these two packages are the legitimate, official TanStack libraries before install:
    1. Visit https://www.npmjs.com/package/@tanstack/react-query — publisher is the TanStack org, weekly downloads in the millions, repo github.com/TanStack/query.
    2. Visit https://www.npmjs.com/package/@tanstack/react-table — publisher is the TanStack org, repo github.com/TanStack/table.
    Both are named directly in locked decision D-R1. Approve only if both match the official TanStack packages.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with install, or name a substitute.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Add TanStack deps + mount QueryClientProvider + BFF proxy route + apiFetch unwrap</name>
  <files>donnarec-web/package.json, donnarec-web/app/providers.tsx, donnarec-web/app/layout.tsx, donnarec-web/lib/bff.ts, donnarec-web/app/api/bff/donations/route.ts, donnarec-web/lib/api.ts</files>
  <read_first>
    - donnarec-web/app/layout.tsx (provider nesting — add QueryClientProvider inside NextIntlClientProvider/AuthSessionProvider)
    - donnarec-web/lib/api.ts (apiFetch — line 168 returns res.json() raw; getAccessToken at :45)
    - donnarec-web/lib/auth.ts (authOptions, session.accessToken)
    - donnarec-web/app/api/auth (existing route handler style under app/api)
  </read_first>
  <action>
    Add `@tanstack/react-query` and `@tanstack/react-table` to package.json dependencies and install
    (`npm install` in donnarec-web). Create `app/providers.tsx` as a `"use client"` component exporting
    `Providers` that instantiates a single `QueryClient` (via useState so it is stable across renders) and
    wraps children in `QueryClientProvider`. Mount `<Providers>` in `app/layout.tsx` inside
    `AuthSessionProvider` so client components under AppShell can use hooks. Create `lib/bff.ts` exporting a
    server helper `bffForward(request, goPath)` that: calls `getServerSession(authOptions)`, returns 401 JSON
    if no `session.accessToken`, else `fetch(`${API_BASE_URL}${goPath}`, { headers: Authorization Bearer, method,
    body })` and returns the Go response passed through (status + parsed JSON body) — the access token stays on
    the server (D-R1). Create `app/api/bff/donations/route.ts` with a `GET` handler that reads the incoming
    `request.nextUrl.searchParams`, forwards them verbatim to Go `/api/donations?<qs>` via `bffForward`, and
    returns the Go `{data:{items,total,page,per_page}}` JSON unchanged. Finally, in `lib/api.ts`, make
    `apiFetch` unwrap the envelope: when the parsed body is an object with a `data` key, return `body.data as T`
    (204 still returns undefined). Keep the existing error-mapping switch untouched.
  </action>
  <acceptance_criteria>
    - `donnarec-web/package.json` dependencies include `@tanstack/react-query` and `@tanstack/react-table`.
    - `app/providers.tsx` is a client component exporting `Providers` containing `QueryClientProvider`; `app/layout.tsx` renders `<Providers>` around `<AppShell>`.
    - `app/api/bff/donations/route.ts` GET calls `getServerSession(authOptions)` (via lib/bff.ts) and forwards a `Bearer` to Go `/api/donations`; the file never returns the access token in its response body.
    - `lib/api.ts` apiFetch returns `body.data` when present: `grep -nE "\.data\b" donnarec-web/lib/api.ts` shows the unwrap.
    - `cd donnarec-web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd donnarec-web && npx tsc --noEmit</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Migrate the list screen to TanStack Query + TanStack Table against the BFF route</name>
  <files>donnarec-web/lib/donations.ts, donnarec-web/components/DonationListView.tsx, donnarec-web/components/DonationTable.tsx, donnarec-web/app/donations/page.tsx</files>
  <read_first>
    - donnarec-web/lib/donations.ts:26-112 (DonationSummary/DonationListResponse/searchDonations — align to items+created_by/created_by_id; amount is a string now)
    - donnarec-web/components/DonationTable.tsx (columns, StatusBadge, receipt cell, pagination, viewerId routing — preserve UI-SPEC Screen 1)
    - donnarec-web/app/donations/page.tsx (current server-component fetch + viewerId decode + DonationFilterBar wiring)
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-UI-SPEC.md §"Screen 1: Donation List / Search"
  </read_first>
  <action>
    In `lib/donations.ts`: rename `DonationListResponse.donations` → `items` (keys: items/total/page/per_page);
    change `DonationSummary.amount` to `string` (backend returns numeric string); keep `created_by` (name) and
    `created_by_id`. Add a client fetcher `fetchDonations(filter)` that GETs `/api/bff/donations?<qs>` (same
    query keys as before) and returns the unwrapped `{items,total,page,per_page}` (BFF returns the `{data:...}`
    envelope — unwrap `.data` here). Create `components/DonationListView.tsx` (`"use client"`) that takes the
    initial `filter` + `viewerId`, calls `useQuery({ queryKey: ["donations", filter], queryFn: () =>
    fetchDonations(filter) })`, and renders `DonationTable` with `items/total/page/perPage`; show a skeleton on
    `isLoading` and the UI-SPEC error alert on `isError`. Refactor `DonationTable` to build its columns with
    TanStack Table (`@tanstack/react-table` `useReactTable` + `getCoreRowModel`, `flexRender`) while preserving
    the exact UI-SPEC columns/order (วันที่บริจาค / ชื่อผู้บริจาค / จำนวนเงิน right-aligned / สถานะ StatusBadge /
    เลขที่ใบเสร็จ issued|cancelled-only / ผู้สร้าง / จัดการ), the 56px row height, receipt cell rules, viewerId
    draft-routing, and shadcn Pagination; update `formatAmount` to `parseFloat` the string amount. Convert
    `app/donations/page.tsx` to keep server-side viewerId decode + DonationFilterBar, then render
    `<DonationListView filter={filter} viewerId={viewerId} />` instead of fetching server-side; remove the old
    `searchDonations` server call and the `result.donations` access (the crash site).
  </action>
  <acceptance_criteria>
    - `lib/donations.ts` `DonationListResponse` uses key `items` (not `donations`); `DonationSummary.amount` is `string`.
    - `components/DonationListView.tsx` calls `useQuery` and fetches `/api/bff/donations`.
    - `DonationTable` reads `items` (no `result.donations`); `grep -n "result.donations" donnarec-web -r` returns nothing.
    - `DonationTable` uses `@tanstack/react-table` (`useReactTable`) and still renders all 7 UI-SPEC columns in order.
    - `cd donnarec-web && npx tsc --noEmit && npm run build` exits 0 (build proves no `undefined.length` type/runtime break in the list route).
  </acceptance_criteria>
  <verify>
    <automated>cd donnarec-web && npx tsc --noEmit && npm run build</automated>
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The donation list screen now fetches live data from the Go API through the BFF proxy using TanStack Query + TanStack Table. The access token stays server-side.</what-built>
  <how-to-verify>
    Bring the full stack up (docker compose: Go API + Postgres + Keycloak + web) and sign in as a staff user.
    1. Visit `/donations`. The table renders rows (or the Thai empty-state) — NO crash / blank screen.
    2. In browser DevTools → Network: the list request goes to `/api/bff/donations` (same-origin), and the
       response does NOT contain a Keycloak `access_token`/Bearer. Confirm no `Authorization` header is visible
       to client code (it is added server-side in the BFF route).
    3. Filter by donor name / status / date / receipt number and confirm the table updates; pagination works.
    4. Confirm masked national IDs are NOT present in the list rows (list is PII-free per D-53).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g., token visible in browser, crash, wrong columns).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → BFF route (Next server) | untrusted client request; must NOT carry or receive the Keycloak access token |
| BFF route → Go API | server-side Bearer forward; token sourced from getServerSession |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | BFF route / lib/bff.ts | mitigate | token obtained via getServerSession server-side; never serialized into the browser response (D-R1) |
| T-10-02 | Spoofing | /api/bff/donations | mitigate | BFF returns 401 when no server session; Go API re-verifies the Bearer (RequireAuth) — BFF is not the authz authority |
| T-10-03 | Information Disclosure | DonationListView | mitigate | list payload is PII-free (D-53); no national ID rendered client-side |
| T-10-SC | Tampering | npm installs (@tanstack/react-query, @tanstack/react-table) | mitigate | blocking-human legitimacy checkpoint (packages verified on npmjs.com) before install; both named in D-R1 |
</threat_model>

<verification>
- `cd donnarec-web && npx tsc --noEmit && npm run build` exits 0.
- Integration gate (human walkthrough, above checkpoint): list screen renders live data via `/api/bff/donations`; token absent from browser; filters + pagination work.
- Network check: BFF response body contains no access token.
</verification>

<success_criteria>
- List screen renders live Go-API data via TanStack Query + TanStack Table through the BFF proxy.
- apiFetch unwraps `data`; the `undefined.length` crash is gone.
- Keycloak access token never reaches the browser.
</success_criteria>

<output>
Create `.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-10-SUMMARY.md` when done.
</output>
