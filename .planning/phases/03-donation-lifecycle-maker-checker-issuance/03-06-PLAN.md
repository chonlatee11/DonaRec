---
phase: 03-donation-lifecycle-maker-checker-issuance
plan: 06
type: tdd
wave: 5
depends_on: ["03-05"]
files_modified:
  - donnarec-api/internal/donation/service.go
  - donnarec-api/internal/donation/handler.go
  - donnarec-api/internal/donation/model.go
  - donnarec-api/internal/db/queries/donations.sql
  - donnarec-api/internal/db/generated/donations.sql.go
  - donnarec-api/internal/donation/service_test.go
  - donnarec-api/internal/donation/service_integration_test.go
  - donnarec-api/cmd/server/main.go
autonomous: true
requirements: [FR-19, FR-10, FR-29]
must_haves:
  truths:
    - "Cancelling an issued receipt sets status=cancelled, KEEPS its receipt number (no gap, never deleted), and is audited"
    - "Cancelling a record already keyed into e-Donation requires an explicit RD-confirmation reason (D-51)"
    - "Void & Reissue cancels the original (links replaced_by) and creates a NEW draft (links replaces) that earns a fresh number via the normal approval path"
    - "PII reveal is gated to Checker/Admin, decrypts the tax ID, and writes a pii.reveal audit row; staff can search by name/date/status/receipt number only"
  artifacts:
    - path: "donnarec-api/internal/donation/service.go"
      provides: "Cancel (void), Reissue, RevealPII, Search wiring"
      contains: "func (s *DonationService) Cancel"
    - path: "donnarec-api/internal/donation/handler.go"
      provides: "cancel/reissue/reveal/search handlers"
      contains: "RevealPII"
  key_links:
    - from: "internal/donation/service.go Cancel"
      to: "donations (receipt_number_id retained)"
      via: "CancelDonation keeps number, sets status=cancelled (FR-19)"
      pattern: "CancelDonation"
    - from: "internal/donation/service.go RevealPII"
      to: "internal/crypto.DecryptField + audit"
      via: "CanRevealFull gate → decrypt → AppendAuditEntryTx pii.reveal"
      pattern: "crypto\\.DecryptField"
    - from: "internal/donation/service.go Reissue"
      to: "donations.replaces / replaced_by"
      via: "self-FK link old↔new (D-50)"
      pattern: "replaced_by"
---

<objective>
Slice ปิดท้าย: ยกเลิกใบเสร็จ (Void + Void & Reissue, D-50), edonation_keyed guard (D-51), PII reveal แบบ audited (D-46), และ search/filter (D-53). ทุกอย่างรักษา immutability ของเอกสารภาษี — cancel เก็บเลข, ไม่ลบ, audited.

Purpose: ส่งมอบ SC#4 (cancel retains number) และส่วนที่เหลือของ SC#5 (audited reveal + search). ปิด FR-19, FR-10, และ reveal-path ของ FR-29.
Output: `DonationService.Cancel/Reissue/RevealPII` + Search wiring + cancel/reissue/reveal/search routes; tests INV-4 (cancel retains number), INV-5 (reveal), D-50 reissue, D-51 keyed guard, FR-10 search เขียว.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-CONTEXT.md
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-PATTERNS.md
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-RESEARCH.md

<interfaces>
internal/crypto: DecryptField(ctx, kp, ciphertext, wrappedDEK []byte) ([]byte, error)
internal/pii: CanRevealFull(claims) bool  // Admin+Checker only (D-46)
internal/audit: AppendAuditEntryTx(ctx, tx, AuditEntry{Action:"pii.reveal" / "donation.cancel"})
sqlc (03-01): CancelDonation (WHERE status='issued', keeps receipt_number_id), SetReplacedBy, SearchDonations (D-53 nullable filters), GetDonationByID, CreateDonation, LockDonationForUpdate
sentinels (03-03): ErrInvalidTransition, ErrForbidden, ErrMissingReason, ErrEDonationKeyedConfirmation
Reissue path: new record starts at draft and re-uses the normal Submit→Approve (03-05) — NO bypass of maker-checker / SoD (D-50)
Cancel auth: Checker + Admin only (D-47); Maker cannot cancel
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Cancel (void) retains number + e-Donation keyed guard (RED→GREEN)</name>
  <files>donnarec-api/internal/donation/service.go, donnarec-api/internal/donation/model.go, donnarec-api/internal/donation/service_test.go, donnarec-api/internal/donation/service_integration_test.go</files>
  <read_first>
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-CONTEXT.md §"Receipt cancellation" (D-47, D-50, D-51)
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-RESEARCH.md INV-4 + Pitfall 5 + Pitfall 7
    - donnarec-api/internal/donation/service.go (03-05 output — extend)
  </read_first>
  <behavior>
    - TestCancelRetainsReceiptNumber: issue A (2569/000001) → Cancel A → status=cancelled AND receipt_number_id still set; issue B → 2569/000002 (no reuse, no gap)
    - TestCancelRequiresReason: Cancel with empty reason → ErrMissingReason
    - TestCancelAuthCheckerAdminOnly: Maker claims → ErrForbidden (D-47)
    - TestEDonationKeyedGuard: Cancel when edonation_keyed=true with no rd_confirmation_reason → ErrEDonationKeyedConfirmation; with rd_confirmation_reason present → succeeds and the confirmation is recorded in audit (D-51)
  </behavior>
  <action>
    เขียน tests RED ก่อน. Add `CancelDonationRequest{Reason string validate:"required", RDConfirmationReason string}` to model.go. Implement `Cancel(ctx, id, req, claims)` inside WithTx: gate role via `claims.HasRole(checker/admin)` else ErrForbidden (D-47); require Reason else ErrMissingReason; `LockDonationForUpdate` + precondition status='issued' else ErrInvalidTransition; if `donation.EdonationKeyed` is true and `req.RDConfirmationReason` is empty → `ErrEDonationKeyedConfirmation` (D-51 / Pitfall 7); `qtx.CancelDonation` (status=cancelled, cancelled_by/at, cancel_reason — KEEPS receipt_number_id, no gap, FR-19 / Pitfall 5); `AppendAuditEntryTx(Action:"donation.cancel", AfterJSON includes reason + rd_confirmation when keyed)`. Pattern C logging. Never null the receipt_number_id.
  </action>
  <verify>
    <automated>cd donnarec-api && go test -count=1 -run 'TestCancelRetainsReceiptNumber|TestCancelRequiresReason|TestCancelAuthCheckerAdminOnly|TestEDonationKeyedGuard' ./internal/donation/... -timeout 240s</automated>
  </verify>
  <done>cancel keeps the number (no gap), requires reason, Checker/Admin only, keyed records require RD confirmation; all audited.</done>
  <acceptance_criteria>
    - Cancel calls `qtx.CancelDonation` and never sets receipt_number_id to NULL
    - TestCancelRetainsReceiptNumber asserts cancelled record still has receipt_number_id and the next issue is consecutive
    - Cancel returns ErrEDonationKeyedConfirmation when edonation_keyed && empty RDConfirmationReason
    - the 4 named tests exit 0
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Void & Reissue (link replaces/replaced_by) (RED→GREEN)</name>
  <files>donnarec-api/internal/donation/service.go, donnarec-api/internal/db/queries/donations.sql, donnarec-api/internal/db/generated/donations.sql.go, donnarec-api/internal/donation/service_integration_test.go</files>
  <read_first>
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-CONTEXT.md §"D-50" (Void & Reissue, link fields, no bypass of maker-checker)
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-RESEARCH.md §"Phase Requirements → Test Map" (TestVoidAndReissue)
  </read_first>
  <behavior>
    - TestVoidAndReissue: from an issued record, Reissue cancels the original (status=cancelled, replaced_by = new id) AND creates a NEW donation in status=draft (corrected data) with replaces = original id; the new record then goes through normal Submit→Approve (SoD applies) and earns a fresh consecutive number; original keeps its old number
  </behavior>
  <action>
    เขียน test RED ก่อน. Implement `Reissue(ctx, originalID, req, claims)`: inside WithTx — perform the same Cancel guards (Checker/Admin, reason, keyed-confirmation) on the original; create a NEW donation row from corrected request data (re-encrypt tax ID via EncryptField, copy/override donor + consent fields) at status='draft' with `replaces = originalID`; set the original `replaced_by = newID` via a query (`SetReplacedBy` from 03-01, or add `SetReplacesLink`/`SetReplacedByLink` to donations.sql + regenerate if needed); audit "donation.reissue". CRITICAL: the new record does NOT auto-issue — it walks the normal lifecycle (Submit then Approve from 03-05) so SoD + numbering are unchanged (D-50, no bypass). The fresh number is earned only when the new record is approved through the existing issuance tx.
  </action>
  <verify>
    <automated>cd donnarec-api && go test -count=1 -run 'TestVoidAndReissue' ./internal/donation/... -timeout 240s</automated>
  </verify>
  <done>reissue cancels+links original, creates a corrected draft linked via replaces, and the replacement earns a fresh number only via normal approval (no bypass).</done>
  <acceptance_criteria>
    - service.go Reissue sets original.replaced_by and new.replaces (self-FK link)
    - the new record is created at status='draft' (not auto-issued)
    - TestVoidAndReissue asserts both links and that the replacement number is fresh/consecutive after a normal approve
    - test exits 0
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: PII reveal (audited) + Search/filter + handlers + routes</name>
  <files>donnarec-api/internal/donation/service.go, donnarec-api/internal/donation/handler.go, donnarec-api/internal/donation/service_integration_test.go, donnarec-api/cmd/server/main.go</files>
  <read_first>
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-RESEARCH.md INV-5 (reveal) + §"SearchDonations"
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-CONTEXT.md §"PII reveal" (D-46) and §"Search/filter scope" (D-53)
    - donnarec-api/internal/pii/mask.go (CanRevealFull) + internal/crypto/envelope.go (DecryptField)
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-PATTERNS.md §"PII reveal path"
  </read_first>
  <behavior>
    - TestPII_RevealRequiresCheckerOrAdmin: Maker → ErrForbidden (403); Checker → plaintext "1234567890123" AND an audit_log row with action="pii.reveal"
    - TestSearchDonations: filter by donor_name ILIKE / status / from_date / to_date / receipt_no returns the matching subset; passing none returns all (paginated); tax-ID is NOT a searchable field (D-53)
  </behavior>
  <action>
    เขียน tests RED ก่อน. Implement `RevealPII(ctx, id, claims)` inside WithTx: `pii.CanRevealFull(claims)` false → ErrForbidden (D-46); load row; `crypto.DecryptField(ctx, s.keyProvider, row.DonorTaxIdEnc, row.DonorTaxIdDek)`; write `AppendAuditEntryTx(Action:"pii.reveal", Resource path)` BEFORE returning plaintext (D-13 ordering); return plaintext. Implement `Search(ctx, filter, claims)` mapping a `SearchFilter{DonorName, Status, FromDate, ToDate, ReceiptNo, Limit, Offset}` to `qtx.SearchDonations` nullable params (D-53; never accept a tax-ID filter). handler.go: add `RevealPII` (GET, maps ErrForbidden→403), and fold search filters into the existing `List` handler (query params name/status/from/to/receipt_no/page → SearchFilter, default limit 20 per UI-SPEC). Also add `Cancel` and `Reissue` handlers (bind CancelDonationRequest; map ErrForbidden→403, ErrMissingReason→422, ErrEDonationKeyedConfirmation→409 {"error":"edonation_keyed_confirmation_required"}, ErrInvalidTransition→409). main.go: register on the checker/admin group POST "/:id/cancel", POST "/:id/reissue"; on the staff group GET "/:id/pii" (RevealPII — role gated in service, not just route). Set audit_after. Pattern C.
  </action>
  <verify>
    <automated>cd donnarec-api && go build ./... && go test -count=1 -race ./internal/donation/... -timeout 400s</automated>
  </verify>
  <done>reveal is Checker/Admin-only + audited; search supports name/date/status/receipt_no only; cancel/reissue/reveal routes wired; full donation suite green under -race.</done>
  <acceptance_criteria>
    - RevealPII calls CanRevealFull then DecryptField then AppendAuditEntryTx (audit before return)
    - Search maps to SearchDonations and exposes NO tax-ID filter parameter
    - main.go registers POST "/:id/cancel", POST "/:id/reissue" (checker/admin) and GET "/:id/pii"
    - `cd donnarec-api && go test -count=1 -race ./internal/donation/...` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checker → /cancel, /reveal | privileged actions on a legal document + PII decryption |
| client → search params | untrusted filter input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-24 | Tampering | cancel creates a gap (number reused/deleted) | mitigate | CancelDonation keeps receipt_number_id; REVOKE DELETE; TestCancelRetainsReceiptNumber |
| T-03-25 | Repudiation | keyed receipt cancelled without RD reconciliation | mitigate | edonation_keyed guard requires RDConfirmationReason, recorded in audit (D-51, Pitfall 7) |
| T-03-26 | Information Disclosure | unauthorized PII reveal | mitigate | CanRevealFull gate (Checker/Admin) → 403 for Maker; decrypt only after gate |
| T-03-27 | Repudiation | reveal not audited | mitigate | AppendAuditEntryTx pii.reveal written before plaintext returned (D-13) |
| T-03-28 | Injection | SQL injection via search filters | mitigate | sqlc named params only (Pattern F); ILIKE value parameterized, no string concat |
| T-03-29 | Information Disclosure | search/enumerate by national ID | mitigate | search scope excludes tax ID (D-53); no blind index; tax ID not a filter param |
| T-03-30 | Elevation of Privilege | Maker cancels a receipt | mitigate | Cancel gated to Checker/Admin in service (D-47) + checker route group |
</threat_model>

<verification>
- `cd donnarec-api && go test -count=1 -race ./internal/donation/... -timeout 400s` passes (full suite green — phase gate)
- reveal writes a pii.reveal audit row; Maker reveal is 403
- cancel retains number proven by consecutive next-issue
</verification>

<success_criteria>
- Cancel retains the number (no gap), Checker/Admin only, keyed records require RD confirmation, all audited
- Void & Reissue links original↔replacement; replacement earns a fresh number via normal approval (no bypass)
- PII reveal is role-gated + audited; search supports name/date/status/receipt number only
</success_criteria>

<output>
Create `.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-06-SUMMARY.md` when done
</output>
