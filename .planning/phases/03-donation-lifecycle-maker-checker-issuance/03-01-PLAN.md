---
phase: 03-donation-lifecycle-maker-checker-issuance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - donnarec-api/migrations/000005_donations.up.sql
  - donnarec-api/migrations/000005_donations.down.sql
  - donnarec-api/migrations/000007_outbox_jobs.up.sql
  - donnarec-api/migrations/000007_outbox_jobs.down.sql
  - donnarec-api/internal/db/queries/donations.sql
  - donnarec-api/internal/db/queries/outbox.sql
  - donnarec-api/internal/db/generated/donations.sql.go
  - donnarec-api/internal/db/generated/outbox.sql.go
  - donnarec-api/internal/db/generated/models.go
  - donnarec-api/internal/db/generated/querier.go
  - donnarec-api/internal/donation/service_test.go
  - donnarec-api/internal/donation/service_integration_test.go
  - donnarec-api/internal/storage/client_test.go
autonomous: true
requirements: [FR-11, FR-14, FR-19, FR-29]
must_haves:
  truths:
    - "Migrations 000005 and 000007 apply and roll back cleanly against Postgres 17"
    - "donations table enforces SoD and receipt-only-on-issued/cancelled at the DB level"
    - "sqlc generates type-safe Go for every donation and outbox query without error"
    - "Wave 0 test files compile and fail (or skip) — they define the contract before implementation"
    - "D-43: donor fields are stored as a per-donation snapshot on the donations table; no donor master table and no blind index in Phase 3"
  artifacts:
    - path: "donnarec-api/migrations/000005_donations.up.sql"
      provides: "donations table + donation_status enum + SoD/receipt CHECK constraints + indexes + GRANT/REVOKE"
      contains: "chk_sod_approver"
    - path: "donnarec-api/migrations/000007_outbox_jobs.up.sql"
      provides: "transactional outbox_jobs table"
      contains: "CREATE TABLE outbox_jobs"
    - path: "donnarec-api/internal/db/queries/donations.sql"
      provides: "LockDonationForUpdate, CreateDonation, IssueDonation, SearchDonations, etc."
      contains: "FOR UPDATE"
    - path: "donnarec-api/internal/db/queries/outbox.sql"
      provides: "EnqueueOutboxJob"
      contains: "INSERT INTO outbox_jobs"
  key_links:
    - from: "internal/db/queries/donations.sql"
      to: "internal/db/generated/donations.sql.go"
      via: "sqlc generate"
      pattern: "func .*LockDonationForUpdate"
    - from: "migrations/000005_donations.up.sql"
      to: "receipt_numbers(id)"
      via: "FK receipt_number_id (D-38)"
      pattern: "REFERENCES receipt_numbers"
---

<objective>
สร้าง data-layer foundation ของ Phase 3: migration ของ donation entity + outbox, sqlc queries และ Wave 0 test scaffolds ที่กำหนด contract ก่อน implementation (TDD mode).

Purpose: ทุก vertical slice (create/submit/approve/cancel/search) เขียนทับ schema และ query contract เดียวกัน — interface-first ป้องกัน scavenger hunt และทำให้ test scaffolds พร้อมก่อน service code.
Output: migration 000005 (donations) + 000007 (outbox_jobs), `donations.sql`/`outbox.sql` + sqlc-generated Go, และไฟล์ test ว่าง (failing/skip) ตาม 7 invariants ใน RESEARCH Validation Architecture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-CONTEXT.md
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-RESEARCH.md
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-PATTERNS.md

<interfaces>
<!-- Reuse contracts (do not reinvent). Extracted from codebase. -->
From internal/receiptno/allocator.go:
  type AllocatedReceipt struct { ID int64; FiscalYear int; RunningNo int; Formatted string; AllocatedAt time.Time }
    // ID = receipt_numbers ledger PK (BIGSERIAL). It is the value persisted in donations.receipt_number_id (D-38 FK).
    // InsertReceiptNumberLedger already does `RETURNING id`; surface it on AllocatedReceipt.ID (one-line struct field + `ID: ledger.ID` in allocator.go) so 03-05 issuance sets the FK without a re-query.
  func (a *Allocator) Allocate(ctx context.Context, tx pgx.Tx, issueDate time.Time) (AllocatedReceipt, error)
From internal/db/generated/db.go:
  func (q *Queries) WithTx(tx pgx.Tx) *Queries
Migration analog: migrations/000004_receipt_number_tables.up.sql (enum + table + CHECK + GRANT/REVOKE pattern)
Query analog: internal/db/queries/receiptno.sql (named @params, FOR UPDATE, RETURNING)
sqlc config: internal/db/sqlc.yaml
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 000005 (donations) + 000007 (outbox_jobs)</name>
  <files>donnarec-api/migrations/000005_donations.up.sql, donnarec-api/migrations/000005_donations.down.sql, donnarec-api/migrations/000007_outbox_jobs.up.sql, donnarec-api/migrations/000007_outbox_jobs.down.sql</files>
  <read_first>
    - donnarec-api/migrations/000004_receipt_number_tables.up.sql (exact analog: enum→table→CHECK→index→GRANT→REVOKE)
    - donnarec-api/migrations/000004_receipt_number_tables.down.sql (down ordering analog)
    - donnarec-api/migrations/000001_init_schema.up.sql (users table, legal_basis_enum, user_role_enum)
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-RESEARCH.md §"SQL: Donation Table" and §"SQL: Outbox Jobs Table"
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-PATTERNS.md §"Pattern G: Migration Structure"
  </read_first>
  <action>
    สร้าง `CREATE TYPE donation_status AS ENUM ('draft','pending_review','issued','rejected','cancelled')` แล้ว `CREATE TABLE donations` ตาม schema ใน RESEARCH §"SQL: Donation Table": id UUID PK; created_by/created_at/updated_at/status; donor snapshot (donor_name, donor_address, donor_email nullable); PII ciphertext columns donor_tax_id_enc BYTEA NOT NULL + donor_tax_id_dek BYTEA NOT NULL (D-44 NOT NULL); amount NUMERIC(15,2) CHECK (amount > 0); donated_at DATE; notes; consent columns consent_given/consent_at/consent_text_version/consent_purpose (D-49); retain_until + legal_basis (default 'tax_obligation'); submitted_at; review columns reviewed_by/reviewed_at/review_reason; approval columns approved_by/approved_at; receipt_number_id BIGINT REFERENCES receipt_numbers(id) (D-38) + receipt_formatted TEXT (frozen snapshot D-42); cancellation columns cancelled_by/cancelled_at/cancel_reason + edonation_keyed BOOLEAN NOT NULL DEFAULT false (D-51); self-FK reissue links replaces UUID REFERENCES donations(id) + replaced_by UUID REFERENCES donations(id) (D-50). Add constraint `chk_sod_approver CHECK (approved_by IS NULL OR approved_by != created_by)` (CLAUDE.md SoD defense-in-depth) and `chk_receipt_only_on_issued_or_cancelled` exactly as in RESEARCH (receipt_number_id + receipt_formatted set IFF status IN ('issued','cancelled')). Create indexes idx_donations_donor_name, idx_donations_donated_at, idx_donations_status, partial idx_donations_receipt_number_id WHERE receipt_number_id IS NOT NULL, idx_donations_created_by, partial idx_donations_approved_by. GRANT SELECT,INSERT,UPDATE ON donations TO donnarec_app; REVOKE DELETE ON donations FROM donnarec_app (FR-19 immutability). For 000007 create outbox_jobs (id BIGSERIAL PK, job_type TEXT, payload JSONB, status TEXT DEFAULT 'pending' CHECK status IN pending/processing/done/failed, created_at/updated_at, attempts INT, last_error) + partial index idx_outbox_jobs_pending + GRANT SELECT,INSERT,UPDATE + GRANT USAGE,SELECT ON SEQUENCE outbox_jobs_id_seq. Down files DROP in reverse dependency order (table then type). File headers document the D-xx realized (Pattern G).
  </action>
  <verify>
    <automated>cd donnarec-api && go test -count=1 -run TestMigrationsApplyAndRollback ./internal/donation/... -timeout 180s</automated>
  </verify>
  <done>migration 000005 + 000007 apply via testcontainers (testutil.SetupTestPostgres runs all migrations) without error; down files defined; constraints chk_sod_approver and chk_receipt_only_on_issued_or_cancelled present.</done>
  <acceptance_criteria>
    - 000005_donations.up.sql contains `CREATE TYPE donation_status AS ENUM` with exactly the 5 values
    - 000005_donations.up.sql contains `CONSTRAINT chk_sod_approver CHECK (approved_by IS NULL OR approved_by != created_by)`
    - 000005_donations.up.sql contains `REVOKE DELETE ON donations FROM donnarec_app`
    - 000007_outbox_jobs.up.sql contains `CREATE TABLE outbox_jobs` with status CHECK
    - testutil.SetupTestPostgres(t) applies all migrations 000001..000007 with no error (proven by the scaffold integration test in Task 3 reaching its body)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: sqlc queries (donations.sql + outbox.sql) + generate</name>
  <files>donnarec-api/internal/db/queries/donations.sql, donnarec-api/internal/db/queries/outbox.sql, donnarec-api/internal/db/generated/donations.sql.go, donnarec-api/internal/db/generated/outbox.sql.go, donnarec-api/internal/db/generated/models.go, donnarec-api/internal/db/generated/querier.go</files>
  <read_first>
    - donnarec-api/internal/db/queries/receiptno.sql (FOR UPDATE :one, UPDATE RETURNING, INSERT RETURNING, named @params)
    - donnarec-api/internal/db/queries/users.sql (named param + explicit column list pattern)
    - donnarec-api/internal/db/sqlc.yaml (codegen config — output package path)
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-PATTERNS.md §"internal/db/queries/donations.sql" and §"Pattern F"
  </read_first>
  <action>
    เขียน `donations.sql` ด้วย named @params + explicit column lists (Pattern F, no SELECT * in writes, no string concat). Queries: `LockDonationForUpdate :one` (SELECT id,status,created_by,receipt_number_id,edonation_keyed ... WHERE id=@id FOR UPDATE — D-52); `CreateDonation :one` (INSERT donor snapshot + ciphertext + consent + retain/legal_basis, RETURNING id + columns); `GetDonationByID :one`; `UpdateDraftDonation :exec` (WHERE id=@id AND status='draft' — only drafts editable, FR-09); `SubmitDonation :exec` (SET status='pending_review', submitted_at=now() WHERE id=@id AND status='draft'); `ReturnDonation :exec` (status='draft', reviewed_by/reviewed_at/review_reason WHERE status='pending_review'); `RejectDonation :exec` (status='rejected' WHERE status='pending_review'); `IssueDonation :exec` exactly as RESEARCH (SET status='issued', approved_by/at, receipt_number_id, receipt_formatted WHERE id=@id AND status='pending_review' — precondition safety); `CancelDonation :exec` (status='cancelled', cancelled_by/at, cancel_reason, replaced_by nullable WHERE id=@id AND status='issued' — keeps receipt_number_id, FR-19); `SetReplacedBy :exec` and `SearchDonations :many` exactly as RESEARCH (nullable @donor_name ILIKE / @status / @from_date / @to_date / @receipt_no + LIMIT/OFFSET, D-53). Write `outbox.sql` with `EnqueueOutboxJob :exec` (INSERT outbox_jobs (job_type,payload,status) VALUES (@job_type,@payload,'pending')). Run `sqlc generate` from internal/db (per sqlc.yaml) to regenerate donations.sql.go, outbox.sql.go, models.go, querier.go. Ensure `go build ./...` passes.
  </action>
  <verify>
    <automated>cd donnarec-api/internal/db && sqlc generate && cd ../../ && go build ./...</automated>
  </verify>
  <done>sqlc generate completes with no error; generated Queries has LockDonationForUpdate/CreateDonation/IssueDonation/CancelDonation/SearchDonations/EnqueueOutboxJob; go build passes.</done>
  <acceptance_criteria>
    - `grep -c "FOR UPDATE" donnarec-api/internal/db/queries/donations.sql` returns >= 1 (LockDonationForUpdate)
    - donations.sql contains `WHERE id = @id` AND `status = 'pending_review'` in IssueDonation (precondition)
    - generated querier.go contains `LockDonationForUpdate(` and `EnqueueOutboxJob(`
    - `cd donnarec-api && go build ./...` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wave 0 test scaffolds (donation + storage)</name>
  <files>donnarec-api/internal/donation/service_test.go, donnarec-api/internal/donation/service_integration_test.go, donnarec-api/internal/storage/client_test.go</files>
  <read_first>
    - donnarec-api/internal/receiptno/allocator_concurrency_test.go (testcontainers + errgroup + testing.Short skip + DB-level assertion patterns)
    - donnarec-api/internal/testutil/postgres.go (SetupTestPostgres signature + migration path)
    - donnarec-api/internal/users/service_test.go (unit test table-driven pattern)
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-RESEARCH.md §"The 7 Hardest Invariants" and §"Phase Requirements → Test Map"
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-VALIDATION.md
  </read_first>
  <action>
    สร้างไฟล์ test ว่าง (compile ได้ แต่ fail/skip) ที่กำหนด contract ตาม 7 invariants. ใน `service_test.go` (package donation, white-box ok) เขียน table-driven `TestMigrationsApplyAndRollback` (helper that calls testutil.SetupTestPostgres to prove migrations apply — used by Task 1 verify), `TestStateMachine_InvalidTransitions` (cases from RESEARCH INV-6), `TestMandatoryReason`, `TestConsentCapture`, `TestEDonationKeyedGuard` — each body `t.Fatal("not implemented — filled by 03-03/05/06")` so they RED. In `service_integration_test.go` (package donation_test, black-box, with `if testing.Short() { t.Skip }` guard) add signatures `TestIssuanceTransaction_RollbackOnError`, `TestOutboxAtomicity`, `TestSoD_ApproverCannotBeCreator`, `TestSoD_DBCheckConstraint`, `TestConcurrentApproval_ExactlyOneSucceeds`, `TestCancelRetainsReceiptNumber`, `TestPII_TaxIDStoredEncrypted`, `TestPII_RevealRequiresCheckerOrAdmin`, `TestPII_MaskDefault`, `TestVoidAndReissue`, `TestSearchDonations`, `TestCreateDonation`, `TestEditDraft` — each `t.Skip("Wave 0 scaffold — implemented in later plan")`. In `storage/client_test.go` add `TestMagicByteRejectsSpoofed`, `TestSizeLimit`, `TestAllowedTypes` as t.Skip scaffolds. Do NOT implement service logic here — only the failing/skipping test contract. Ensure the package compiles (declare minimal `package donation` files only if needed for compilation, but prefer test-only files referencing exported symbols added in later plans — guard non-compiling references behind build-incomplete stubs by keeping bodies free of undefined identifiers; use t.Skip before any reference to not-yet-existing types).
  </action>
  <verify>
    <automated>cd donnarec-api && go vet ./internal/donation/... ./internal/storage/... && go test -count=1 -short ./internal/donation/... ./internal/storage/... 2>&1 | head -20</automated>
  </verify>
  <done>test files compile (go vet clean); running with -short skips integration tests and the unit scaffolds are present (RED via t.Fatal where applicable). Every invariant from RESEARCH §7 Hardest Invariants has a named test function.</done>
  <acceptance_criteria>
    - `grep -c "func Test" donnarec-api/internal/donation/service_integration_test.go` returns >= 11
    - service_integration_test.go contains `func TestConcurrentApproval_ExactlyOneSucceeds(`
    - service_integration_test.go contains `func TestIssuanceTransaction_RollbackOnError(`
    - storage/client_test.go contains `func TestMagicByteRejectsSpoofed(`
    - `cd donnarec-api && go vet ./internal/donation/... ./internal/storage/...` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app role → DB | donnarec_app role acts on donations/outbox; DB constraints are the last line of defense |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | donations.approved_by == created_by (SoD bypass at DB) | mitigate | chk_sod_approver CHECK constraint in 000005 (defense-in-depth backstop; code guard in 03-05) |
| T-03-02 | Tampering | receipt number set on non-issued record (gap/used-up number) | mitigate | chk_receipt_only_on_issued_or_cancelled CHECK constraint |
| T-03-03 | Tampering | DELETE of donation/receipt row (immutability / audit loss) | mitigate | REVOKE DELETE ON donations FROM donnarec_app (FR-19) |
| T-03-04 | Information Disclosure | national/tax ID stored in plaintext | mitigate | columns are BYTEA ciphertext only (donor_tax_id_enc/_dek); no plaintext column exists |
| T-03-SC | Tampering | Go module supply chain (minio-go added in 03-04) | accept | minio-go v7.2.1 = official MinIO org, Approved in RESEARCH Package Legitimacy Audit; not [ASSUMED]/[SUS]; no install in this plan |
</threat_model>

<verification>
- `cd donnarec-api && go build ./...` exits 0
- `cd donnarec-api/internal/db && sqlc generate` exits 0
- testcontainers applies migrations 000001..000007 cleanly
</verification>

<success_criteria>
- donations + outbox_jobs schema exists with SoD + receipt-on-issued CHECK constraints and REVOKE DELETE
- sqlc-generated Go compiles and exposes all donation/outbox queries
- Wave 0 test scaffolds compile and define the 7-invariant contract (RED/skip)
</success_criteria>

<output>
Create `.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-01-SUMMARY.md` when done
</output>
