---
phase: 03-donation-lifecycle-maker-checker-issuance
plan: 04
type: tdd
wave: 3
depends_on: ["03-03"]
files_modified:
  - donnarec-api/migrations/000006_slip_attachments.up.sql
  - donnarec-api/migrations/000006_slip_attachments.down.sql
  - donnarec-api/internal/db/queries/slip.sql
  - donnarec-api/internal/db/generated/slip.sql.go
  - donnarec-api/internal/storage/client.go
  - donnarec-api/internal/storage/client_test.go
  - donnarec-api/internal/donation/slip_service.go
  - donnarec-api/internal/donation/slip_handler.go
  - donnarec-api/internal/config/config.go
  - donnarec-api/docker-compose.yml
  - donnarec-api/go.mod
  - donnarec-api/go.sum
  - donnarec-api/cmd/server/main.go
autonomous: true
requirements: [FR-09]
must_haves:
  truths:
    - "A staff user can attach an optional slip (jpg/png/pdf) to a donation; type is validated by magic bytes, not extension"
    - "Files over 10 MB are rejected; slips are stored in MinIO object storage, never as DB BLOB"
    - "Viewing a slip returns a short-lived presigned URL"
    - "Removing/replacing a slip soft-deletes the reference (file retained) and is audited (D-54)"
    - "D-48: the slip attachment is optional in Flow A — cash/no-slip donations are fully supported"
  artifacts:
    - path: "donnarec-api/internal/storage/client.go"
      provides: "MinIO client + PutSlip (magic-byte + size) + PresignedGet"
      contains: "mimetype.Detect"
    - path: "donnarec-api/migrations/000006_slip_attachments.up.sql"
      provides: "slip_attachments reference table (soft-delete, FK donations)"
      contains: "REFERENCES donations(id)"
    - path: "donnarec-api/internal/donation/slip_handler.go"
      provides: "upload/view/remove slip handlers"
      contains: "UploadSlip"
  key_links:
    - from: "internal/storage/client.go"
      to: "MinIO (minio-go/v7)"
      via: "PutObject after magic-byte validation"
      pattern: "PutObject"
    - from: "internal/donation/slip_service.go"
      to: "audit.AppendAuditEntryTx"
      via: "audit slip remove (soft-delete) D-54"
      pattern: "AppendAuditEntry"
user_setup:
  - service: minio
    why: "S3-compatible object storage for donation slips (Flow A optional upload; Flow B reuse Phase 6)"
    env_vars:
      - name: MINIO_ENDPOINT
        source: "docker-compose minio service (localhost:9000 in dev)"
      - name: MINIO_ACCESS_KEY
        source: "MINIO_ROOT_USER (docker-compose .env)"
      - name: MINIO_SECRET_KEY
        source: "MINIO_ROOT_PASSWORD (docker-compose .env)"
      - name: MINIO_BUCKET
        source: "bucket name, default donnarec-slips"
      - name: MINIO_SECURE
        source: "false in dev, true in prod (TLS)"
---

<objective>
Slip storage slice: เพิ่ม object storage seam (MinIO/S3-compatible) + magic-byte validation + size limit + soft-delete-retain สำหรับสลิป Flow A (optional). ตรง ROADMAP SC#1 "view any attached slip"; Phase 6 (Flow B public upload) reuse seam นี้.

Purpose: ส่งมอบ "Maker แนบ/ดูสลิป" (FR-09) อย่างปลอดภัย — validate content จริง (D-48), ไม่เก็บ BLOB ใน DB, ไม่ hard-delete ไฟล์ (D-54).
Output: `internal/storage/client.go` (MinIO), migration 000006 slip_attachments, slip_service/slip_handler, config MinIO + docker-compose minio service.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-CONTEXT.md
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-RESEARCH.md
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-PATTERNS.md

<interfaces>
<!-- New package minio-go/v7 v7.2.1 (Approved in RESEARCH Package Legitimacy Audit). -->
github.com/minio/minio-go/v7: minio.New(endpoint, &minio.Options{Creds, Secure}); PutObject(ctx, bucket, object, reader, size, opts); PresignedGetObject(ctx, bucket, object, expiry, reqParams)
github.com/minio/minio-go/v7/pkg/credentials: NewStaticV4(accessKey, secretKey, "")
github.com/gabriel-vasile/mimetype (already in go.mod): mimetype.Detect(buf) → .String(), .Extension()
config analog: internal/config/config.go (getEnvStr/getEnvInt + validate() required map)
audit: AppendAuditEntryTx(ctx, tx, AuditEntry{Action:"donation.slip.remove", ...})
RESEARCH Pattern 4 (PutSlip magic-byte + size) and Pattern 5 (NewMinIOClient)
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: MinIO storage client + magic-byte/size validation (RED→GREEN)</name>
  <files>donnarec-api/internal/storage/client.go, donnarec-api/internal/storage/client_test.go, donnarec-api/go.mod, donnarec-api/go.sum</files>
  <read_first>
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-RESEARCH.md §"Pattern 4: Magic-Byte Slip Validation" and §"Pattern 5: MinIO Client Initialization" and §"Package Legitimacy Audit"
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-PATTERNS.md §"internal/storage/client.go"
    - donnarec-api/internal/audit/service.go §NewAuditService (panic-guard constructor style to mirror)
  </read_first>
  <behavior>
    - TestAllowedTypes: a real JPEG/PNG/PDF byte stream passes validation and returns an object key
    - TestMagicByteRejectsSpoofed: a .pdf-named file whose bytes are actually a script/text → ErrUnsupportedFileType (content, not extension)
    - TestSizeLimit: a stream > 10 MB → ErrFileTooLarge
  </behavior>
  <action>
    `go get github.com/minio/minio-go/v7@v7.2.1` (Approved package). เขียน client_test.go RED ก่อน (use in-memory byte readers; the validation path needs no live MinIO — separate the validate function from PutObject so it is unit-testable). Implement `storage/client.go`: `StorageClient{client *minio.Client, bucket string}`; `NewStorageClient(endpoint, accessKey, secretKey, bucket string, secure bool) (*StorageClient, error)` (minio.New + NewStaticV4). Implement `validateSlip(r io.Reader, size int64) (mime *mimetype.MIME, head []byte, err error)` enforcing `maxSlipSize = 10<<20` via io.LimitReader and magic-byte allowlist {image/jpeg, image/png, application/pdf} → ErrUnsupportedFileType / ErrFileTooLarge (sentinels in storage package). Implement `PutSlip(ctx, r, size, donationID) (objectKey string, err error)` that calls validateSlip then `client.PutObject` with reassembled reader (io.MultiReader prepend consumed head bytes per RESEARCH Pattern 4), objectKey = "slips/{donationID}/{uuid}{ext}". Implement `PresignedGet(ctx, objectKey, ttl) (string, error)` (15-min TTL per UI-SPEC Screen 5).
  </action>
  <verify>
    <automated>cd donnarec-api && go test -count=1 -run 'TestAllowedTypes|TestMagicByteRejectsSpoofed|TestSizeLimit' ./internal/storage/... -timeout 60s</automated>
  </verify>
  <done>magic-byte allowlist + size limit enforced and unit-tested without a live MinIO; minio-go v7.2.1 added.</done>
  <acceptance_criteria>
    - client.go contains `mimetype.Detect` and the allowlist map with image/jpeg, image/png, application/pdf
    - client.go enforces `10 << 20` size limit
    - go.mod contains `github.com/minio/minio-go/v7 v7.2.1`
    - the 3 named tests exit 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migration 000006 slip_attachments + config MinIO + docker-compose</name>
  <files>donnarec-api/migrations/000006_slip_attachments.up.sql, donnarec-api/migrations/000006_slip_attachments.down.sql, donnarec-api/internal/db/queries/slip.sql, donnarec-api/internal/db/generated/slip.sql.go, donnarec-api/internal/config/config.go, donnarec-api/docker-compose.yml</files>
  <read_first>
    - donnarec-api/migrations/000004_receipt_number_tables.up.sql (GRANT/REVOKE + header pattern)
    - donnarec-api/internal/config/config.go (env struct + validate analog)
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-RESEARCH.md §"MinIO docker-compose service addition" and §"Recommended Project Structure"
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-PATTERNS.md §"internal/config/config.go"
  </read_first>
  <action>
    Migration 000006: `CREATE TABLE slip_attachments` (id UUID PK, donation_id UUID NOT NULL REFERENCES donations(id), object_key TEXT NOT NULL, mime_type TEXT NOT NULL, size_bytes BIGINT NOT NULL, uploaded_by UUID NOT NULL REFERENCES users(id), uploaded_at TIMESTAMPTZ DEFAULT now(), deleted_at TIMESTAMPTZ NULL, deleted_by UUID NULL REFERENCES users(id)) — soft-delete via deleted_at (D-54, never hard-delete). Index on (donation_id) WHERE deleted_at IS NULL. GRANT SELECT,INSERT,UPDATE; REVOKE DELETE (file/reference retained for audit). Down drops table. `slip.sql` queries: `InsertSlip :one`, `GetActiveSlipByDonation :one` (WHERE donation_id=@id AND deleted_at IS NULL), `SoftDeleteSlip :exec` (SET deleted_at=now(), deleted_by=@by WHERE id=@id). Run sqlc generate. config.go: add `MinIOConfig{Endpoint, AccessKey, SecretKey, Bucket, Secure}` loaded from MINIO_* env (Bucket default "donnarec-slips", Secure via getEnvBool), add MINIO_ENDPOINT to validate() required map. docker-compose.yml: add the `minio` service from RESEARCH (image minio/minio, ports 9000/9001, MINIO_ROOT_USER/PASSWORD, volume, healthcheck) + MINIO_* env on the api service.
  </action>
  <verify>
    <automated>cd donnarec-api/internal/db && sqlc generate && cd ../../ && go build ./... && docker compose -f docker-compose.yml config >/dev/null</automated>
  </verify>
  <done>slip_attachments migration + queries generated; MinIOConfig loaded + validated; docker-compose minio service valid.</done>
  <acceptance_criteria>
    - 000006_slip_attachments.up.sql contains `REFERENCES donations(id)` and `deleted_at` and `REVOKE DELETE`
    - config.go contains `MinIOConfig` and reads MINIO_ENDPOINT
    - docker-compose.yml contains a `minio:` service with ports 9000
    - `cd donnarec-api && go build ./...` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Slip service + handlers (upload/view/remove) + routes</name>
  <files>donnarec-api/internal/donation/slip_service.go, donnarec-api/internal/donation/slip_handler.go, donnarec-api/cmd/server/main.go</files>
  <read_first>
    - donnarec-api/internal/users/handler.go (claims extraction, audit_after)
    - donnarec-api/internal/donation/handler.go (DonationHandler patterns from 03-03)
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-CONTEXT.md §"Slip retention" (D-54) and §"Slip upload" (D-48)
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-PATTERNS.md §"Pattern D" (AppendAuditEntryTx)
  </read_first>
  <action>
    `slip_service.go`: `SlipService{pool, queries *db.Queries, storage *storage.StorageClient, auditSvc, logger}` + constructor. `UploadSlip(ctx, donationID, file io.Reader, size, claims)` → `storage.PutSlip` → `qtx.InsertSlip` (within WithTx). `ViewSlip(ctx, donationID, claims)` → GetActiveSlipByDonation → `storage.PresignedGet(15m)`. `RemoveSlip(ctx, slipID, claims)` → WithTx: `qtx.SoftDeleteSlip` (set deleted_at, NOT a file delete — D-54) + `auditSvc.AppendAuditEntryTx(Action:"donation.slip.remove")`. `slip_handler.go`: `SlipHandler{svc *SlipService, logger}`; `UploadSlip` reads multipart form file (c.FormFile), enforces size via storage validation, maps storage.ErrUnsupportedFileType→422 + storage.ErrFileTooLarge→413, sets audit_after; `ViewSlip` returns {url}; `RemoveSlip` soft-deletes. No PII in logs (Pattern C). In `main.go` construct `storageClient, _ := storage.NewStorageClient(cfg.MinIO...)`, `slipSvc := donation.NewSlipService(...)`, `slipHandler := donation.NewSlipHandler(...)`, and register on the existing `/api/donations` group (staff): POST "/:id/slip" (UploadSlip), GET "/:id/slip" (ViewSlip), DELETE "/:id/slip/:slipId" (RemoveSlip).
  </action>
  <verify>
    <automated>cd donnarec-api && go build ./... && go test -count=1 ./internal/donation/... ./internal/storage/... -timeout 240s</automated>
  </verify>
  <done>upload validates by magic byte, stores in MinIO + DB reference; view returns presigned URL; remove soft-deletes + audits; routes wired.</done>
  <acceptance_criteria>
    - slip_handler.go contains `func (h *SlipHandler) UploadSlip(` and maps ErrUnsupportedFileType to 422
    - slip_service.go RemoveSlip calls `SoftDeleteSlip` and `AppendAuditEntryTx` (no hard delete)
    - main.go registers POST "/:id/slip" and GET "/:id/slip"
    - `cd donnarec-api && go build ./...` exits 0 and storage+donation tests pass
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client upload → server | untrusted file bytes cross here (malicious payload, spoofed type) |
| server → MinIO | object storage; presigned URLs grant time-bound read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Tampering | malicious/spoofed upload (script renamed .pdf) | mitigate | magic-byte validation via mimetype.Detect; allowlist jpg/png/pdf only (TestMagicByteRejectsSpoofed) |
| T-03-15 | Denial of Service | oversized upload exhausts memory/disk | mitigate | io.LimitReader 10 MB cap → ErrFileTooLarge (TestSizeLimit) |
| T-03-16 | Information Disclosure | long-lived/guessable slip URL | mitigate | PresignedGet 15-min TTL; objectKey contains uuid; not publicly listable |
| T-03-17 | Repudiation | silent slip deletion hides evidence | mitigate | soft-delete only (deleted_at); REVOKE DELETE; AppendAuditEntryTx on remove (D-54) |
| T-03-SC | Tampering | minio-go supply chain | mitigate | v7.2.1 official MinIO org, Approved in RESEARCH audit; verify go.sum checksum on `go get` |
</threat_model>

<verification>
- `cd donnarec-api && go build ./...` exits 0
- storage validation tests pass without a live MinIO
- `docker compose config` validates the minio service
</verification>

<success_criteria>
- Optional slip upload validated by magic byte + size; stored in MinIO with DB reference
- View returns short-lived presigned URL; remove soft-deletes + audits (no hard delete)
- MinIO config + docker-compose seam ready for Phase 6 reuse
</success_criteria>

<output>
Create `.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-04-SUMMARY.md` when done
</output>
