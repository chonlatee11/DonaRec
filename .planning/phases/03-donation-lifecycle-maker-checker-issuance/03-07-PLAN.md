---
phase: 03-donation-lifecycle-maker-checker-issuance
plan: 07
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - donnarec-web/app/donations/page.tsx
  - donnarec-web/app/donations/[id]/page.tsx
  - donnarec-web/components/DonationTable.tsx
  - donnarec-web/components/DonationFilterBar.tsx
  - donnarec-web/components/MaskedIdField.tsx
  - donnarec-web/components/ReviewReasonDialog.tsx
  - donnarec-web/components/SoDBlockedAlert.tsx
  - donnarec-web/lib/donations.ts
  - donnarec-web/messages/th.json
  - donnarec-web/messages/en.json
autonomous: true
requirements: [FR-10, FR-11, FR-12, FR-09]
must_haves:
  truths:
    - "Staff see a searchable donation list filtered by name, date range, status, and receipt number"
    - "The detail view shows status, receipt number (only for issued/cancelled), masked donor ID, slip link, and review history"
    - "A Checker can approve, return (with reason), or reject (with reason) a pending record from the detail view"
    - "When a Checker views their own pending record, the approve/return/reject controls are removed and an SoD alert is shown"
    - "D-53: search/filter scope is name, date range, status, and receipt number only — never searchable by tax/national ID"
  artifacts:
    - path: "donnarec-web/app/donations/page.tsx"
      provides: "list/search screen (UI-SPEC Screen 1)"
      contains: "DonationTable"
    - path: "donnarec-web/app/donations/[id]/page.tsx"
      provides: "detail + review action panel (UI-SPEC Screen 3)"
      contains: "ReviewReasonDialog"
    - path: "donnarec-web/components/MaskedIdField.tsx"
      provides: "masked-by-default ID display"
      contains: "x-xxxx-xxxxx"
  key_links:
    - from: "donnarec-web/lib/donations.ts"
      to: "GET /api/donations + /:id + /:id/approve|return|reject"
      via: "authenticated API client (lib/api.ts)"
      pattern: "/api/donations"
    - from: "app/donations/[id]/page.tsx"
      to: "SoDBlockedAlert"
      via: "render when viewer.id === record.created_by on pending_review"
      pattern: "created_by"
---

<objective>
Frontend slice: หน้า list/search (Screen 1) + detail/review (Screen 3) ตาม UI-SPEC ที่ LOCKED — รวม SoD blocked state, masked ID, review actions (approve/return/reject). เชื่อมกับ backend (03-03/03-05) ผ่าน authenticated API client.

Purpose: ทำให้ SC#1 (lifecycle visible), SC#2 (approve/return UI), FR-10 (search) ใช้งานจริงผ่าน UI. เป็น vertical slice ฝั่ง user-facing.
Output: `app/donations/page.tsx`, `app/donations/[id]/page.tsx` + DonationTable/FilterBar/MaskedIdField/ReviewReasonDialog/SoDBlockedAlert + th/en strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-UI-SPEC.md
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-CONTEXT.md

<interfaces>
<!-- Frontend foundation from 03-02 (AppShell, StatusBadge, lib/api.ts, next-intl). -->
lib/api.ts: authenticated fetch wrapper (Bearer); maps 403/409/422 to typed errors
components/StatusBadge.tsx: status → color/label (from 03-02)
Backend API (03-03/03-05): GET /api/donations?name=&status=&from=&to=&receipt_no=&page= ; GET /api/donations/:id ; POST /api/donations/:id/approve ; POST /api/donations/:id/return {reason} ; POST /api/donations/:id/reject {reason} ; GET /api/donations/:id/pii (03-06)
UI-SPEC Screen 1 (list/filter/table columns), Screen 3 (detail + action panel + SoD blocked state), Copywriting Contract (action labels, error copy), Color (status tokens, receipt-only-on-issued/cancelled)
Masked format: x-xxxx-xxxxx-1234 (font-mono 14px)
SoD rule: viewer is creator on a pending_review record → remove approve/return/reject, show amber SoD alert
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Donation list + search screen (Screen 1)</name>
  <files>donnarec-web/app/donations/page.tsx, donnarec-web/components/DonationTable.tsx, donnarec-web/components/DonationFilterBar.tsx, donnarec-web/lib/donations.ts</files>
  <read_first>
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-UI-SPEC.md §"Screen 1: Donation List / Search" and §"Copywriting Contract" (empty/error states) and §"Color" (status tokens)
    - donnarec-web/lib/api.ts (03-02 client) and donnarec-web/components/StatusBadge.tsx
  </read_first>
  <action>
    `lib/donations.ts`: typed API functions `searchDonations(filter)`, `getDonation(id)`, `approve(id)`, `returnForEdit(id, reason)`, `reject(id, reason)`, `revealPII(id)` calling lib/api.ts. `DonationFilterBar.tsx`: horizontal filter bar (name Input, date-range two Calendar Popover, status Select with all 5 + "ทั้งหมด", receipt number Input, "ค้นหา" accent + "ล้างการกรอง" ghost) per Screen 1. `DonationTable.tsx`: shadcn Table with columns วันที่บริจาค/ชื่อผู้บริจาค/จำนวนเงิน(right-aligned comma)/สถานะ(StatusBadge)/เลขที่ใบเสร็จ(only issued/cancelled else em-dash; cancelled → slate-500 strikethrough + "(ยกเลิก)")/ผู้สร้าง/จัดการ; 56px row min; pagination 20/page (shadcn Pagination). `page.tsx`: compose FilterBar + Table + "สร้างรายการบริจาค" CTA (accent, Plus icon, Maker/Admin only), empty-state + no-results copy from Copywriting Contract. Row routing per Screen 1 row rules (Maker own draft → edit; else → detail). Use Intl.NumberFormat('th-TH') for amount, BE date for Thai locale.
  </action>
  <verify>
    <automated>cd donnarec-web && npm run build && npx tsc --noEmit</automated>
  </verify>
  <done>list renders filter bar + table with correct columns, status badges, receipt-only-on-issued rule, pagination, empty states; build + tsc clean.</done>
  <acceptance_criteria>
    - DonationTable renders receipt number only for issued/cancelled (em-dash otherwise) with cancelled strikethrough
    - DonationFilterBar exposes name, date range, status, receipt number filters (no tax-ID field — D-53)
    - lib/donations.ts contains searchDonations/getDonation/approve/returnForEdit/reject
    - `cd donnarec-web && npm run build && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Detail view + review action panel + SoD blocked state (Screen 3)</name>
  <files>donnarec-web/app/donations/[id]/page.tsx, donnarec-web/components/ReviewReasonDialog.tsx, donnarec-web/components/SoDBlockedAlert.tsx</files>
  <read_first>
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-UI-SPEC.md §"Screen 3" (two-column, action-per-role table, SoD Blocked State) and §"Destructive Confirmation Dialogs" (ตีกลับแก้ไข Dialog, ปฏิเสธถาวร AlertDialog)
  </read_first>
  <action>
    `[id]/page.tsx`: two-column detail (left 60% data panel: large StatusBadge, receipt-number block visible only when issued/cancelled with cancelled "(ยกเลิกแล้ว)", donor definition list, MaskedIdField, slip "ดูสลิป" link or "ไม่มีสลิปแนบ", consent row, review-history accordion, replaces/replaced_by chain links per Screen 3; right 40% action panel). Action panel renders per the Screen 3 role×status table: Checker on pending_review (not own) → "อนุมัติ"(accent), "ตีกลับแก้ไข"(outline), "ปฏิเสธถาวร"(destructive). `ReviewReasonDialog.tsx`: reusable — Dialog (reversible, for return) and AlertDialog (terminal, for reject) variants with a MANDATORY reason textarea (block confirm until non-empty; error copy "กรุณาระบุเหตุผลก่อนดำเนินการ"). `SoDBlockedAlert.tsx`: amber-50 Alert with AlertTriangle + the locked SoD message; when viewer.id === record.created_by on pending_review, render this and DO NOT render approve/return/reject (removed from DOM, not disabled — Screen 3 SoD Blocked State). Map API 403→sod alert/toast, 409→status conflict toast (Copywriting error copy).
  </action>
  <verify>
    <automated>cd donnarec-web && npm run build && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>detail shows receipt only on issued/cancelled, masked ID, review history, replace chain; review actions work with mandatory reason; SoD blocked state removes controls + shows alert; build/tsc/lint clean.</done>
  <acceptance_criteria>
    - [id]/page.tsx renders receipt-number block only when status is issued or cancelled
    - SoDBlockedAlert is shown and approve/return/reject are absent from DOM when viewer is the creator on a pending_review record
    - ReviewReasonDialog blocks confirm until reason is non-empty for both return and reject
    - `cd donnarec-web && npm run build && npx tsc --noEmit && npm run lint` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: MaskedIdField + i18n strings (th/en)</name>
  <files>donnarec-web/components/MaskedIdField.tsx, donnarec-web/messages/th.json, donnarec-web/messages/en.json</files>
  <read_first>
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-UI-SPEC.md §"Screen 4: PII Reveal Interaction" (reveal flow — full interaction lands in 03-08; here render masked + reveal button placeholder) and §"i18n Contract" and §"Accessibility Contract"
  </read_first>
  <action>
    `MaskedIdField.tsx`: display the masked value `x-xxxx-xxxxx-1234` in font-mono 14px with `aria-label="เลขประจำตัว (ซ่อน)"`; for Checker/Admin show a "เปิดเผยข้อมูล" button (secondary/blue) whose full reveal AlertDialog flow is implemented in 03-08 (here expose an `onReveal` prop + render the masked state + button). Add all list/detail/review i18n keys to `messages/th.json` (primary) and `messages/en.json` per UI-SPEC Copywriting Contract + status EN labels: action labels, status names, empty-state copy, error copy (errors.sod, errors.statusConflict, errors.reasonRequired). Ensure status badge EN labels match the Color section table.
  </action>
  <verify>
    <automated>cd donnarec-web && npm run build && npx tsc --noEmit</automated>
  </verify>
  <done>MaskedIdField renders masked-by-default with reveal affordance + aria-label; th/en catalogs complete for list/detail/review; build + tsc clean.</done>
  <acceptance_criteria>
    - MaskedIdField renders the masked format and an aria-label, exposing an onReveal prop
    - messages/th.json and messages/en.json both contain keys for sod, statusConflict, reasonRequired errors
    - `cd donnarec-web && npm run build && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser UI → Go API | all data fetched via bearer-authenticated client; server is the authority |
| client-side role logic | role-based UI hiding is UX only; server enforces SoD/RBAC/reveal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-31 | Elevation of Privilege | client hides SoD controls but server is the authority | mitigate | SoD/RBAC enforced server-side (03-05); UI removal is defense-in-UX only; 403 mapped to alert |
| T-03-32 | Information Disclosure | donor ID shown unmasked by default | mitigate | MaskedIdField masked by default; plaintext only via audited reveal (03-08 flow → server gate 03-06) |
| T-03-33 | Tampering | stale state lets a second approver act | mitigate | 409 status_conflict surfaced as "reload" toast (Copywriting); server precondition is authoritative |
</threat_model>

<verification>
- `cd donnarec-web && npm run build && npx tsc --noEmit && npm run lint` exits 0
- list applies name/date/status/receipt filters; receipt shown only for issued/cancelled
- SoD blocked state removes controls when viewer is creator
</verification>

<success_criteria>
- Searchable donation list (name/date/status/receipt) per locked Screen 1
- Detail + review (approve/return/reject with mandatory reason) per locked Screen 3, with SoD blocked state
- Masked ID by default; th/en catalogs complete
</success_criteria>

<output>
Create `.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-07-SUMMARY.md` when done
</output>
