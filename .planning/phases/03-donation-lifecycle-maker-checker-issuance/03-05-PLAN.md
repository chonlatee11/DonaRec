---
phase: 03-donation-lifecycle-maker-checker-issuance
plan: 05
type: tdd
wave: 4
depends_on: ["03-04"]
files_modified:
  - donnarec-api/internal/donation/service.go
  - donnarec-api/internal/donation/handler.go
  - donnarec-api/internal/donation/service_test.go
  - donnarec-api/internal/donation/service_integration_test.go
  - donnarec-api/cmd/server/main.go
autonomous: true
requirements: [FR-12, FR-14, FR-11, NFR-04, NFR-05]
must_haves:
  truths:
    - "On approval a single DB transaction sets status=issued, allocates the gap-less number, writes the audit row, and enqueues the outbox job — all-or-nothing"
    - "A Checker cannot approve a record they created (SoD enforced in code AND by DB CHECK)"
    - "Concurrent approvals of the same record issue exactly one receipt number"
    - "Return and Reject each require a mandatory reason; a receipt number exists only for issued records"
    - "D-45: the Checker has two distinct non-approve actions — return-for-edit and reject — each requiring a mandatory reason"
    - "D-52: double issuance is prevented by row-lock plus a status precondition during approve (SELECT … FOR UPDATE + status guard)"
  artifacts:
    - path: "donnarec-api/internal/donation/service.go"
      provides: "Approve (issuance tx), Return, Reject"
      contains: "func (s *DonationService) Approve"
    - path: "donnarec-api/internal/donation/service_integration_test.go"
      provides: "issuance atomicity + SoD + concurrency tests"
      contains: "TestConcurrentApproval_ExactlyOneSucceeds"
  key_links:
    - from: "internal/donation/service.go Approve"
      to: "internal/receiptno.Allocator.Allocate"
      via: "called with the WithTx closure's pgx.Tx (D-33/D-35)"
      pattern: "allocator\\.Allocate\\(ctx, tx"
    - from: "internal/donation/service.go Approve"
      to: "outbox_jobs"
      via: "EnqueueOutboxJob in the same tx"
      pattern: "EnqueueOutboxJob"
    - from: "internal/donation/service.go Approve"
      to: "audit_log"
      via: "AppendAuditEntryTx in the same tx (NFR-05)"
      pattern: "AppendAuditEntryTx"
---

<objective>
★ Load-bearing slice: maker-checker review + the single atomic issuance transaction. ตอน approve: lock donation row (D-52) → precondition pending_review → SoD approver≠creator → allocate gap-less number (Phase 2, D-33) → set status=issued → write audit (NFR-05) → enqueue outbox (Phase 4 consumes). All-or-nothing. นี่คือ invariant ที่เสี่ยงสุดของโปรเจกต์.

Purpose: ส่งมอบ SC#2 (approve/return + SoD) และ SC#3 (atomic issuance, number only on issued). TDD บังคับพร้อม concurrency test (N parallel approvals → exactly one receipt).
Output: `DonationService.Approve/Return/Reject` + checker route group; tests INV-1 (atomic), INV-2 (SoD), INV-3 (concurrency), INV-7 (mandatory reason) เขียว.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-CONTEXT.md
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-PATTERNS.md
@.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-RESEARCH.md

<interfaces>
<!-- Reuse Phase 1/2 — Allocate MUST receive the closure's tx, never pool (D-33/D-35). -->
internal/receiptno: (a *Allocator) Allocate(ctx, tx pgx.Tx, issueDate time.Time) (AllocatedReceipt, error)
  AllocatedReceipt{ ID int64; FiscalYear int; RunningNo int; Formatted string; AllocatedAt time.Time }  // ID = receipt_numbers ledger PK → donations.receipt_number_id FK (D-38)
internal/db: WithTx(ctx, pool, fn func(pgx.Tx) error) error; (q *Queries).WithTx(tx) *Queries
internal/audit: (s).AppendAuditEntryTx(ctx, tx, AuditEntry{ActorID, ActorEmail, Action, Resource, AfterJSON})
internal/auth: KeycloakClaims{Subject}; ActorIdentity()
sqlc (03-01): LockDonationForUpdate (FOR UPDATE), IssueDonation (WHERE status='pending_review'), ReturnDonation, RejectDonation; outbox: EnqueueOutboxJob
sentinels (03-03 errors.go): ErrInvalidTransition, ErrSoDViolation, ErrMissingReason
canTransition helper (03-03 service.go) — extend for approve/return/reject arms
Issuance exact sequence: 03-PATTERNS §"Issuance Transaction — Exact Sequence" (7 steps)
DB backstop: chk_sod_approver (000005) — SoD defense-in-depth
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Issuance transaction (Approve) — atomicity + SoD (RED→GREEN→REFACTOR)</name>
  <files>donnarec-api/internal/donation/service.go, donnarec-api/internal/donation/service_integration_test.go</files>
  <read_first>
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-PATTERNS.md §"Issuance Transaction — Exact Sequence" and §"Allocator — Never Call Outside Issuance tx" and §"Pattern B/D"
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-RESEARCH.md §"Pattern 1: Issuance Transaction" + INV-1 + INV-2 + Pitfalls 1-5
    - donnarec-api/internal/receiptno/allocator.go (Allocate signature + tx contract)
    - donnarec-api/internal/audit/service.go (AppendAuditEntryTx)
  </read_first>
  <behavior>
    - TestIssuanceTransaction_RollbackOnError: inject error after Allocate (before IssueDonation) → receipt_numbers ledger 0 rows + donation still pending_review; inject error after IssueDonation (before audit) → status still pending_review + outbox 0 rows; happy path → status=issued + receipt_number_id set + 1 audit row + 1 outbox row
    - TestOutboxAtomicity: an outbox 'issue_receipt' job exists IFF the receipt was issued
    - TestSoD_ApproverCannotBeCreator: approve with creator's own claims → ErrSoDViolation (no number allocated)
    - TestSoD_DBCheckConstraint: superuser direct INSERT with approved_by = created_by → pgerrcode.CheckViolation (DB backstop fires)
  </behavior>
  <action>
    เขียน tests RED ก่อน (เปลี่ยนจาก skip ใน 03-01). Implement `Approve(ctx, donationID, claims)` ตาม 03-PATTERNS exact 7-step sequence inside ONE `dbhelpers.WithTx` closure: (1) `qtx.LockDonationForUpdate(ctx, donationID)` — D-52; (2) precondition `donation.Status == pending_review` else `ErrInvalidTransition`; (3) SoD `donation.CreatedBy != approverID` else `ErrSoDViolation` (parse approverID from claims.Subject); (4) `s.allocator.Allocate(ctx, tx, time.Now())` — pass the closure's `tx` NOT pool (D-33/D-35; this is the ONLY call site of Allocate); (5) `qtx.IssueDonation(...)` stamping status=issued + approved_by/at + receipt_number_id = allocated.ID (the receipt_numbers ledger PK, D-38 FK) + receipt_formatted = allocated.Formatted (frozen, D-42); (6) `s.auditSvc.AppendAuditEntryTx(ctx, tx, Action:"donation.approve", AfterJSON receipt formatted)` — in-tx, NOT best-effort (Pitfall 4); (7) `qtx.EnqueueOutboxJob(JobType:"issue_receipt", Payload:{donation_id})`. Any error → WithTx rolls back all 7. Never render PDF/email here (enqueue only). For the error-injection test, structure the closure so a test seam (e.g. an injectable hook or a deliberately failing step via a test-only branch) can force failure after a given step; prefer a small unexported `issueTx` function taking the steps so tests assert partial-state-free rollback. Pattern C logging.
  </action>
  <verify>
    <automated>cd donnarec-api && go test -count=1 -run 'TestIssuanceTransaction_RollbackOnError|TestOutboxAtomicity|TestSoD_ApproverCannotBeCreator|TestSoD_DBCheckConstraint' ./internal/donation/... -timeout 240s</automated>
  </verify>
  <done>issuance is all-or-nothing across all 7 effects; SoD blocked in code and DB; Allocate called only inside this tx with the closure tx.</done>
  <acceptance_criteria>
    - service.go Approve calls `s.allocator.Allocate(ctx, tx,` (closure tx, not pool)
    - service.go Approve calls `AppendAuditEntryTx(ctx, tx,` and `EnqueueOutboxJob(` inside the same WithTx
    - IssueDonation sets receipt_number_id = allocated.ID (the receipt_numbers ledger PK), not a re-queried or pre-computed value
    - on error after Allocate, no receipt_numbers row persists and status stays pending_review (asserted by TestIssuanceTransaction_RollbackOnError)
    - TestSoD_DBCheckConstraint asserts a DB CheckViolation on approved_by==created_by
    - the 4 named tests exit 0
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Return/Reject mandatory reason + concurrency proof (RED→GREEN)</name>
  <files>donnarec-api/internal/donation/service.go, donnarec-api/internal/donation/service_test.go, donnarec-api/internal/donation/service_integration_test.go</files>
  <read_first>
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-CONTEXT.md §"Approval / rejection workflow" (D-45 return vs reject)
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-RESEARCH.md INV-3 (concurrency) + INV-7 (mandatory reason)
    - donnarec-api/internal/receiptno/allocator_concurrency_test.go (errgroup parallel pattern + DB-level count assertion)
  </read_first>
  <behavior>
    - TestMandatoryReason: Return with empty reason → ErrMissingReason; Reject with empty reason → ErrMissingReason
    - TestReturnToDraft: Return on pending_review → status=draft, review_reason persisted, reviewed_by/at set (non-terminal, D-45)
    - TestRejectTerminal: Reject on pending_review → status=rejected (terminal), reason persisted; rejected→submit/approve → ErrInvalidTransition
    - TestConcurrentApproval_ExactlyOneSucceeds: N=5 goroutines Approve same pending record → exactly 1 nil, 4 ErrInvalidTransition; receipt_numbers has exactly 1 row for that donation; status=issued
  </behavior>
  <action>
    เขียน tests RED ก่อน. Implement `Return(ctx, id, reason, claims)`: require non-empty reason (ErrMissingReason) → `qtx.ReturnDonation` (pending_review→draft, reviewed_by/at/review_reason) + `AppendAuditEntryTx(Action:"donation.return")`; 0 rows affected → ErrInvalidTransition. Implement `Reject(ctx, id, reason, claims)`: require reason → `qtx.RejectDonation` (pending_review→rejected) + audit "donation.reject"; 0 rows → ErrInvalidTransition. Extend `canTransition` for return/reject/approve arms. Write `TestConcurrentApproval_ExactlyOneSucceeds` using the errgroup pattern from allocator_concurrency_test.go: assert exactly one Approve returns nil and the rest return `ErrInvalidTransition` (not an internal error — the FOR UPDATE lock + precondition serializes them), and a DB-level count proves exactly one receipt_numbers row for that fiscal year/donation. Run this test under `-race`.
  </action>
  <verify>
    <automated>cd donnarec-api && go test -count=1 -race -run 'TestMandatoryReason|TestReturnToDraft|TestRejectTerminal|TestConcurrentApproval_ExactlyOneSucceeds' ./internal/donation/... -timeout 300s</automated>
    <quick-check>cd donnarec-api && go test -short ./internal/donation/... -timeout 30s  # per-commit sample (skips testcontainer/-race integration); the full -race run above is the per-wave gate</quick-check>
  </verify>
  <done>return loops to draft, reject is terminal, both require reason; concurrent approvals issue exactly one receipt under -race.</done>
  <acceptance_criteria>
    - service.go contains `func (s *DonationService) Return(` and `func (s *DonationService) Reject(` both rejecting empty reason with ErrMissingReason
    - TestConcurrentApproval_ExactlyOneSucceeds asserts exactly 1 success and a single receipt_numbers row
    - the 4 named tests pass under `-race`
    - a per-commit `go test -short ./internal/donation/...` quick-check (≤30s) runs alongside the full -race/testcontainer per-wave gate
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Checker handlers + route group + error mapping</name>
  <files>donnarec-api/internal/donation/handler.go, donnarec-api/cmd/server/main.go</files>
  <read_first>
    - donnarec-api/internal/donation/handler.go (03-03 handler patterns + sentinel mapping)
    - .planning/phases/03-donation-lifecycle-maker-checker-issuance/03-PATTERNS.md §"SoD error mapping" and §"cmd/server/main.go" (checkerGroup) and §"Pattern E"
  </read_first>
  <action>
    `handler.go`: add `Approve`, `Return`, `Reject` handlers. Each extracts claims (Pattern A), binds request (Return/Reject have `Reason string validate:"required"`), calls the service, and maps sentinels: `ErrInvalidTransition`→409 {"error":"status_conflict"}, `ErrSoDViolation`→403 {"error":"sod_violation"}, `ErrMissingReason`→422 {"error":"reason_required"}, default→500 with `zap.String("donation_id", id)` only (Pattern C). Set `c.Set("audit_after", resp)`. In `main.go` register a second `/api/donations` group guarded by `auth.RequireRoles(auth.RoleChecker, auth.RoleAdmin)` with POST "/:id/approve", POST "/:id/return", POST "/:id/reject" (PATTERNS §main.go checkerGroup). Note: RequireRoles gates role; SoD is the separate per-record check in Approve (both required, defense-in-depth).
  </action>
  <verify>
    <automated>cd donnarec-api && go build ./... && go test -count=1 ./internal/donation/... -timeout 300s</automated>
  </verify>
  <done>checker actions wired under RBAC; SoD→403, conflict→409, missing reason→422; full donation package green.</done>
  <acceptance_criteria>
    - handler.go maps ErrSoDViolation to http.StatusForbidden and ErrInvalidTransition to http.StatusConflict
    - main.go registers POST "/:id/approve" under `auth.RequireRoles(auth.RoleChecker, auth.RoleAdmin)`
    - `cd donnarec-api && go build ./...` exits 0 and donation tests pass
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checker → /approve | privileged state transition that mints a legal receipt number |
| concurrent approvers → same row | race that could double-issue |
| service → DB constraints | DB CHECK is the last line for SoD + receipt-on-issued |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | Elevation of Privilege | Maker approves own record (SoD bypass) | mitigate | code guard approver≠created_by (ErrSoDViolation) + chk_sod_approver DB CHECK (TestSoD_* both layers) |
| T-03-19 | Tampering | double-issuance under concurrency | mitigate | LockDonationForUpdate FOR UPDATE (D-52) + precondition status='pending_review'; TestConcurrentApproval proves exactly-one under -race |
| T-03-20 | Tampering | partial issuance (number without audit/outbox or vice versa) | mitigate | all 7 effects in one WithTx; any error rolls back all (TestIssuanceTransaction_RollbackOnError) |
| T-03-21 | Repudiation | issuance without audit trail | mitigate | AppendAuditEntryTx inside the same tx (NFR-05); not best-effort (Pitfall 4) |
| T-03-22 | Tampering | receipt number on non-issued / pre-computed on draft | mitigate | Allocate only inside issuance tx (sole call site, D-35) + chk_receipt_only_on_issued_or_cancelled |
| T-03-23 | Elevation of Privilege | non-checker hits approve | mitigate | RequireRoles(checker/admin) on the checker route group |
</threat_model>

<verification>
- `cd donnarec-api && go test -count=1 -race ./internal/donation/... -timeout 400s` passes (incl concurrency)
- error-injection proves no partial issuance state
- DB CheckViolation proven for SoD direct-insert
</verification>

<success_criteria>
- Approval issues a numbered receipt in one atomic transaction (status+number+audit+outbox)
- SoD enforced in code AND DB; concurrent approvals issue exactly one receipt
- Return (loop to draft) and Reject (terminal) require a mandatory reason
</success_criteria>

<output>
Create `.planning/phases/03-donation-lifecycle-maker-checker-issuance/03-05-SUMMARY.md` when done
</output>
