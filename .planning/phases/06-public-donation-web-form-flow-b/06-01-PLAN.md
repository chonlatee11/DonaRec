---
phase: 06-public-donation-web-form-flow-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - donnarec-api/migrations/000015_donation_source.up.sql
  - donnarec-api/migrations/000015_donation_source.down.sql
  - donnarec-api/migrations/000016_seed_public_web_user.up.sql
  - donnarec-api/migrations/000016_seed_public_web_user.down.sql
  - donnarec-api/internal/db/queries/donations.sql
  - donnarec-api/internal/donation/service.go
  - donnarec-api/internal/donation/handler.go
autonomous: true
requirements: [FR-01, FR-08]
must_haves:
  truths:
    - "donations rows carry a source value of 'flow_a' or 'flow_b'; all pre-existing rows read back 'flow_a'"
    - "a fixed-UUID 'public-web' system user exists in users and can be an audit actor without a parseUUID error"
    - "GET /api/donations?source=flow_b returns only web-sourced records; ?source=flow_a excludes them"
  artifacts:
    - donnarec-api/migrations/000015_donation_source.up.sql
    - donnarec-api/migrations/000016_seed_public_web_user.up.sql
    - donnarec-api/internal/db/queries/donations.sql
  key_links:
    - "SearchDonations/CountDonations source narg filter <-> ListFilter.Source <-> handler ?source= query param"
    - "seeded public-web users.id (fixed UUID) <-> future CreatePublicSubmission created_by FK (plan 03)"
---

<objective>
Lay the Flow B data foundation: a `source` column that explicitly separates web submissions from staff-entered records (D-77), a seeded `public-web` system user with a UUID-shaped identity (D-76, Pitfall 1), and a `source` filter threaded through the existing donation search/list contract so the staff queue (plan 07) can query Flow B records.

Purpose: Everything downstream (the public create path in plan 03, the queue screen in plan 07) depends on these two migrations and the source filter existing first.
Output: Migrations 000015/000016, extended `SearchDonations`/`CountDonations` queries, `ListFilter.Source`, and a `?source=` handler param — all regenerated through sqlc and green under a source-filter test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-public-donation-web-form-flow-b/06-CONTEXT.md
@.planning/phases/06-public-donation-web-form-flow-b/06-RESEARCH.md
@.planning/phases/06-public-donation-web-form-flow-b/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrations 000015 (source column + backfill) and 000016 (seed public-web system user)</name>
  <files>donnarec-api/migrations/000015_donation_source.up.sql, donnarec-api/migrations/000015_donation_source.down.sql, donnarec-api/migrations/000016_seed_public_web_user.up.sql, donnarec-api/migrations/000016_seed_public_web_user.down.sql</files>
  <read_first>
    - donnarec-api/migrations/000013_edonation_keyed_metadata.up.sql (ADD COLUMN + backfill precedent — Pitfall 2)
    - donnarec-api/migrations/000014_edonation_config.up.sql (seed-in-migration precedent)
    - donnarec-api/migrations/000001_*.up.sql or the migration that creates the `users` table (exact column list + NOT NULL constraints for the INSERT)
    - donnarec-api/migrations/000005_*.up.sql or wherever `donations` is created (confirm table name + that created_by REFERENCES users(id) NOT NULL)
  </read_first>
  <action>
    000015 up: add a `source` column to `donations` as `TEXT NOT NULL DEFAULT 'flow_a'` with a `CHECK (source IN ('flow_a','flow_b'))` constraint. The DEFAULT backfills every existing row to 'flow_a' automatically (D-77). Choose TEXT+CHECK over an enum type (Claude's discretion — simpler migration, matches the `sqlc.narg('source')::TEXT` filter shape in plan Task 2). 000015 down: drop the column.
    000016 up: `INSERT INTO users` one system-user row with a FIXED literal UUID reused for BOTH `id` and `keycloak_subject` — use `00000000-0000-4000-8000-000000000006` (a valid UUID literal; MUST NOT be a human-readable sentinel like the word public-web, because internal/audit/service.go runs parseUUID on the audit ActorID and rolls the whole transaction back on a non-UUID — Pitfall 1). Set display_name to a readable label, assign the least-privileged role the users schema requires, and fill every other NOT NULL column per the schema read above. Make the INSERT idempotent (ON CONFLICT (id) DO NOTHING). 000016 down: delete that user row by its fixed UUID.
    Record the fixed UUID as a comment in 000016 up so plan 03 can mirror it as a Go constant.
  </action>
  <verify>
    <automated>cd donnarec-api && rtk go build ./... && rtk go test ./internal/testutil/... -run TestStartPostgres -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `migrate up` applies 000015 and 000016 with no error (testcontainers Postgres boot runs full migration chain green)
    - donations has a `source` column, NOT NULL, default 'flow_a', with a CHECK limiting values to flow_a/flow_b
    - users contains a row whose id and keycloak_subject both equal 00000000-0000-4000-8000-000000000006
    - down migrations for both cleanly reverse the up
  </acceptance_criteria>
  <done>Both migrations apply and reverse; the fixed-UUID public-web user exists; source defaults flow_a on existing rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: source filter on SearchDonations/CountDonations + ListFilter.Source + ?source= handler param (regen sqlc)</name>
  <files>donnarec-api/internal/db/queries/donations.sql, donnarec-api/internal/donation/service.go, donnarec-api/internal/donation/handler.go</files>
  <read_first>
    - donnarec-api/internal/db/queries/donations.sql (SearchDonations line ~226, CountDonations line ~260, GetDonationByID line ~66 — existing sqlc.narg chain and physical SELECT column order)
    - donnarec-api/internal/donation/service.go (List line ~451, Search line ~1341, the ListFilter struct definition)
    - donnarec-api/internal/donation/handler.go (the list/search handler that parses query params into ListFilter)
    - donnarec-api/sqlc.yaml (regen config)
    - .planning/phases/06-public-donation-web-form-flow-b/06-RESEARCH.md ("Extending SearchDonations/CountDonations" example + Pitfall 2)
  </read_first>
  <behavior>
    - Test: SearchDonations with source='flow_b' returns only rows inserted with source flow_b; a flow_a row is absent from that result set and vice versa
    - Test: SearchDonations with source unset (nil narg) returns both flow_a and flow_b rows (filter is skipped when nil, matching the existing donor_name/status narg semantics, D-53)
    - Test: CountDonations mirrors the same source predicate so the pagination total matches the filtered item count
  </behavior>
  <action>
    Write the failing test first in donnarec-api/internal/donation/search_source_test.go (TestSearchDonations_SourceFilter) exercising the behaviors above against a testcontainers Postgres, seeding one flow_a and one flow_b donation.
    Then add one AND clause to BOTH SearchDonations and CountDonations mirroring the existing narg pattern: source is nullable and skipped when nil, otherwise equals d.source (cast the narg to TEXT as the existing clauses do). Add `source` to GetDonationByID's explicit SELECT column list in the correct physical position (last, matching the ALTER TABLE ADD COLUMN order) so sqlc keeps reusing the db.Donation model — this is the exact 000013 fragility documented in STATE.md (Pitfall 2). Run sqlc generate. Add a `Source *string` (or equivalent nullable) field to ListFilter and map it into the sqlc params in both List and Search. Parse `?source=` from the query string in the list handler and pass it into ListFilter, rejecting any value that is not flow_a/flow_b with a 400.
  </action>
  <verify>
    <automated>cd donnarec-api && rtk go build ./... && rtk go test ./internal/donation/... -run TestSearchDonations_SourceFilter -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` is green after `sqlc generate` (no db.Donation field-count/type mismatch — Pitfall 2 avoided)
    - TestSearchDonations_SourceFilter passes: flow_b filter excludes flow_a rows, nil filter returns both, CountDonations total equals the filtered set size
    - the list handler rejects `?source=bogus` with HTTP 400 and accepts flow_a/flow_b
  </acceptance_criteria>
  <done>Source filter works end to end through query, service filter, and handler param; sqlc regenerated cleanly; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration DDL -> production data | ADD COLUMN + seed INSERT run against existing donor data |
| staff query param -> DB filter | `?source=` reaches a sqlc predicate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | 000015 backfill | medium | mitigate | Column added NOT NULL with DEFAULT 'flow_a'; CHECK constraint prevents any out-of-domain value at the DB layer |
| T-06-02 | Repudiation | seeded public-web actor | high | mitigate | Fixed-UUID keycloak_subject so every public-submit audit row (plan 03) has a parseable, well-known actor — prevents the audit-rollback failure of Pitfall 1 |
| T-06-03 | Elevation of Privilege | public-web user role | high | mitigate | Seed the user with the least-privileged role; it is a created_by FK target only, never used to authenticate a login (no Keycloak credential issued) |
| T-06-04 | Injection | `?source=` param | medium | mitigate | Handler allow-lists flow_a/flow_b; sqlc parameterizes the narg — no string concatenation into SQL |
| T-06-SC | Tampering | npm/go installs | low | accept | This plan adds no new package; go.mod unchanged |
</threat_model>

<verification>
- `rtk go build ./...` green after sqlc regen
- `rtk go test ./internal/donation/... -run TestSearchDonations_SourceFilter -count=1` passes
- migration chain applies + reverses under testcontainers Postgres
</verification>

<success_criteria>
donations.source exists (NOT NULL, CHECK, default flow_a); public-web fixed-UUID user seeded; source filter threaded through query/service/handler and proven by test.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 01)
- SQL column: `donations.source` (TEXT, CHECK flow_a|flow_b)
- Migrations: `000015_donation_source`, `000016_seed_public_web_user`
- Go constant target: fixed UUID `00000000-0000-4000-8000-000000000006` (public-web user id + keycloak_subject)
- sqlc query params: `SearchDonations`/`CountDonations` `source` narg; `GetDonationByID` SELECT gains `source`
- Struct field: `ListFilter.Source`
- Handler param: `?source=` on the donation list endpoint
- Test: `TestSearchDonations_SourceFilter`
</artifacts_produced>

<output>
Create `.planning/phases/06-public-donation-web-form-flow-b/06-01-SUMMARY.md` when done
</output>
