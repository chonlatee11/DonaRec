---
phase: 06-public-donation-web-form-flow-b
plan: 07
type: execute
wave: 2
depends_on: ["06-05", "06-01"]
files_modified:
  - donnarec-web/components/SourceBadge.tsx
  - donnarec-web/app/api/bff/queue/route.ts
  - donnarec-web/components/QueueTable.tsx
  - donnarec-web/components/QueueSourceFilter.tsx
  - donnarec-web/app/(app)/queue/page.tsx
  - donnarec-web/components/DonationTable.tsx
  - donnarec-web/components/DonationDetailView.tsx
  - donnarec-web/messages/th.json
  - donnarec-web/messages/en.json
autonomous: true
requirements: [FR-08]
must_haves:
  truths:
    - "staff open /queue and see a pending-review list of both Flow A and Flow B submissions, with a source badge on each row"
    - "a source filter (all / from-website / staff-entered) narrows the queue by source; a Flow A record never appears under the web-only filter and vice versa"
    - "opening a Flow B record shows a source-aware creator label (submitted by donor via website), not the raw public-web system user name"
  artifacts:
    - donnarec-web/app/(app)/queue/page.tsx
    - donnarec-web/components/SourceBadge.tsx
    - donnarec-web/components/QueueSourceFilter.tsx
  key_links:
    - "QueueSourceFilter chip <-> queue BFF ?source= <-> Go /api/donations?status=pending_review&source= (plan 01 filter)"
    - "donations.source value <-> SourceBadge mapping <-> DonationDetailView creator-label branch"
---

<objective>
Give staff the FR-08 pending-review queue that surfaces web submissions separated from staff-entered records (Screen 11, slate/blue `(app)`), implementing the currently-dead `/queue` nav link. Adds a source badge + a 3-chip source filter backed by plan 01's `source` query filter, plus the Screen 1 source column and Screen 3 source-aware creator-label amendments.

Purpose: Flow B records must be visible and distinguishable in the back-office to flow into the existing approve/issue pipeline.
Output: SourceBadge, a queue BFF route, QueueTable + QueueSourceFilter, the `/queue` page, DonationTable + DonationDetailView amendments, and `queue.*`/`source.*` i18n.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-public-donation-web-form-flow-b/06-CONTEXT.md
@.planning/phases/06-public-donation-web-form-flow-b/06-UI-SPEC.md
@.planning/phases/06-public-donation-web-form-flow-b/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SourceBadge + queue BFF route + queue/source i18n</name>
  <files>donnarec-web/components/SourceBadge.tsx, donnarec-web/app/api/bff/queue/route.ts, donnarec-web/messages/th.json, donnarec-web/messages/en.json</files>
  <read_first>
    - donnarec-web/components/StatusBadge.tsx (shadcn Badge wrapper pattern to mirror)
    - donnarec-web/app/api/bff/donations/route.ts (the session-bound BFF list proxy: bffForward + query-param forwarding to Go /api/donations)
    - donnarec-web/lib/bff.ts (bffForward/getBffToken to reuse — queue IS authenticated, unlike the public route)
    - .planning/phases/06-public-donation-web-form-flow-b/06-UI-SPEC.md (Source badge tokens; Queue empty-state copy; i18n Contract source.*/queue.* namespaces)
  </read_first>
  <action>
    SourceBadge.tsx: wrap shadcn Badge mapping source 'flow_b' -> blue-50/blue-700 with a Globe icon and label from source.flow_b, and 'flow_a' -> slate-100/slate-600 with a UserCog icon and label from source.flow_a. Add aria-label with the full Thai source text.
    app/api/bff/queue/route.ts: an authenticated BFF route (reuse bffForward/getBffToken — this is (app), staff-only) that forwards to Go GET /api/donations with status pinned to pending_review and passes through the ?source= param (all -> omit source, from-website -> flow_b, staff-entered -> flow_a) into plan 01's source filter.
    i18n: add source.flow_a / source.flow_b label pairs and the queue.* namespace (page heading, three source-filter chip labels, both empty-state heading/body pairs) to th.json + en.json per the Copywriting Contract; the nav.queue key already exists (do not add).
  </action>
  <verify>
    <automated>cd donnarec-web && rtk npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds
    - the queue BFF route pins status=pending_review and forwards ?source= (mapping all->none, from-website->flow_b, staff-entered->flow_a)
    - SourceBadge renders distinct blue (flow_b) vs slate (flow_a) styling with aria-labels
  </acceptance_criteria>
  <done>Source badge, authenticated queue proxy, and queue/source i18n exist and build.</done>
</task>

<task type="auto">
  <name>Task 2: Queue page (Screen 11) — QueueTable + QueueSourceFilter</name>
  <files>donnarec-web/components/QueueTable.tsx, donnarec-web/components/QueueSourceFilter.tsx, donnarec-web/app/(app)/queue/page.tsx</files>
  <read_first>
    - donnarec-web/components/DonationTable.tsx (column-definition + TanStack Table pattern to mirror)
    - donnarec-web/components/DonationListView.tsx (the smart-container query/pagination pattern for a list screen)
    - donnarec-web/components/AgingStatCards.tsx (aria-pressed clickable-toggle pattern for the filter chips)
    - .planning/phases/06-public-donation-web-form-flow-b/06-UI-SPEC.md (Screen 11 layout, columns, filter chips, 20-row pagination, empty state)
  </read_first>
  <action>
    QueueSourceFilter.tsx: a segmented 3-chip control (all / from-website / staff-entered) with role="group" aria-label and per-chip aria-pressed, active chip in accent blue, default "all" — mirrors the AgingStatCards toggle pattern.
    QueueTable.tsx: a TanStack table with the Screen 11 column subset — submission date (created_at), donor name, amount (right-aligned comma-formatted), source (SourceBadge), and a manage link to the existing Screen 3 detail (/donations/[id]) — NO status column (all pending_review) and NO receipt column (none issued yet). 20 rows/page via shadcn Pagination.
    queue/page.tsx (in the (app) group): the smart container — heading "คิวรอตรวจสอบ", QueueSourceFilter driving the ?source= param, a TanStack query against /api/bff/queue, empty-state copy per the UI-SPEC. Slate/blue, no warm theme.
  </action>
  <verify>
    <automated>cd donnarec-web && rtk npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; /queue resolves inside AppShell
    - the source filter changes the query ?source= and the table reflects the filtered set
    - the table has no status column and no receipt column
  </acceptance_criteria>
  <done>/queue lists pending-review Flow A + Flow B records with a working source filter and source badges.</done>
</task>

<task type="auto">
  <name>Task 3: Screen 1 source column + Screen 3 source-aware creator label</name>
  <files>donnarec-web/components/DonationTable.tsx, donnarec-web/components/DonationDetailView.tsx</files>
  <read_first>
    - donnarec-web/components/DonationTable.tsx (existing columns; insert point between status and receipt number)
    - donnarec-web/components/DonationDetailView.tsx (the current creator/"ผู้สร้าง" field rendering)
    - .planning/phases/06-public-donation-web-form-flow-b/06-UI-SPEC.md ("Amendment to Screen 1" + "Amendment to Screen 3")
  </read_first>
  <action>
    DonationTable.tsx: insert a "แหล่งที่มา" column (~130px) between "สถานะ" and "เลขที่ใบเสร็จ" rendering SourceBadge from each row's source. No new filter on Screen 1 (D-77 scopes explicit separation to the queue only).
    DonationDetailView.tsx: make the creator label source-aware — when source==='flow_b', render the "ผู้สร้าง" value as "ผู้บริจาคส่งเอง (ผ่านเว็บไซต์)" with the blue SourceBadge beside it (NOT the raw public-web system-user display name); when source==='flow_a' render the existing creator name with the slate SourceBadge alongside for parity. Requires source to be present on the detail response — confirm GetDonationByID returns source (added in plan 01) and the BFF/detail types carry it; extend the FE type if needed.
  </action>
  <verify>
    <automated>cd donnarec-web && rtk npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds
    - DonationTable shows a source column between status and receipt number
    - a flow_b detail view renders the donor-self-submitted label, never the public-web system user's raw name
  </acceptance_criteria>
  <done>Screen 1 gains a source column; Screen 3 shows a source-aware creator label for web submissions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff browser -> queue BFF | authenticated (app) request |
| queue BFF -> Go list endpoint | session-bound bearer forwarding |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-25 | Elevation of Privilege | queue visibility | medium | mitigate | /queue is (app), behind middleware auth + the existing donationGroup role guard on /api/donations; the queue BFF uses bffForward (session bearer), never the session-less public passthrough |
| T-06-26 | Information Disclosure | public-web actor name shown | low | mitigate | Screen 3 renders a source-aware creator label for flow_b, never leaking the synthetic system-user identity as if a real staff member entered it |
| T-06-27 | Tampering | source filter bypass | low | mitigate | Filter maps to plan 01's server-side narg predicate + handler allow-list; client cannot inject an arbitrary source value that reaches SQL |
| T-06-SC | Tampering | npm install | low | accept | No new package this plan |
</threat_model>

<verification>
- `rtk npm run build` green
- /queue lists pending-review records with working source filter + badges
- flow_b detail shows the source-aware creator label
</verification>

<success_criteria>
Staff have a working /queue showing separated Flow A/Flow B pending-review records with a source filter; Screen 1 shows a source column; Screen 3 labels web submissions correctly.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 07)
- Components: `SourceBadge`, `QueueTable`, `QueueSourceFilter`
- Page: `app/(app)/queue/page.tsx` (implements the previously-dead /queue nav link)
- BFF route: `app/api/bff/queue/route.ts` (authenticated, status=pending_review + source passthrough)
- i18n namespaces: `queue.*`, `source.*` (th + en)
- Amendments: DonationTable source column; DonationDetailView source-aware creator label
</artifacts_produced>

<output>
Create `.planning/phases/06-public-donation-web-form-flow-b/06-07-SUMMARY.md` when done
</output>
