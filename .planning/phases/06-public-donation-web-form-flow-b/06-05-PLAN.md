---
phase: 06-public-donation-web-form-flow-b
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - donnarec-web/app/layout.tsx
  - donnarec-web/app/(app)/layout.tsx
  - donnarec-web/app/(public)/layout.tsx
  - donnarec-web/app/(public)/public-theme.css
  - donnarec-web/tailwind.config.ts
  - donnarec-web/middleware.ts
  - donnarec-web/package.json
autonomous: true
requirements: [NFR-06, FR-06]
must_haves:
  truths:
    - "authenticated pages render inside AppShell via an (app) route group and keep their exact URLs (/donations, /e-donation, /reports, /admin)"
    - "a (public) route group renders WITHOUT AppShell, wrapped in a .theme-public warm-token scope, reachable unauthenticated"
    - "the slate/blue (app) surface is visually unchanged — no :root or existing colors.* mutation leaks the warm theme"
  artifacts:
    - donnarec-web/app/(public)/layout.tsx
    - donnarec-web/app/(public)/public-theme.css
    - donnarec-web/app/(app)/layout.tsx
  key_links:
    - ".theme-public scoped CSS variable block <-> shadcn semantic var names <-> reused shadcn primitives auto-restyle"
    - "middleware.ts matcher excludes /donate (public reachable) and still matches (app) routes"
---

<objective>
The structural prerequisite for every public-facing screen (06-UI-SPEC "Architecture Change Required" + "Dual-Theme Architecture"): split the root layout so authenticated pages live in an `(app)` route group under `AppShell` (slate/blue, unchanged) and a new `(public)` route group renders unauthenticated pages under a scoped warm `.theme-public` token layer. Zero leakage into `(app)`.

Purpose: Today `app/layout.tsx` wraps EVERY page in AppShell and runs role checks — a public `/donate` page would render inside the authenticated shell against an empty session. This must be restructured, not worked around.
Output: Root layout reduced to html/body + providers + all font loads; `(app)/layout.tsx` renders AppShell; `(public)/layout.tsx` renders PublicHeader (added in plan 06) + the warm scope; `public-theme.css`; additive tailwind `public.*` colors; Turnstile npm dep added.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-public-donation-web-form-flow-b/06-CONTEXT.md
@.planning/phases/06-public-donation-web-form-flow-b/06-UI-SPEC.md
@.planning/phases/06-public-donation-web-form-flow-b/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split root layout into (app) and (public) route groups</name>
  <files>donnarec-web/app/layout.tsx, donnarec-web/app/(app)/layout.tsx, donnarec-web/app/(public)/layout.tsx</files>
  <read_first>
    - donnarec-web/app/layout.tsx (current: unconditional AppShell wrap, font loads, NextIntlClientProvider, AuthSessionProvider, Providers)
    - donnarec-web/app/page.tsx and the existing top-level route dirs (donations, e-donation, reports, admin) — the pages to move
    - donnarec-web/components/AppShell.tsx (what (app)/layout must render; role-check helpers it uses)
    - donnarec-web/middleware.ts (current matcher list)
    - .planning/phases/06-public-donation-web-form-flow-b/06-UI-SPEC.md ("Architecture Change Required" steps 1-4)
  </read_first>
  <action>
    Reduce app/layout.tsx to only html/body + font-variable classes + NextIntlClientProvider + AuthSessionProvider + Providers (TanStack Query) — REMOVE the direct AppShell wrap.
    Create app/(app)/layout.tsx that renders AppShell around {children}. MOVE the existing authenticated route dirs (donations/, e-donation/, reports/, admin/, and the root landing page/redirect) into app/(app)/ — route groups do not change URLs, so /donations etc. keep their exact paths and (app) keeps resolving the existing :root slate/blue variables unchanged. Do NOT change any (app) token or component.
    Create app/(public)/layout.tsx that imports public-theme.css and renders a single wrapper `<div className="theme-public ...font variable classes...">` containing PublicHeader (component built in plan 06 — reference it; if it does not yet exist, render a minimal placeholder header this plan, replaced in plan 06) and {children}. No AppShell, no SignOutButton, no role checks.
    Keep the app/api routes where they are (route groups are for pages).
  </action>
  <verify>
    <automated>cd donnarec-web && rtk npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; /donations, /e-donation, /reports, /admin still resolve (route-group move preserves URLs)
    - app/layout.tsx no longer imports or renders AppShell
    - app/(app)/layout.tsx renders AppShell; app/(public)/layout.tsx renders no AppShell and imports public-theme.css
  </acceptance_criteria>
  <done>Two route groups exist; authenticated URLs unchanged; public group renders outside AppShell.</done>
</task>

<task type="auto">
  <name>Task 2: Warm theme scope (public-theme.css) + additive tailwind namespace + fonts + Turnstile dep</name>
  <files>donnarec-web/app/(public)/public-theme.css, donnarec-web/tailwind.config.ts, donnarec-web/app/layout.tsx, donnarec-web/package.json</files>
  <read_first>
    - donnarec-web/app/globals.css (the :root and .dark scoped-variable precedent — shadcn semantic var names)
    - donnarec-web/tailwind.config.ts (existing colors.* hsl(var(--x)) mapping + borderRadius mapping — must NOT be mutated, only extended)
    - .planning/phases/06-public-donation-web-form-flow-b/06-UI-SPEC.md ("Dual-Theme Architecture" §1-§5, "Color" locked hex+HSL table, "Typography" (public) fonts, "Accessibility Contract" pine focus-ring override)
  </read_first>
  <action>
    public-theme.css: define a `.theme-public { ... }` block redefining the SAME shadcn semantic variables (--background, --foreground, --card, --popover, --primary, --primary-foreground, --secondary, --secondary-foreground, --muted, --muted-foreground, --accent, --accent-foreground, --destructive, --destructive-foreground, --border, --input, --ring, --radius) with the warm HSL values from the UI-SPEC Color table (paper/pine/gold-soft/gold-wash/ink/muted/line/destructive) — re-verify each hex->HSL with a converter before pasting (UI-SPEC flags the HSL triplets as needing verification). Set --radius to 18px in scope. Add the extra non-shadcn tokens in the same scope (--public-pine-2, --public-sage, --public-gold, --public-gold-wash, --public-ink, --public-line) and a --shadow-public-card custom property. Apply the warm font-family and the pine focus-visible outline (NOT gold — WCAG 1.4.11, pine measures ~8.94:1) per the Accessibility Contract, scoped under .theme-public. Override input/select/textarea min-height to 44px within scope. NOTHING here touches :root.
    tailwind.config.ts: ADD (do not mutate) an extend.colors.public.{pine,pine-2,sage,gold,gold-wash,ink,line} namespace mapping to the var(--public-*) tokens so bg-public-pine etc. resolve. Leave colors.primary/secondary/etc. exactly as-is.
    app/layout.tsx: add the three public fonts via next/font/google (Trirong weights 500+600 + italic 400; IBM Plex Sans Thai 400+600; IBM Plex Mono 400+500), exposing their CSS variables globally (harmless — unused until referenced under .theme-public). Keep the existing Sarabun+Inter loads.
    package.json: add @marsidev/react-turnstile at the pinned version 1.5.3 (legitimacy verdict OK in 06-RESEARCH audit — no human checkpoint required). Run npm install.
  </action>
  <verify>
    <automated>cd donnarec-web && rtk npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds with the new fonts + tailwind namespace + dep
    - public-theme.css defines .theme-public overriding shadcn semantic vars and sets --radius:18px, with a pine (not gold) focus-visible outline
    - globals.css :root and tailwind colors.primary/secondary are untouched (no diff to those lines)
    - @marsidev/react-turnstile@1.5.3 present in package.json
  </acceptance_criteria>
  <done>The warm theme is a scoped variable layer + additive tailwind namespace; fonts and Turnstile dep in place; (app) slate/blue untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public route group -> unauthenticated visitors | (public) pages must be reachable without a session |
| (public) theme scope -> (app) render tree | warm tokens must not leak to authenticated screens |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-18 | Tampering | middleware matcher | high | mitigate | /donate stays OUT of the auth matcher by omission; (app) routes stay matched — verified in plan 06 wiring and here by not adding /donate |
| T-06-19 | Elevation of Privilege | public layout role checks | medium | mitigate | (public)/layout runs no role checks / no session read — cannot 500 on an empty session or expose (app) nav |
| T-06-20 | Tampering | theme leakage into (app) | low | mitigate | .theme-public is a descendant-scoped class; :root + colors.* untouched (scoped-selector isolation, same mechanism as .dark) |
| T-06-SC | Tampering | npm install | medium | mitigate | @marsidev/react-turnstile@1.5.3 pinned, verdict OK (06-RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `rtk npm run build` green
- /donations, /e-donation, /reports, /admin URLs preserved after the route-group move
- globals.css :root and tailwind.config.ts colors.primary/secondary unchanged
</verification>

<success_criteria>
Authenticated app moved into (app)/AppShell (URLs + slate/blue unchanged); (public) route group renders outside AppShell under a scoped warm .theme-public token layer; fonts + Turnstile dep added; zero theme leakage.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 05)
- Route groups: `app/(app)/`, `app/(public)/` with their layouts
- File: `app/(public)/public-theme.css` (.theme-public scoped variable block)
- Tailwind namespace: `colors.public.*` (pine, pine-2, sage, gold, gold-wash, ink, line)
- Fonts: Trirong, IBM Plex Sans Thai, IBM Plex Mono CSS variables
- CSS custom props: `--shadow-public-card`, `--public-*` tokens
- npm dependency: `@marsidev/react-turnstile@1.5.3`
</artifacts_produced>

<output>
Create `.planning/phases/06-public-donation-web-form-flow-b/06-05-SUMMARY.md` when done
</output>
