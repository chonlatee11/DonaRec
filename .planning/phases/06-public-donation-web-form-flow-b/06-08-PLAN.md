---
phase: 06-public-donation-web-form-flow-b
plan: 08
type: execute
wave: 4
depends_on: ["06-05", "06-06", "06-07", "06-04"]
files_modified:
  - donnarec-web/components/MobileNavDrawer.tsx
  - donnarec-web/components/AppShell.tsx
  - donnarec-web/components/DonationTable.tsx
  - donnarec-web/components/AgingTable.tsx
autonomous: false
requirements: [NFR-06]
user_setup:
  - service: cloudflare-turnstile
    why: "Real CAPTCHA challenge on the public form requires a Turnstile site+secret key pair"
    env_vars:
      - name: NEXT_PUBLIC_TURNSTILE_SITE_KEY
        source: "Cloudflare Dashboard -> Turnstile -> Add site (or the documented test keys)"
      - name: TURNSTILE_SECRET_KEY
        source: "Cloudflare Dashboard -> Turnstile -> the site's secret key (Go backend env)"
must_haves:
  truths:
    - "below 768px the AppShell sidebar collapses into a hamburger-triggered slide-in drawer with backdrop + Escape/close + focus return; at >=768px the fixed sidebar is unchanged"
    - "the public form and the staff queue are usable and correctly laid out on a mobile viewport in both Thai and English"
    - "wide data tables scroll horizontally below 768px instead of overflowing"
  artifacts:
    - donnarec-web/components/MobileNavDrawer.tsx
  key_links:
    - "hamburger button (AppShell) <-> MobileNavDrawer open state <-> the existing <aside> nav markup"
---

<objective>
Complete NFR-06: retrofit `AppShell` with a mobile navigation drawer (the fixed 256px sidebar consumes 68% of a 375px viewport today) and add horizontal-scroll wrappers to wide tables, then run the responsive + bilingual human walkthrough across the public form and the staff queue on desktop and mobile in Thai and English — the phase's natural completion point for full bilingual/responsive coverage.

Purpose: NFR-06 is explicitly anchored to this phase; the public form is the first mobile-first surface and the back-office must finally be mobile-usable.
Output: MobileNavDrawer + AppShell hamburger/drawer, table scroll wrappers, and a human-verify checkpoint confirming responsive/bilingual behavior and a real Turnstile challenge.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-public-donation-web-form-flow-b/06-UI-SPEC.md
@.planning/phases/06-public-donation-web-form-flow-b/06-VALIDATION.md
@$HOME/.claude/projects/-home-chonlatee-Desktop-Lab-DonaRec/memory/uat-local-stack.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile nav drawer retrofit + table horizontal-scroll wrappers</name>
  <files>donnarec-web/components/MobileNavDrawer.tsx, donnarec-web/components/AppShell.tsx, donnarec-web/components/DonationTable.tsx, donnarec-web/components/AgingTable.tsx</files>
  <read_first>
    - donnarec-web/components/AppShell.tsx (the current `<aside className="flex w-64 ... shrink-0">` nav markup + header)
    - donnarec-web/components/CancelDialog.tsx or any existing AlertDialog usage (the focus-trap pattern to reuse for the drawer dialog)
    - donnarec-web/components/DonationTable.tsx, donnarec-web/components/AgingTable.tsx (wrap points)
    - .planning/phases/06-public-donation-web-form-flow-b/06-UI-SPEC.md ("Mobile Navigation Retrofit" + "Table/filter responsiveness" + Accessibility Contract drawer roles)
  </read_first>
  <action>
    MobileNavDrawer.tsx: a <768px slide-in wrapper around AppShell's EXISTING <aside> nav markup — fixed inset-y-0 left-0 w-[280px] (min(280px,85vw)) z-50, translate-x-0 when open / -translate-x-full when closed with transition-transform, a bg-slate-900/40 backdrop that closes on click, role="dialog" aria-modal="true" aria-label="เมนูนำทาง", focus trapped while open, Escape to close, an X close button (44px), focus returning to the hamburger on close.
    AppShell.tsx: at >=768px keep the fixed 256px sidebar exactly as today. At <768px hide the sidebar and add a hamburger Button (ghost, Menu icon, aria-label "เมนู", 44px, aria-expanded reflecting drawer state, aria-controls pointing at the drawer id) at the header's left; tapping it opens MobileNavDrawer with the identical nav links. Also add the shared mobile page-padding override (px-4 / lg vertical) to <main>. Slate/blue, no warm theme.
    DonationTable.tsx + AgingTable.tsx: wrap the table in an overflow-x-auto container below 768px so wide tables scroll horizontally rather than overflow (per UI-SPEC — no column-hiding scheme). PublicHeader needs no drawer (only a LocaleSwitcher).
  </action>
  <verify>
    <automated>cd donnarec-web && rtk npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds
    - AppShell renders a hamburger with aria-expanded/aria-controls below md and the fixed sidebar at md+
    - MobileNavDrawer is role="dialog" aria-modal with a focus trap, backdrop-click/Escape/X close, and focus return
    - DonationTable and AgingTable have an overflow-x-auto wrapper
  </acceptance_criteria>
  <done>The back-office is mobile-usable via a drawer; wide tables scroll; desktop layout unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Responsive + bilingual human walkthrough (public form + queue, desktop + mobile, TH/EN, real Turnstile)</name>
  <action>
    All prior tasks are automated and green. This is a human verification of the assembled Flow B slice on a live local stack. The operator brings up the stack, provides Turnstile keys, and walks the numbered steps in how-to-verify across desktop and a ~375px mobile viewport in both Thai and English, checking the dev mailer capture for the ack email. Fix any responsive/bilingual/flow defect surfaced, then re-run this checkpoint.
  </action>
  <what-built>
    The full Flow B slice: the warm public /donate form (bilingual, mandatory slip, consent, Turnstile), the session-less submit path landing in pending_review, the ack email, the staff /queue with source separation, and the responsive mobile-nav retrofit.
  </what-built>
  <how-to-verify>
    Bring up the local stack per the UAT local-stack memory (docker compose + seeded users + donnarec-web/.env.local). Provide Turnstile keys (NEXT_PUBLIC_TURNSTILE_SITE_KEY + TURNSTILE_SECRET_KEY — Cloudflare test keys are acceptable for this walkthrough).
    1. Desktop, Thai: open /donate, complete the form with a valid slip (jpg/png/pdf), check consent, complete the real Turnstile challenge, submit. Confirm the in-page confirmation shows a reference number and the explicit "not yet a receipt" wording, and a copy button works.
    2. Toggle EN on /donate: confirm all labels/intro/consent/confirmation copy switch to English and the submitted donor_language follows.
    3. Mobile viewport (~375px): repeat the /donate submission — confirm the card goes full-bleed, fields reflow to single column, inputs are >=44px, the sticky header + language toggle stay reachable, no horizontal overflow.
    4. Check the donor inbox (dev mailer capture): an ack email arrived in the submitted language stating "received, not yet a receipt" with the reference number.
    5. Staff, desktop: open /queue, confirm the new submission appears with a "จากเว็บไซต์" source badge; exercise the all / from-website / staff-entered filter; open the record and confirm the source-aware creator label ("ผู้บริจาคส่งเอง (ผ่านเว็บไซต์)").
    6. Staff, mobile (~375px): confirm the sidebar is a hamburger-triggered drawer (backdrop, Escape, focus behavior) and the queue table scrolls horizontally without breaking layout.
    7. Confirm the negative CAPTCHA path: an unverified/failed challenge surfaces the distinct CAPTCHA error copy, not a field-validation error.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any responsive/bilingual/flow issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile drawer -> (app) nav | drawer exposes the same authenticated nav as desktop |
| real Turnstile challenge -> Go siteverify | live outbound egress during the walkthrough |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-28 | Elevation of Privilege | mobile drawer nav | low | mitigate | Drawer renders the SAME nav markup behind the SAME middleware/role guards — no new route or capability exposed by the responsive variant |
| T-06-29 | Denial of Service | real Turnstile egress-restricted | medium | accept | Fail-closed verifier (plan 02/03) rejects on egress failure; hosting egress is an open stakeholder item (06-RESEARCH Pitfall 5) surfaced in this walkthrough, not silently decided |
| T-06-SC | Tampering | npm install | low | accept | No new package this plan |
</threat_model>

<verification>
- `rtk npm run build` green
- Full backend suite green before the walkthrough: `cd donnarec-api && rtk go test ./... -count=1` (and the plan 03 E2E gate under -race)
- Human walkthrough (Task 2) approved
</verification>

<success_criteria>
NFR-06 met: back-office is mobile-usable via a drawer, wide tables scroll, and the public form + queue pass a responsive + bilingual human walkthrough in Thai and English on desktop and mobile with a real Turnstile challenge.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 08)
- Component: `MobileNavDrawer`
- AppShell: mobile hamburger + drawer integration + mobile page-padding override
- Table amendments: overflow-x-auto wrappers on DonationTable + AgingTable
</artifacts_produced>

<output>
Create `.planning/phases/06-public-donation-web-form-flow-b/06-08-SUMMARY.md` when done
</output>
