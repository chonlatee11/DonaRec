---
phase: 06-public-donation-web-form-flow-b
plan: 04
type: tdd
wave: 3
depends_on: ["06-03"]
files_modified:
  - donnarec-api/internal/worker/ack_email.go
  - donnarec-api/internal/worker/ack_email_test.go
  - donnarec-api/internal/worker/worker.go
  - donnarec-api/internal/i18n/locales/th.json
  - donnarec-api/internal/i18n/locales/en.json
autonomous: true
requirements: [FR-05, FR-06]
must_haves:
  truths:
    - "the worker's ProcessOnce dispatches job_type 'ack_email' to a handler that sends a bilingual email to the donor in donor_language"
    - "the ack email copy explicitly states the submission was received and is NOT yet a receipt, and carries the reference number"
    - "ack sending runs in the worker off the submit request path (does not block or roll back the submission)"
  artifacts:
    - donnarec-api/internal/worker/ack_email.go
    - donnarec-api/internal/i18n/locales/th.json
  key_links:
    - "worker.go ProcessOnce switch case 'ack_email' <-> handleAckEmail <-> mailer.EmailSender + i18n catalog"
---

<objective>
Consume the `ack_email` outbox job (enqueued atomically in plan 03) in the existing worker, sending the donor a bilingual acknowledgement that explicitly says "received, not yet a receipt" with their reference number (D-85, FR-05, FR-06). Reuses the Phase 4 outbox/worker/mailer seam verbatim — only a new job-type arm and new i18n strings.

Purpose: Closes the donor-facing feedback loop off the request path (NFR-07), decoupled and retryable like issue_receipt.
Output: `handleAckEmail`, a new `case "ack_email"` in ProcessOnce, and new bilingual `ackEmail.*` i18n keys, proven by a worker test that drives ProcessOnce synchronously.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-public-donation-web-form-flow-b/06-CONTEXT.md
@.planning/phases/06-public-donation-web-form-flow-b/06-RESEARCH.md
@.planning/phases/06-public-donation-web-form-flow-b/06-PATTERNS.md
@.planning/phases/06-public-donation-web-form-flow-b/06-UI-SPEC.md
</context>

<feature>
  <name>ack_email outbox job handler (bilingual "not a receipt" acknowledgement)</name>
  <files>donnarec-api/internal/worker/ack_email.go, donnarec-api/internal/worker/ack_email_test.go, donnarec-api/internal/worker/worker.go, donnarec-api/internal/i18n/locales/*.json</files>
  <read_first>
    - donnarec-api/internal/worker/issue_receipt.go (handleIssueReceipt — sibling handler shape: payload decode, donation fetch, mailer.Send, delivery record write, error contract)
    - donnarec-api/internal/worker/worker.go (ProcessOnce switch at line ~219; MarkOutboxJobFailed/backoff at ~226-239 — uniform for all job types)
    - donnarec-api/internal/mailer/sender.go (EmailSender.Send signature + how issue_receipt builds subject/body via i18n)
    - donnarec-api/internal/i18n/ (catalog structure + existing receipt/email keys added in Phase 4; how donor_language selects locale)
    - donnarec-api/internal/worker/*_test.go (existing worker test that drives ProcessOnce with a fake EmailSender)
    - .planning/phases/06-public-donation-web-form-flow-b/06-UI-SPEC.md ("Ack Email Template Copy" — exact TH subject + opening line)
  </read_first>
  <behavior>
    - Test: enqueue an ack_email job for a pending_review flow_b donation, run ProcessOnce with a capturing fake EmailSender; assert one email was sent to the donor email, subject/body resolved from the ackEmail.* i18n keys, and the reference number appears in the body
    - Test: donor_language 'en' resolves the English catalog; 'th' resolves Thai (FR-06)
    - Test: a donation with no donor email does NOT send and does not error the job fatally (no email is an expected terminal state, not a retry loop) — mirror how issue_receipt handles the no-email case
    - Test: an unknown job_type still hits the default error arm (regression guard on the switch)
  </behavior>
  <implementation>
    ack_email.go: `handleAckEmail(ctx, job)` on *Worker — decode the payload donation_id, fetch the donation, compute the donor-facing reference number the same way plan 03's response did (derive from donation id, NOT a receipt number), select locale from donor_language, build subject + body from new i18n keys, and send via mailer.EmailSender. Follow issue_receipt's error contract (return a plain error so ProcessOnce's existing retry/backoff applies uniformly). Optionally record an email_delivery row for parity (06-RESEARCH Open Question 3 recommends reuse; cheap, no new UI) — Claude's discretion.
    worker.go: add `case "ack_email": procErr = w.handleAckEmail(ctx, job)` alongside the existing issue_receipt case; leave the default error arm intact.
    i18n: add ackEmail.subject / ackEmail.body (or opening-line) keys to th.json and en.json. TH subject and opening line must match 06-UI-SPEC's Ack Email Template Copy and must contain the explicit not-yet-a-receipt statement (non-negotiable per FR-05/D-84). Provide the English equivalents. Reference number rendered with a monospace fallback stack in the email HTML (email web-font support is unreliable — same approach as the Phase 4 receipt email).
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outbox job payload -> worker | job payload written by the trusted in-tx enqueue (plan 03) |
| worker -> donor email | outbound email to a donor-supplied address |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-15 | Spoofing/Repudiation | ack email misrepresents status | high | mitigate | Copy explicitly states "received, not yet a receipt" (FR-05/D-84); no receipt number is present (none exists pre-approval) |
| T-06-16 | Denial of Service | ack send failures block submit | high | mitigate | Runs in the worker off the request path via outbox (NFR-07); a send failure retries/backs off, never rolls back the submission |
| T-06-17 | Information Disclosure | donor PII in logs | medium | mitigate | Pattern C logging — donation_id + job id only, never donor name/email/tax ID |
| T-06-SC | Tampering | package installs | low | accept | No new package; reuses mailer/i18n/worker |
</threat_model>

<verification>
- `rtk go test ./internal/worker/... -run TestAckEmail -count=1` passes (bilingual send, no-email terminal, unknown-jobtype default)
- `rtk go build ./...` green
</verification>

<success_criteria>
ack_email jobs are dispatched and produce a bilingual "not a receipt" email carrying the reference number, off the request path, without disturbing issue_receipt.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 04)
- Worker handler: `handleAckEmail` + ProcessOnce `case "ack_email"`
- i18n keys: `ackEmail.subject`, `ackEmail.body` (th + en)
- Test: `ack_email_test.go` (TestAckEmail)
</artifacts_produced>

<output>
Create `.planning/phases/06-public-donation-web-form-flow-b/06-04-SUMMARY.md` when done
</output>
