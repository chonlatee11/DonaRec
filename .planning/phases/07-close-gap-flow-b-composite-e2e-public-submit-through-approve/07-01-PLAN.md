---
phase: 07-close-gap-flow-b-composite-e2e-public-submit-through-approve
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - donnarec-api/cmd/server/e2e_flowb_composite_test.go
autonomous: true
requirements: [FR-01, FR-03, FR-04, FR-08, FR-14, FR-15, FR-16]

must_haves:
  truths:
    - "A flow_b record submitted via multipart POST /api/public/donations, when approved by a real Keycloak-shaped Checker token, reaches status=issued with a non-empty gap-less receipt_formatted."
    - "Approval enqueues exactly one issue_receipt outbox job carrying the donation id."
    - "SoD is NOT violated across the composite: the flow_b record's created_by is the seeded public-web user UUID, distinct from the approving Checker — so the same-pipeline approve succeeds rather than 403."
    - "An in-tx approval audit row (action=donation.approve) is written under the Checker's Keycloak subject."
  artifacts:
    - "donnarec-api/cmd/server/e2e_flowb_composite_test.go (new sibling test file, package main)"
    - "Test function TestE2E_FlowBCompositePublicSubmitToIssued"
  key_links:
    - "doPublicSubmission (public-half seam) → DB flow_b pending_review row lookup → POST /api/donations/{id}/approve (issuance seam) — the untested composite handoff (audit WARNING-1)."
    - "receiptno.Allocator.Allocate is source-agnostic: a flow_b record must receive a gap-less number identically to a flow_a record (FR-15/FR-16)."
---

<objective>
Close v1.0 milestone audit WARNING-1 by adding ONE automated end-to-end test that spans
the full Flow B composite seam: a public donation submission
(`POST /api/public/donations`) creates a `source=flow_b` `pending_review` record, then a
Checker approves that same record (`POST /api/donations/{id}/approve` with a real
Keycloak-shaped Checker token) and the test asserts the record reaches `status=issued`
with a non-empty gap-less `receipt_formatted`, exactly one `issue_receipt` outbox job, and
an approval audit row.

This phase adds NO new product behavior. Both halves are already independently E2E-tested
(`TestPublicDonationE2E`, `TestE2E_MakerCheckerIssuancePipeline`) and the issuance path is
already proven source-agnostic. The gap is a missing automated lock on the highest-value
cross-flow handoff, per the project CONVENTIONS integration-test gate (which caught 3 seam
bugs in Phase 3). This test locks that handoff.

Purpose: Regression-guard the Flow B → issuance composite so a future change to either the
public-submission seam or the approve/issuance seam cannot silently break the handoff.
Output: One new sibling test file reusing the existing E2E harness verbatim — no new
harness, no new fixtures, no production-code change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONVENTIONS.md
@.planning/v1.0-MILESTONE-AUDIT.md

# The two existing E2E tests whose helpers this plan REUSES verbatim (do NOT rebuild):
@donnarec-api/cmd/server/e2e_public_test.go
@donnarec-api/cmd/server/e2e_test.go

# Source of truth for the approve seam's audit action + outbox job type (already read during planning):
@donnarec-api/internal/donation/service.go
@donnarec-api/internal/donation/public_submission.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Flow B composite E2E test (public-submit → Checker-approve → issued)</name>
  <files>donnarec-api/cmd/server/e2e_flowb_composite_test.go</files>
  <read_first>
    - donnarec-api/cmd/server/e2e_public_test.go — REUSE `doPublicSubmission(t, fields, slipFilename, slipBytes, remoteAddr)`, `validPublicFields(donorName)`, and `settingsPNGBytes()`. Note the public response carries a `reference_number` (REF-…), NOT the donation id, so the donation id must be fetched from the DB (mirror the `SELECT id::text FROM donations WHERE donor_name = $1` pattern in the HappyPath subtest).
    - donnarec-api/cmd/server/e2e_test.go — REUSE `newE2EHarness(t)`, `h.provisionUser(t, email, displayName, subject, db.UserRoleEnumChecker)`, `h.kc.MintTokenForSubject(sub, backendClientID, "checker")`, `h.do(t, method, path, token, body)`, `decodeDonation(t, w)` (returns `donation.DonationDetailResponse` with `.Status` and `.ReceiptFormatted *string`), and `backendClientID`. The file is package `main`; a sibling file in the same package sees all these identifiers.
    - donnarec-api/internal/donation/service.go (≈616-640) — the approve in-tx audit row uses `ActorID: claims.Subject` (the Checker's raw Keycloak subject) with `Action: "donation.approve"`, and enqueues `JobType: "issue_receipt"` with `payload->>'donation_id'` = the donation id.
    - donnarec-api/internal/donation/public_submission.go — `donation.PublicWebUserID` is the seeded public-web actor UUID; flow_b rows are created with `created_by = donation.PublicWebUserID`, `source = flow_b`. This is why SoD (`approver_id != created_by`) is satisfied when any Checker approves a flow_b record.
  </read_first>
  <action>
    Create a NEW sibling file `donnarec-api/cmd/server/e2e_flowb_composite_test.go` in package `main` with a single test `TestE2E_FlowBCompositePublicSubmitToIssued`. Guard it with the same `if testing.Short() { t.Skip(...) }` preamble the other E2E tests use (requires Docker testcontainers; runs under -race). Do NOT modify e2e_public_test.go or e2e_test.go; do NOT add any new harness, fixture, or helper unless strictly needed (a small local DB-lookup closure is fine). Do NOT touch any production code — this phase is test-only, adds no product behavior, and locks existing wiring (FR-01/FR-03/FR-04/FR-08 integration coverage; FR-14/FR-15/FR-16 asserted end-to-end).

    Test body:
    1. `h := newE2EHarness(t)`. Provision a Checker with a distinct keycloak subject, e.g. `const subChecker = "77777777-7777-7777-7777-777777777777"`, via `h.provisionUser(t, "checker-flowb-e2e@example.com", "Checker FlowB E2E", subChecker, db.UserRoleEnumChecker)`; mint `checkerToken := h.kc.MintTokenForSubject(subChecker, backendClientID, "checker")`.
    2. Submit a valid public donation with a unique donor name (e.g. `const donorName = "นาย ทดสอบ Flow B Composite"`): `w := h.doPublicSubmission(t, validPublicFields(donorName), "slip.png", settingsPNGBytes(), "")`; `require.Equal(t, http.StatusCreated, w.Code, "public submission body: %s", w.Body.String())`.
    3. Look up the created flow_b donation id from the DB (the public response does not return it): `require.NoError(t, h.pool.QueryRow(h.ctx, `+"`"+`SELECT id::text, status, source, created_by::text FROM donations WHERE donor_name = $1`+"`"+`, donorName).Scan(&donationID, &status, &source, &createdBy))`. Assert `status == "pending_review"`, `source == "flow_b"`, and `createdBy == donation.PublicWebUserID` (this is the SoD precondition — the flow_b creator is the public-web user, distinct from the Checker, so approve must NOT 403 on SoD).
    4. Approve that same record over the real HTTP path: `w = h.do(t, http.MethodPost, "/api/donations/"+donationID+"/approve", checkerToken, nil)`; `require.Equal(t, http.StatusOK, w.Code, "approve body: %s", w.Body.String())`. Decode with `issued := decodeDonation(t, w)`.
    5. Assert issuance: `assert.Equal(t, "issued", issued.Status)`; `require.NotNil(t, issued.ReceiptFormatted, ...)`; `assert.NotEmpty(t, *issued.ReceiptFormatted, "receipt number must be allocated gap-less in the issuance tx")`. (Optionally assert the formatted receipt matches the configured shape via a `\d` regex on the running-number portion — keep it minimal; non-empty is the load-bearing assertion.)
    6. Assert exactly one `issue_receipt` outbox job for this donation: `SELECT count(*) FROM outbox_jobs WHERE job_type = 'issue_receipt' AND payload->>'donation_id' = $1` → `assert.Equal(t, 1, issueCount, ...)`.
    7. Assert the approval audit row exists under the Checker's subject: `SELECT count(*) FROM audit_log WHERE actor_id = $1 AND action = 'donation.approve'` with `subChecker` → `assert.Equal(t, 1, approveAuditCount, ...)`.

    Add a top-of-file doc comment explaining this closes v1.0 audit WARNING-1 and cites the CONVENTIONS integration-test gate. Follow the import + assertion style of the two existing E2E files (`require`/`assert` from testify, `donation` package import for `PublicWebUserID`).

    FAILURE SEMANTICS (critical — this is a gate-closing characterization test): if any assertion fails, treat it as a candidate real seam defect in the flow_b → approve composite handoff and investigate the wiring (this is exactly the class of bug this test exists to catch — the Phase 3 gate surfaced 3 such seam bugs). Do NOT relax, delete, or soften any assertion to make the test go green.
  </action>
  <verify>
    <automated>cd donnarec-api && go vet ./cmd/server/ && go test ./cmd/server/ -run TestE2E_FlowBCompositePublicSubmitToIssued -race -count=1 -v</automated>
  </verify>
  <done>
    `TestE2E_FlowBCompositePublicSubmitToIssued` compiles and PASSES under -race against the
    Postgres testcontainer harness. It drives the real router for BOTH halves: multipart
    public submit → DB flow_b pending_review lookup → Checker-token approve → issued. All
    assertions hold: status=issued, non-empty receipt_formatted, exactly one issue_receipt
    outbox job, exactly one donation.approve audit row under the Checker subject, and the
    flow_b created_by = public-web UUID (SoD satisfied). No production file changed; the two
    existing E2E test files are untouched.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
- **New test file:** `donnarec-api/cmd/server/e2e_flowb_composite_test.go` (package `main`).
- **New test function:** `TestE2E_FlowBCompositePublicSubmitToIssued`.
- **No new helpers/fixtures/harness** — reuses `newE2EHarness`, `doPublicSubmission`,
  `validPublicFields`, `settingsPNGBytes`, `provisionUser`, `MintTokenForSubject`, `do`,
  `decodeDonation`, `backendClientID`, `donation.PublicWebUserID`.
- **No production-code change** — coverage-only phase.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

This phase adds NO new product behavior and NO new runtime trust boundary — it adds a
test that exercises the two already-existing seams (unauthenticated public submit; Checker
approve). The composite handoff it locks is precisely the seam class the CONVENTIONS
integration-test gate exists to protect.

| Boundary | Description |
|----------|-------------|
| public (unauthenticated) → /api/public/donations | Untrusted donor input (already mitigated in Phase 6: magic-byte slip validation, rate-limit, CAPTCHA). Unchanged here. |
| Checker (authenticated) → /api/donations/{id}/approve | Real Keycloak token → RequireAuth → RequireAnyRole → SoD. Unchanged here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | New test file introduces no new attack surface (test-only, package main, Docker-gated) | low | accept | No production code changes; test compiled+run under -race in CI-equivalent local run. |
| T-07-02 | Elevation of Privilege | SoD across composite (flow_b created_by = public-web UUID vs approving Checker) | low | mitigate | Test explicitly asserts created_by == public-web UUID (distinct from Checker) so the approve succeeds via the SAME SoD-enforcing service path, not a bypass — locks the invariant that flow_b records are approvable without violating `approver_id != created_by`. |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new dependencies installed — reuses existing testify + testcontainers already in go.mod. No package-legitimacy gate needed. |
</threat_model>

<verification>
- `cd donnarec-api && go test ./cmd/server/ -run TestE2E_FlowBCompositePublicSubmitToIssued -race -count=1 -v` PASSES (Docker required).
- Full E2E suite still green (no regression to existing tests): `cd donnarec-api && go test ./cmd/server/ -race -count=1` (or with `-short` to confirm the file compiles without Docker: `go test ./cmd/server/ -short`).
- `go vet ./cmd/server/` clean.
- Existing files `e2e_public_test.go` and `e2e_test.go` are byte-unchanged (git diff shows only the new file).
</verification>

<success_criteria>
- One new automated E2E test spans the full Flow B composite (public-submit → Checker-approve → issued) and passes under -race.
- Asserted end-to-end: status=issued, non-empty gap-less receipt_formatted, exactly one issue_receipt outbox job, one donation.approve audit row, SoD satisfied (flow_b created_by ≠ Checker).
- v1.0 milestone audit WARNING-1 is closed by an automated lock, per the CONVENTIONS integration-test gate.
- No production code changed; no new harness/fixtures added.
</success_criteria>

<output>
Create `.planning/phases/07-close-gap-flow-b-composite-e2e-public-submit-through-approve/07-01-SUMMARY.md` when done.
</output>
