---
phase: 04-receipt-pdf-email-delivery-outbox-worker
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - donnarec-api/docker/chrome.Dockerfile
  - donnarec-api/docker-compose.yml
  - donnarec-api/internal/testutil/chrome.go
  - donnarec-api/go.mod
  - donnarec-api/go.sum
  - donnarec-api/assets/fonts/README.md
autonomous: true
requirements: [FR-23, NFR-07]
must_haves:
  truths:
    - "A chrome sidecar service builds from chromedp/headless-shell:stable + fonts-thai-tlwg and exposes CDP on the internal compose network only"
    - "chromedp is pinned at v0.14.2 and the go.mod `go` directive stays at 1.25.1 (no toolchain bump)"
    - "internal/testutil/chrome.go starts the chrome sidecar via testcontainers and returns a CDP ws URL, mirroring postgres.go/keycloak.go"
    - "TH Sarabun New sourcing/licensing is documented as an explicit assumption for the executor"
  artifacts:
    - donnarec-api/docker/chrome.Dockerfile
    - donnarec-api/internal/testutil/chrome.go
    - donnarec-api/assets/fonts/README.md
  key_links:
    - "PDF render core (04-03) connects to this sidecar via chromedp.NewRemoteAllocator for golden + security tests"
    - "worker integration test (04-05) reuses testutil.StartChrome"
  prohibitions:
    - statement: "chromedp must not be upgraded past v0.14.2 (v0.15.1 forces Go 1.26)"
      status: enforced
      verification: "grep chromedp go.mod shows v0.14.2 AND `go` directive is 1.25.1"
    - statement: "the chrome sidecar must not publish host ports / expose an internet-reachable route"
      status: enforced
      verification: "docker-compose.yml chrome service has no `ports:` mapping"
---

<objective>
สร้าง Chromium sidecar (แยก container จาก app) + pin chromedp v0.14.2 + test harness (testcontainers) — โครงสร้างพื้นฐานที่ทำให้ PDF render pipeline (04-03) และ worker (04-05) รันและทดสอบได้ พร้อม Thai font + network isolation (D-58)

Purpose: RESEARCH ยืนยัน live ว่า Chromium render Thai ถูกต้องเมื่อมี fonts-thai-tlwg และ sandbox mitigations ทำงานที่ CDP level — plan นี้วางกล่อง Chromium ที่ปลอดภัยและ deterministic ให้ golden-file test พึ่งได้
Output: docker/chrome.Dockerfile, docker-compose chrome service, testutil/chrome.go, go.mod pin, font sourcing doc
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-RESEARCH.md
@.planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: chrome sidecar Dockerfile + docker-compose service + chromedp v0.14.2 pin</name>
  <files>donnarec-api/docker/chrome.Dockerfile, donnarec-api/docker-compose.yml, donnarec-api/go.mod, donnarec-api/go.sum, donnarec-api/assets/fonts/README.md</files>
  <read_first>
    - donnarec-api/docker-compose.yml (existing postgres/keycloak/minio service blocks — analog for the new chrome service)
    - donnarec-api/Dockerfile (why NOT to bake Chromium into the app image — deliberately slim)
    - .planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-RESEARCH.md (Pattern 4 verified Dockerfile; Standard Stack digest note; Pitfall 1 fonts; Pitfall 2 version pin)
    - donnarec-api/go.mod (current `go 1.25.1` directive — must NOT change)
  </read_first>
  <action>
    docker/chrome.Dockerfile: FROM chromedp/headless-shell:stable then apt-get install -y --no-install-recommends fonts-thai-tlwg fontconfig && fc-cache -f (RESEARCH Pattern 4, verified). Add a comment to re-pin the image digest (RESEARCH records sha256:313ed72... as of 2026-07-04 — re-pull and record before CI lock, Assumption A4). Provide a COPY slot for TH Sarabun New from assets/fonts/ when the licensed file is supplied.
    docker-compose.yml: add a `chrome` service building from docker/chrome.Dockerfile, on the default internal network, restart: unless-stopped, and NO `ports:` mapping (reachable only via internal network by `api` and by tests). Add depends-free; the app service gets CHROME_WS_URL=ws://chrome:9222 env.
    Pin the driver: `go get github.com/chromedp/chromedp@v0.14.2` and confirm the go.mod `go` directive is unchanged at 1.25.1 (Pitfall 2). Do not run an unpinned `@latest`.
    assets/fonts/README.md: document that TH Sarabun New must be sourced/licensed separately (Assumption A3 — not in fonts-thai-tlwg), the expected filename/path, and that fonts-thai-tlwg fonts (Waree/Purisa/etc.) are only an interim shaping fallback, never production typography.
  </action>
  <verify>
    <automated>cd donnarec-api && docker build -f docker/chrome.Dockerfile -t donnarec-chrome-test . && grep -q 'chromedp v0.14.2' go.mod && grep -qE '^go 1\.25' go.mod</automated>
  </verify>
  <acceptance_criteria>
    - `docker build -f docker/chrome.Dockerfile` succeeds and the resulting image contains fonts-thai-tlwg (fc-list | grep -i thai returns matches)
    - go.mod pins chromedp v0.14.2 and the `go` directive remains 1.25.1
    - docker-compose.yml chrome service has no host `ports:` mapping
  </acceptance_criteria>
  <done>A network-isolated Thai-font-equipped Chromium sidecar builds; chromedp is pinned without a toolchain bump; TH Sarabun sourcing is documented.</done>
</task>

<task type="auto">
  <name>Task 2: testutil.StartChrome testcontainers helper</name>
  <files>donnarec-api/internal/testutil/chrome.go</files>
  <read_first>
    - donnarec-api/internal/testutil/postgres.go (testcontainers-go wiring idiom — closest structural analog)
    - donnarec-api/internal/testutil/keycloak.go (container-with-readiness pattern)
    - .planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-RESEARCH.md (Environment Availability: headless-shell image; NewRemoteAllocator)
  </read_first>
  <action>
    Add StartChrome(t *testing.T) (wsURL string, cleanup func()) that starts the chromedp/headless-shell image (or the locally built donnarec-chrome image) via testcontainers-go, waits for the CDP endpoint to be reachable, and returns the browser websocket URL usable by chromedp.NewRemoteAllocator. Mirror postgres.go's constructor/cleanup shape and t.Helper() usage. Skip the test (t.Skip) with a clear message when Docker is unavailable, matching the existing testutil skip convention.
  </action>
  <verify>
    <automated>cd donnarec-api && go build ./internal/testutil/... && go vet ./internal/testutil/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/testutil/...` exits 0
    - StartChrome returns a ws:// URL and a cleanup func; skips gracefully without Docker
    - Signature/style mirror StartPostgres (t.Helper, cleanup closure)
  </acceptance_criteria>
  <done>Integration tests in 04-03 and 04-05 can obtain a real Chromium CDP endpoint through one helper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app→chrome (CDP) | render engine reachable only over the internal compose network |
| chrome→internet | must have NO outbound route (SSRF defense-in-depth, D-58) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Information Disclosure / SSRF | chrome sidecar network | high | mitigate | No host port mapping; internal-only network; CDP Fetch-block + JS-disable enforced in 04-03 render code (defense-in-depth layer 1 of 3) |
| T-04-04 | Tampering | pinned chromedp/base image | medium | mitigate | Pin chromedp v0.14.2 + document image digest re-pin before CI lock (Assumption A4) so render output stays deterministic |
| T-04-SC | Tampering | chromedp Go module | low | accept | RESEARCH Package Legitimacy Audit: chromedp OK (13k stars, official headless-shell pairing); pixelmatch [SUS] explicitly NOT adopted |
</threat_model>

<verification>
- chrome image builds with Thai fonts
- chromedp pinned v0.14.2, go directive 1.25.1
- StartChrome compiles and returns a CDP endpoint
</verification>

<success_criteria>
A safe, deterministic, Thai-capable Chromium sidecar plus a test helper exist so the render core can be built and golden-tested.
</success_criteria>

<artifacts_produced>
- docker/chrome.Dockerfile (chrome sidecar image)
- docker-compose.yml chrome service (internal network, no host ports)
- internal/testutil/chrome.go: StartChrome helper
- go.mod pin: github.com/chromedp/chromedp v0.14.2
- assets/fonts/README.md: TH Sarabun New sourcing/licensing note
</artifacts_produced>

<output>
Create `.planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-02-SUMMARY.md` when done
</output>
