---
phase: 04-receipt-pdf-email-delivery-outbox-worker
plan: 06
type: execute
wave: 4
depends_on: ["04-01", "04-05"]
files_modified:
  - donnarec-api/internal/donation/model.go
  - donnarec-api/internal/donation/service.go
  - donnarec-api/internal/donation/handler.go
  - donnarec-api/cmd/server/main.go
  - donnarec-api/cmd/server/e2e_test.go
  - donnarec-web/app/api/bff/donations/[id]/resend/route.ts
  - donnarec-web/app/api/bff/donations/[id]/receipt-pdf/route.ts
  - donnarec-web/components/DonationForm.tsx
  - donnarec-web/components/EmailDeliveryPanel.tsx
  - donnarec-web/components/DeliveryStatusBadge.tsx
  - donnarec-web/components/DonationDetailView.tsx
  - donnarec-web/messages/th.json
  - donnarec-web/messages/en.json
autonomous: false
requirements: [FR-27, FR-28, FR-23]
must_haves:
  truths:
    - "a Maker can pick donor_language (th|en) when creating/editing a Flow A donation; it is persisted and frozen into the snapshot (D-55)"
    - "Checker/Admin can resend a receipt email; resend re-enqueues an outbox job and never allocates a new number or re-renders (D-56/D-57)"
    - "any staff role can download the frozen receipt PDF via a short-lived presigned URL (FR-28)"
    - "the Donation Detail screen shows email delivery status + recipient + attempts and resend/download actions (Screen 3b)"
  artifacts:
    - donnarec-api/internal/donation/service.go
    - donnarec-api/internal/donation/handler.go
    - donnarec-web/components/EmailDeliveryPanel.tsx
    - donnarec-api/cmd/server/e2e_test.go
  key_links:
    - "resend inserts an outbox_jobs row consumed by the 04-05 worker (reuses frozen PDF)"
    - "download presigns the receipt_pdf_object_key set by the worker via the receipts-bucket StorageClient"
  prohibitions:
    - statement: "resend must never allocate a new receipt number"
      status: enforced
      verification: "e2e_test.go: donation receipt_no is identical before and after resend; the numbering allocator is not called on the resend path"
    - statement: "resend must not re-render the PDF (reuse the frozen object, D-56)"
      status: enforced
      verification: "resend only enqueues a job; the worker's freeze-idempotency (04-05) reuses the stored PDF"
---

<objective>
สร้าง staff resend/download slice + donor_language capture: Maker เลือกภาษาตอนสร้างรายการ (D-55), Checker/Admin กด resend (re-enqueue ไม่ออกเลขใหม่/ไม่ re-render), staff ดาวน์โหลด PDF เองได้ (FR-28), และหน้า Donation Detail แสดงสถานะการส่ง + ปุ่ม (Screen 3b) — ครบทั้ง backend + BFF + UI + E2E gate

Purpose: ปิด failure-UX loop ของอีเมล (D-57) และบังคับหลักความถูกต้องของเลขใบเสร็จ (resend ห้ามออกเลขใหม่); เพิ่ม donor_language ที่ freeze ขับภาษา PDF/email (FR-23)
Output: donation service/handler resend+download+donor_language, routes, BFF, EmailDeliveryPanel/DeliveryStatusBadge, E2E over real HTTP path
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-PATTERNS.md
@.planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-UI-SPEC.md
@.planning/CONVENTIONS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: donor_language capture on create/edit (backend) + E2E persistence</name>
  <files>donnarec-api/internal/donation/model.go, donnarec-api/internal/donation/service.go, donnarec-api/internal/donation/handler.go, donnarec-api/cmd/server/e2e_test.go</files>
  <read_first>
    - donnarec-api/internal/donation/service.go (Create/UpdateDraft — where snapshot fields are set; add donor_language, freeze per D-43 precedent)
    - donnarec-api/internal/donation/model.go (request/response DTOs)
    - donnarec-api/internal/donation/handler.go (Pattern A claims block; Create/Update handlers)
    - donnarec-api/cmd/server/e2e_test.go (existing real-HTTP+token E2E harness to extend)
  </read_first>
  <behavior>
    - Create/Update accept an optional donor_language ('th'|'en', default 'th'); it is persisted via CreateDonation/UpdateDraftDonation and returned in the detail response.
    - E2E: create a donation with donor_language 'en' via the real HTTP path (real Keycloak-shaped token, Maker role) → GET detail returns donor_language 'en'; omitting it defaults 'th'.
  </behavior>
  <action>
    Add DonorLanguage to the create/update request DTOs (validate oneof th en, default th) and to the detail response builder (buildDetailResponse). Thread it into the CreateDonation / UpdateDraftDonation sqlc calls (params added to donations.sql in 04-01). Frozen at create-time like other snapshot fields (D-43). Extend e2e_test.go with a subtest asserting persistence + default.
  </action>
  <verify>
    <automated>cd donnarec-api && go test ./internal/donation/... -run 'DonorLanguage' && go test ./cmd/server/... -run 'E2E'</automated>
  </verify>
  <acceptance_criteria>
    - E2E create-with-donor_language 'en' round-trips through real router→auth→handler→service→DB and detail returns 'en'
    - Omitted donor_language defaults to 'th'
    - Invalid value rejected with 422
  </acceptance_criteria>
  <done>donor_language is captured, validated, frozen, and returned; proven over the real HTTP path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resend + DownloadReceipt endpoints (backend) + E2E</name>
  <files>donnarec-api/internal/donation/service.go, donnarec-api/internal/donation/handler.go, donnarec-api/cmd/server/main.go, donnarec-api/cmd/server/e2e_test.go</files>
  <read_first>
    - donnarec-api/internal/donation/service.go (Approve Step 7 EnqueueOutboxJob — reuse the enqueue path for resend)
    - donnarec-api/internal/donation/handler.go (RevealPII + Approve handlers — Pattern A/C/D + app_user_id extraction)
    - donnarec-api/cmd/server/main.go (checkerGroup lines 269-275; donationGroup lines 235-258; RevealPII placement rationale)
    - donnarec-api/internal/storage/client.go (PresignedGet — 15-min TTL; a receipts-bucket StorageClient is wired in 04-05)
    - .planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-RESEARCH.md (Open Question 1 recommendation (b): resend inserts a new outbox job row)
  </read_first>
  <behavior>
    - Resend (Checker/Admin): only for issued receipts with a frozen PDF; inserts a NEW outbox_jobs issue_receipt row for the same donation_id (never allocates a number, never re-renders); returns 200; audited.
    - Download (any staff role): returns a short-lived presigned URL for receipt_pdf_object_key from the receipts bucket; 409/425-style error when the PDF is not yet frozen; audited.
    - E2E over real HTTP path: (a) resend by Checker enqueues a job and leaves receipt_no unchanged; (b) resend by Maker → 403; (c) download by Maker/Checker/Admin → 200 with a URL; (d) download before freeze → not-ready error.
  </behavior>
  <action>
    service.go: Resend(ctx, id, appUserID) loads the donation, guards status=issued AND receipt_pdf_object_key set, EnqueueOutboxJob(job_type issue_receipt, payload {donation_id}), AppendAuditEntryTx('receipt_resend'); DownloadReceipt(ctx, id) returns PresignedGet(receipt_pdf_object_key, 15m) via the receipts store, AppendAuditEntry('receipt_download'). handler.go: Resend + DownloadReceipt handlers (Pattern A claims + app_user_id extraction + Pattern C logging + audit_after). main.go: checkerGroup.POST("/:id/resend", donationHandler.Resend); donationGroup.GET("/:id/receipt-pdf", donationHandler.DownloadReceipt) (placed on donationGroup so a Maker gets 200, not 403, per D-57 "staff download always"). Extend e2e_test.go.
  </action>
  <verify>
    <automated>cd donnarec-api && go test ./cmd/server/... -run 'E2E' && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - Resend E2E: a new outbox_jobs row is created; donation receipt_no is byte-identical before/after
    - Resend by Maker returns 403; resend on a non-issued donation returns a domain error
    - Download E2E returns a presigned URL for all staff roles; pre-freeze download returns a not-ready error
    - Both actions write an append-only audit row
  </acceptance_criteria>
  <done>Resend re-enqueues without a new number or re-render; download presigns the frozen PDF; both audited and proven over the real HTTP path.</done>
</task>

<task type="auto">
  <name>Task 3: FE — donor_language field, resend/download BFF + EmailDeliveryPanel (Screen 3b)</name>
  <files>donnarec-web/app/api/bff/donations/[id]/resend/route.ts, donnarec-web/app/api/bff/donations/[id]/receipt-pdf/route.ts, donnarec-web/components/DonationForm.tsx, donnarec-web/components/EmailDeliveryPanel.tsx, donnarec-web/components/DeliveryStatusBadge.tsx, donnarec-web/components/DonationDetailView.tsx, donnarec-web/messages/th.json, donnarec-web/messages/en.json</files>
  <read_first>
    - donnarec-web/app/api/bff/donations/[id]/approve/route.ts (bffForward proxy shape — exact analog)
    - donnarec-web/app/api/bff/donations/[id]/slip/route.ts (presigned-URL proxy analog for receipt-pdf)
    - donnarec-web/components/DonationDetailView.tsx (useQuery/useMutation wiring; where to mount Screen 3b)
    - donnarec-web/components/DonationForm.tsx (create/edit form — add a language Select)
    - .planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-UI-SPEC.md (Screen 3b layout/state rules; DeliveryStatusBadge tokens; copywriting; accessibility)
  </read_first>
  <action>
    BFF: resend/route.ts POST → bffForward `/api/donations/${id}/resend`; receipt-pdf/route.ts GET → bffForward `/api/donations/${id}/receipt-pdf` (thin proxy only, Go re-enforces RBAC). DonationForm: add a donor_language Select (ไทย/English, default ไทย) mapped into the create/edit request body. DeliveryStatusBadge: Badge wrapper mapping sent/failed/pending/no_email → the UI-SPEC color tokens + Thai aria-label. EmailDeliveryPanel: render in DonationDetailView left panel only when status is issued|cancelled; show status badge + recipient + last-attempt timestamp (buddhist formatter) + attempts + collapsed provider msg id; download button (outline, always when a PDF exists) + resend button (outline, Checker/Admin only, hidden on cancelled); useMutation for resend with success/error toasts per copywriting; no-email empty state. Add settings-free emailDelivery.* message keys to messages/th.json + en.json.
  </action>
  <verify>
    <automated>cd donnarec-web && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; EmailDeliveryPanel renders for issued/cancelled only
    - Resend button visible to Checker/Admin only; download always available when a PDF exists
    - donor_language Select present in create/edit and posts to the BFF create/update body
    - emailDelivery.* keys exist in both th.json and en.json
  </acceptance_criteria>
  <done>Staff can see delivery status and resend/download from the detail screen; Makers set donor language on create.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify — Screen 3b delivery panel + resend/download + donor_language</name>
  <action>Human walkthrough of the email-delivery panel, resend (number-unchanged), download, and donor_language selector against the running stack. No code changes; confirm behavior matches the UI-SPEC Screen 3b contract.</action>
  <what-built>Screen 3b Email Delivery panel (status badge, recipient, attempts, resend/download) on Donation Detail, plus the donor_language selector on the create/edit form. Stack: API :8000, worker running, web :3000, MinIO, chrome sidecar.</what-built>
  <how-to-verify>
    1. Log in as a Maker; create a Flow A donation with donor_language = English; submit.
    2. Log in as a Checker; approve it → receipt issued.
    3. Wait a few seconds; open the donation detail — the Email Delivery panel shows a status badge (sent, or failed if the dev sender is configured to fail) with recipient + attempt count.
    4. Click "ดาวน์โหลด PDF" — the frozen receipt PDF downloads and renders Thai/English correctly.
    5. As Checker, click "ส่งอีเมลอีกครั้ง" — a success toast appears; verify the receipt number is unchanged and no new number was allocated.
    6. Confirm a Maker viewing the same receipt sees Download but NOT Resend.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→resend/download API | staff-triggered mutations/reads gated by role at the Go layer |
| BFF→Go API | BFF proxies a server-side bearer; never the authorization authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15 | Elevation of Privilege | resend endpoint | high | mitigate | checkerGroup RequireAnyRole(Checker,Admin); Maker → 403 (verified E2E, Conventions gate) |
| T-04-16 | Tampering | resend allocating a new number / re-render | high | mitigate | Resend only enqueues; worker reuses frozen PDF; receipt_no unchanged asserted in E2E (D-56/D-57) |
| T-04-17 | Information Disclosure | receipt PDF download URL | medium | mitigate | Short-lived presigned GET (15m); role-gated to staff; download audited |
| T-04-18 | Repudiation | resend/download actions | low | mitigate | Append-only audit rows for receipt_resend / receipt_download |
</threat_model>

<verification>
- E2E over real HTTP path: resend (RBAC + number-unchanged), download (all roles + not-ready), donor_language round-trip
- `npm run build` green; human UI walkthrough approved (Conventions integration gate)
</verification>

<success_criteria>
Staff can resend (safely) and download receipts, see delivery status, and set donor language — proven by real-HTTP E2E and a human walkthrough.
</success_criteria>

<artifacts_produced>
- Backend: donation service Resend + DownloadReceipt + donor_language capture; handlers Resend/DownloadReceipt; routes checkerGroup POST /:id/resend, donationGroup GET /:id/receipt-pdf
- E2E subtests: resend RBAC + number-unchanged, download all-roles + not-ready, donor_language persistence
- FE: BFF resend + receipt-pdf routes, DonationForm donor_language Select, DeliveryStatusBadge, EmailDeliveryPanel, DonationDetailView mount, emailDelivery.* messages
</artifacts_produced>

<output>
Create `.planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-06-SUMMARY.md` when done
</output>
