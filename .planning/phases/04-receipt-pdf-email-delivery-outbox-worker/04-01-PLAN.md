---
phase: 04-receipt-pdf-email-delivery-outbox-worker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - donnarec-api/migrations/000008_donor_language.up.sql
  - donnarec-api/migrations/000008_donor_language.down.sql
  - donnarec-api/migrations/000009_outbox_next_attempt_at.up.sql
  - donnarec-api/migrations/000009_outbox_next_attempt_at.down.sql
  - donnarec-api/migrations/000010_email_delivery.up.sql
  - donnarec-api/migrations/000010_email_delivery.down.sql
  - donnarec-api/migrations/000011_receipt_template_config.up.sql
  - donnarec-api/migrations/000011_receipt_template_config.down.sql
  - donnarec-api/migrations/000012_receipt_pdf_reference.up.sql
  - donnarec-api/migrations/000012_receipt_pdf_reference.down.sql
  - donnarec-api/internal/db/queries/outbox.sql
  - donnarec-api/internal/db/queries/email_delivery.sql
  - donnarec-api/internal/db/queries/settings.sql
  - donnarec-api/internal/db/queries/donations.sql
  - donnarec-api/internal/config/config.go
autonomous: true
requirements: [FR-23, FR-24, FR-27, FR-33, NFR-09, NFR-07]
must_haves:
  truths:
    - "golang-migrate up applies 000008..000012 cleanly and down reverts them"
    - "donations table has donor_language (th|en, default th) and receipt_pdf_object_key (nullable)"
    - "outbox_jobs has next_attempt_at so backoff is expressible without a scheduler"
    - "email_delivery table records one row per send attempt (status/provider_msg_id/attempts/error)"
    - "receipt_template_config single-row table seeded with a valid default Thai receipt template"
    - "sqlc generate produces compiling Go for the new worker/email/settings/donation queries"
  artifacts:
    - donnarec-api/migrations/000008_donor_language.up.sql
    - donnarec-api/migrations/000010_email_delivery.up.sql
    - donnarec-api/migrations/000011_receipt_template_config.up.sql
    - donnarec-api/migrations/000012_receipt_pdf_reference.up.sql
    - donnarec-api/internal/db/queries/email_delivery.sql
    - donnarec-api/internal/db/queries/settings.sql
  key_links:
    - "worker (04-05) reads receipt_template_config + GetDonationByID (donor_language) via these queries"
    - "resend/download (04-06) reads receipt_pdf_object_key populated by the worker"
    - "settings service (04-07) reads/writes receipt_template_config via GetReceiptTemplateConfig/UpdateReceiptTemplateConfig"
---

<objective>
สร้าง data-layer foundation ของ Phase 4: migrations 000008–000012, sqlc queries ใหม่ (worker claim/mark, email_delivery, settings, donation additions) และ config fields ใหม่ — เป็น enabling wave ที่ทุก plan ถัดไป (worker, resend/download, settings) ต่อยอด

Purpose: ทุก slice ของเฟสนี้ต้องมีตาราง/คอลัมน์/คิวรี/ค่า config พร้อมก่อน จึงจะ render/ส่ง/ตั้งค่าได้ โดยยึด immutability (D-55/D-56) และ backoff-without-scheduler (D-57)
Output: 5 migration pairs, 4 sqlc query files (แก้/เพิ่ม), generated db code, config.go additions
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-CONTEXT.md
@.planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-RESEARCH.md
@.planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrations 000008–000012 (donor_language, outbox next_attempt_at, email_delivery, receipt_template_config, receipt_pdf_reference)</name>
  <files>donnarec-api/migrations/000008_donor_language.up.sql, donnarec-api/migrations/000008_donor_language.down.sql, donnarec-api/migrations/000009_outbox_next_attempt_at.up.sql, donnarec-api/migrations/000009_outbox_next_attempt_at.down.sql, donnarec-api/migrations/000010_email_delivery.up.sql, donnarec-api/migrations/000010_email_delivery.down.sql, donnarec-api/migrations/000011_receipt_template_config.up.sql, donnarec-api/migrations/000011_receipt_template_config.down.sql, donnarec-api/migrations/000012_receipt_pdf_reference.up.sql, donnarec-api/migrations/000012_receipt_pdf_reference.down.sql</files>
  <read_first>
    - donnarec-api/migrations/000007_outbox_jobs.up.sql (status CHECK + attempts + grants footer — analog for email_delivery)
    - donnarec-api/migrations/000005_donations.up.sql (additive nullable/defaulted ALTER pattern — analog for donor_language + receipt_pdf_object_key)
    - donnarec-api/migrations/000004_receipt_number_tables.up.sql (single-row `id BOOLEAN PRIMARY KEY DEFAULT true` config table — analog for receipt_template_config)
    - .planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-PATTERNS.md (Migrations section, lines 252-278)
    - .planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-RESEARCH.md (Open Questions 1-3: column-not-table for pdf ref, sibling table not ALTER for template config)
  </read_first>
  <action>
    000008: ALTER TABLE donations ADD COLUMN donor_language TEXT NOT NULL DEFAULT 'th' CHECK (donor_language IN ('th','en')) — default backfills existing rows per D-55; down drops the column.
    000009: ALTER TABLE outbox_jobs ADD COLUMN next_attempt_at TIMESTAMPTZ NOT NULL DEFAULT now() — enables backoff filtering in the claim query (RESEARCH Pattern 1); recreate idx_outbox_jobs_pending is NOT required (partial index unchanged); down drops the column.
    000010: CREATE TABLE email_delivery with columns id BIGSERIAL PRIMARY KEY, donation_id UUID NOT NULL REFERENCES donations(id), sent_to TEXT, status TEXT NOT NULL CHECK (status IN ('sent','failed','no_email')), provider_message_id TEXT, attempts INT NOT NULL DEFAULT 0, last_error TEXT, created_at TIMESTAMPTZ NOT NULL DEFAULT now(). Copy the GRANT SELECT, INSERT, UPDATE ON email_delivery TO donnarec_app + GRANT USAGE, SELECT ON SEQUENCE email_delivery_id_seq TO donnarec_app footer from 000007. Add index on (donation_id, created_at DESC). Down drops table.
    000011: CREATE TABLE receipt_template_config single-row (id BOOLEAN PRIMARY KEY DEFAULT true, CONSTRAINT single_row CHECK (id = true)) with template_html TEXT NOT NULL DEFAULT '', template_html_en TEXT NOT NULL DEFAULT '', section6_text_th TEXT NOT NULL DEFAULT '', section6_text_en TEXT NOT NULL DEFAULT '', deduction_multiplier TEXT NOT NULL DEFAULT '1x' CHECK (deduction_multiplier IN ('1x','2x')), letterhead_object_key TEXT, seal_object_key TEXT, signature_object_key TEXT, watermark_object_key TEXT, updated_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_by UUID. Seed via INSERT ... DEFAULT VALUES then UPDATE the seeded row to set template_html to a minimal-but-complete Thai receipt HTML skeleton containing the placeholders donor_name, receipt_no, amount, issue_date, section6_text, and letterhead/signature/watermark img slots (so the worker renders a meaningful receipt before any admin edit — D-58 store). Use ON CONFLICT (id) DO NOTHING for idempotency. Grant SELECT, INSERT, UPDATE to donnarec_app. Do NOT alter receipt_number_config (RESEARCH Open Q3). Down drops table.
    000012: ALTER TABLE donations ADD COLUMN receipt_pdf_object_key TEXT (nullable — populated by worker post-render per D-56, RESEARCH Open Q2); down drops the column.
    ทุกไฟล์: header comment block ระบุ D-decision/FR ที่ realize + numbered section comments ตาม convention 000004/000007.
  </action>
  <verify>
    <automated>cd donnarec-api && (docker compose up -d postgres >/dev/null 2>&1 || true) && migrate -path migrations -database "$DATABASE_URL" up && migrate -path migrations -database "$DATABASE_URL" down 5 && migrate -path migrations -database "$DATABASE_URL" up</automated>
  </verify>
  <acceptance_criteria>
    - `migrate up` then `migrate down 5` then `migrate up` all exit 0 (reversible)
    - Query `SELECT template_html FROM receipt_template_config` returns a non-empty string after up (seed applied)
    - `SELECT column_name FROM information_schema.columns WHERE table_name='donations' AND column_name IN ('donor_language','receipt_pdf_object_key')` returns 2 rows
    - `SELECT column_name FROM information_schema.columns WHERE table_name='outbox_jobs' AND column_name='next_attempt_at'` returns 1 row
  </acceptance_criteria>
  <done>Migrations 000008–000012 apply and revert cleanly; receipt_template_config seeded with a usable default template; email_delivery + new columns exist with donnarec_app grants.</done>
</task>

<task type="auto">
  <name>Task 2: sqlc queries — outbox worker claim/mark, email_delivery, settings, donation additions</name>
  <files>donnarec-api/internal/db/queries/outbox.sql, donnarec-api/internal/db/queries/email_delivery.sql, donnarec-api/internal/db/queries/settings.sql, donnarec-api/internal/db/queries/donations.sql</files>
  <read_first>
    - donnarec-api/internal/db/queries/outbox.sql (existing EnqueueOutboxJob — extend, do not replace)
    - donnarec-api/internal/db/queries/receiptno.sql (GetReceiptNumberConfig — single-row config CRUD analog for settings.sql)
    - donnarec-api/internal/db/queries/donations.sql (GetDonationByID lines 64-103 — add new columns to the SELECT list)
    - donnarec-api/sqlc.yaml (codegen config — confirm output package + emit settings)
    - .planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-PATTERNS.md (outbox.sql additions lines 90-131; RESEARCH Pattern 1 claim query)
  </read_first>
  <action>
    outbox.sql — append below EnqueueOutboxJob: ClaimNextOutboxJob :one (RESEARCH Pattern 1 verbatim — UPDATE outbox_jobs SET status='processing', updated_at=now() WHERE id=(SELECT id FROM outbox_jobs WHERE status IN ('pending','failed') AND next_attempt_at <= now() AND attempts < @max_attempts ORDER BY created_at FOR UPDATE SKIP LOCKED LIMIT 1) RETURNING id, job_type, payload, attempts); MarkOutboxJobDone :exec (status='done'); MarkOutboxJobFailed :exec (attempts+1, last_error=@last_error, next_attempt_at=@next_attempt_at, status = CASE WHEN attempts+1 >= @max_attempts THEN 'failed' ELSE 'pending' END). Keep the `-- name: VerbNoun :kind` convention.
    email_delivery.sql (new) — InsertEmailDelivery :one (donation_id, sent_to, status, provider_message_id, attempts, last_error RETURNING id, created_at); GetLatestEmailDeliveryForDonation :one (ORDER BY created_at DESC LIMIT 1) for the Screen 3b panel. File-top comment cites D-57.
    settings.sql (new) — GetReceiptTemplateConfig :one (SELECT the single row); UpdateReceiptTemplateConfig :exec (set all template/section6/1x2x/image-key columns + updated_at=now() + updated_by=@updated_by WHERE id=true). File-top comment cites D-58/D-59.
    donations.sql — add donor_language and receipt_pdf_object_key to the GetDonationByID SELECT column list (and GetReceiptRefByID if it feeds download/resend); add SetReceiptPDFObjectKey :exec (UPDATE donations SET receipt_pdf_object_key=@receipt_pdf_object_key WHERE id=@id) used by the worker after freezing the PDF.
    Then run `sqlc generate` and ensure generated code compiles.
  </action>
  <verify>
    <automated>cd donnarec-api && sqlc generate && go build ./internal/db/...</automated>
  </verify>
  <acceptance_criteria>
    - `sqlc generate` exits 0 and emits ClaimNextOutboxJob, MarkOutboxJobDone, MarkOutboxJobFailed, InsertEmailDelivery, GetLatestEmailDeliveryForDonation, GetReceiptTemplateConfig, UpdateReceiptTemplateConfig, SetReceiptPDFObjectKey as generated Go methods
    - `go build ./internal/db/...` exits 0
    - Generated GetDonationByID row struct includes DonorLanguage and ReceiptPdfObjectKey fields (grep the generated file)
  </acceptance_criteria>
  <done>All new queries generate compiling Go; worker/email/settings/download consumers can call them without further schema work.</done>
</task>

<task type="auto">
  <name>Task 3: config.go — receipts bucket, chrome ws url, worker poll + retry knobs</name>
  <files>donnarec-api/internal/config/config.go</files>
  <read_first>
    - donnarec-api/internal/config/config.go (MinIOConfig block + getEnvStr helpers — analog per PATTERNS lines 282-298)
    - .planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-RESEARCH.md (Common Pitfalls Pitfall 5: 5 attempts, backoff 1m/5m/15m/1h/4h)
  </read_first>
  <action>
    Add to config struct + loader: MinIO.ReceiptsBucket (getEnvStr "MINIO_RECEIPTS_BUCKET" default "donnarec-receipts", per D-56 separate bucket); a Worker config block with WSChromeURL (getEnvStr "CHROME_WS_URL" default "ws://chrome:9222"), PollInterval (parse "WORKER_POLL_INTERVAL" default "5s"), MaxAttempts (getEnvInt "WORKER_MAX_ATTEMPTS" default 5), and a fixed backoff schedule constant BackoffSchedule = [1m,5m,15m,1h,4h] exposed as a package-level var or method ComputeBackoff(attempts) returning the next delay (used by the worker to set next_attempt_at). If no getEnvInt/getEnvDuration helper exists, add one mirroring getEnvStr. Do NOT read secrets here (email provider deferred, D-60).
  </action>
  <verify>
    <automated>cd donnarec-api && go build ./internal/config/... && go vet ./internal/config/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/config/...` exits 0
    - Config exposes ReceiptsBucket, WSChromeURL, PollInterval, MaxAttempts, and a backoff computation reachable by the worker package
    - Defaults resolve without any new required env var (dev boots with zero new config)
  </acceptance_criteria>
  <done>Config surface for the receipts bucket, chrome sidecar URL, and worker poll/retry knobs exists with safe defaults.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app→Postgres (DDL) | migrations run as a privileged role; app runs as donnarec_app with least-privilege grants |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | donnarec_app grants on new tables | medium | mitigate | Grant only SELECT/INSERT/UPDATE (no DELETE) on email_delivery + receipt_template_config, mirroring 000007; audit/immutability preserved |
| T-04-02 | Tampering | outbox next_attempt_at / attempts | low | mitigate | Claim query gates on attempts < max_attempts + next_attempt_at; UNIQUE-free but status CHECK enforces valid states |
| T-04-SC | Tampering | Go module deps | low | accept | No new dependency added in this plan; chromedp pin handled in 04-02 (RESEARCH package-legitimacy audit: OK) |
</threat_model>

<verification>
- `migrate up/down/up` reversible across 000008–000012
- `sqlc generate` + `go build ./...` green
- receipt_template_config seeded with a non-empty default template
</verification>

<success_criteria>
Data layer and config for the entire phase exist; downstream plans (03/05/06/07) can compile against the new queries and config without adding schema.
</success_criteria>

<artifacts_produced>
- Migrations: 000008_donor_language, 000009_outbox_next_attempt_at, 000010_email_delivery, 000011_receipt_template_config, 000012_receipt_pdf_reference (up+down)
- Tables/columns: donations.donor_language, donations.receipt_pdf_object_key, outbox_jobs.next_attempt_at, email_delivery table, receipt_template_config table
- sqlc queries: ClaimNextOutboxJob, MarkOutboxJobDone, MarkOutboxJobFailed, InsertEmailDelivery, GetLatestEmailDeliveryForDonation, GetReceiptTemplateConfig, UpdateReceiptTemplateConfig, SetReceiptPDFObjectKey (+ donor_language/receipt_pdf_object_key added to GetDonationByID)
- Config fields: MinIO.ReceiptsBucket, Worker.WSChromeURL, Worker.PollInterval, Worker.MaxAttempts, backoff schedule
</artifacts_produced>

<output>
Create `.planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-01-SUMMARY.md` when done
</output>
