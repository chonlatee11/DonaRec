---
phase: 04-receipt-pdf-email-delivery-outbox-worker
plan: 08
type: execute
wave: 6
depends_on: ["04-07"]
files_modified:
  - donnarec-web/app/admin/settings/page.tsx
  - donnarec-web/app/api/bff/settings/route.ts
  - donnarec-web/app/api/bff/settings/preview/route.ts
  - donnarec-web/app/api/bff/settings/preview/pdf/route.ts
  - donnarec-web/components/SettingsTabs.tsx
  - donnarec-web/components/TemplateEditor.tsx
  - donnarec-web/components/ImageUploadSlot.tsx
  - donnarec-web/components/NumberFormatEditor.tsx
  - donnarec-web/components/AppShell.tsx
  - donnarec-web/messages/th.json
  - donnarec-web/messages/en.json
autonomous: false
requirements: [FR-33, NFR-09, FR-24]
must_haves:
  truths:
    - "an Admin-only /settings screen lets admins edit template HTML, brand images, §6/1x2x text, and number format across four tabs and save all at once"
    - "a live HTML preview updates 400ms-debounced in a sandboxed iframe (no JS, no network) using TH Sarabun + sample data (D-61)"
    - "a 'Render Real PDF' button renders the current unsaved template via the server sandbox for final fidelity check"
    - "the Settings nav item and route are visible only to Admin"
  artifacts:
    - donnarec-web/app/admin/settings/page.tsx
    - donnarec-web/components/TemplateEditor.tsx
    - donnarec-web/components/SettingsTabs.tsx
  key_links:
    - "BFF settings routes proxy to the 04-07 Admin API (Go re-enforces Admin RBAC)"
  prohibitions:
    - statement: "the preview iframe must be sandboxed with no script/network permissions"
      status: enforced
      verification: "the iframe uses sandbox=\"allow-same-origin\" only (no allow-scripts); info banner states no-JS/no-network"
    - statement: "preview must never use live donor PII — sample/mock data only (D-61)"
      status: enforced
      verification: "the preview request carries no donation id; sample data comes from the server preview endpoint"
---

<objective>
สร้าง Admin Settings UI (Screen 6): แก้ template HTML / รูปแบรนด์ / ข้อความ §6+1x2x / รูปแบบเลข ใน 4 tabs, live HTML preview (sandbox iframe, debounce 400ms, TH Sarabun, sample data) + ปุ่มเรนเดอร์ PDF จริง, บันทึกทีเดียว — Admin-only, ต่อ API ของ 04-07 (D-58/D-61, NFR-09)

Purpose: ปิด no-deploy config loop ให้ผู้ดูแลจริง; D-61 บังคับ editor UX ระดับ production (preview + สมูท) ไม่ใช่ textarea ดิบ
Output: /settings page, SettingsTabs/TemplateEditor/ImageUploadSlot/NumberFormatEditor, BFF settings routes, AppShell nav gate, settings.* messages
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-UI-SPEC.md
@.planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BFF settings routes + Admin nav gate + settings.* messages + install tabs</name>
  <files>donnarec-web/app/api/bff/settings/route.ts, donnarec-web/app/api/bff/settings/preview/route.ts, donnarec-web/app/api/bff/settings/preview/pdf/route.ts, donnarec-web/components/AppShell.tsx, donnarec-web/messages/th.json, donnarec-web/messages/en.json</files>
  <read_first>
    - donnarec-web/app/api/bff/donations/route.ts (bffForward GET/POST proxy analog)
    - donnarec-web/components/AppShell.tsx (role-conditional nav rendering — where to add the Admin-only Settings link)
    - .planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-UI-SPEC.md (copywriting: nav/tab labels; i18n contract settings.* namespace)
  </read_first>
  <action>
    BFF: settings/route.ts GET→bffForward `/api/admin/settings`, PUT→`/api/admin/settings`; settings/preview/route.ts POST→`/api/admin/settings/preview`; settings/preview/pdf/route.ts POST→`/api/admin/settings/preview/pdf` (thin proxies; Go enforces Admin). AppShell: add a "ตั้งค่า" (Settings Lucide icon) nav link rendered only when the session role is Admin (mirror existing role-conditional rendering). Add settings.* keys (tab labels, field labels, helper text, save/validation copy from UI-SPEC) to messages/th.json + en.json. Install the tabs component: `npx shadcn@latest add tabs`.
  </action>
  <verify>
    <automated>cd donnarec-web && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; tabs component installed
    - Settings nav link renders only for Admin role
    - settings.* keys present in both th.json and en.json
    - BFF routes proxy to /api/admin/settings* via bffForward
  </acceptance_criteria>
  <done>The Admin-only settings surface is reachable and localized, backed by proxy routes to the Go Admin API.</done>
</task>

<task type="auto">
  <name>Task 2: Settings page — four tabs, sandboxed live preview + real-PDF button, save-all</name>
  <files>donnarec-web/app/admin/settings/page.tsx, donnarec-web/components/SettingsTabs.tsx, donnarec-web/components/TemplateEditor.tsx, donnarec-web/components/ImageUploadSlot.tsx, donnarec-web/components/NumberFormatEditor.tsx</files>
  <read_first>
    - donnarec-web/app/donations/page.tsx (server component: getServerSession + getTranslations + role decode — analog for the settings page wrapper + Admin guard)
    - donnarec-web/components/DonationListView.tsx (TanStack Query client-component pattern)
    - donnarec-web/components/SlipUploadZone.tsx (drag-drop + magic-byte error visual language — analog for ImageUploadSlot at thumbnail scale)
    - .planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-UI-SPEC.md (Screen 6 full contract: 45/55 split, tabs, 400ms debounce, iframe sandbox, info banner, preview modes, save behavior, accessibility)
  </read_first>
  <action>
    page.tsx: server component under app/admin/settings guarded to Admin (redirect otherwise), loads i18n + session, renders SettingsTabs client component seeded with GET /api/bff/settings. SettingsTabs: shadcn Tabs with four panels (template / images / tax-text / number-format) on the left + a persistent right-hand live-preview pane (45/55 split ≥1024px, stacked below). TemplateEditor: mono textarea (14px/1.6, min 480px) + a sandboxed <iframe sandbox="allow-same-origin"> injected with the current template + TH Sarabun @font-face + sample data returned by POST /api/bff/settings/preview, debounced 400ms; a non-dismissible blue info banner above the preview stating no-JS/no-network; a segmented "ตัวอย่างด่วน (HTML) / เรนเดอร์ PDF จริง" toggle where the real-PDF mode POSTs to /api/bff/settings/preview/pdf and shows a loading skeleton then the PDF inline. ImageUploadSlot: 96×96 thumbnail upload tile (jpg/png, ≤2MB, magic-byte error copy) for letterhead/seal/signature/watermark. NumberFormatEditor: separator + zero-pad inputs with a client-computed live example (2569/000123) + the freeze reassurance note. A single "บันทึกการตั้งค่า" (accent) button PUTs all tabs' values in one request with success/validation toasts per copywriting; aria-live/aria-busy per the accessibility contract.
  </action>
  <verify>
    <automated>cd donnarec-web && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds
    - Four tabs render; save issues one PUT with all tab values
    - Preview iframe uses sandbox="allow-same-origin" only (no allow-scripts) and re-renders debounced at 400ms
    - Real-PDF button calls the preview/pdf BFF route and shows a loading state
    - Number-format tab shows a live example computed client-side
  </acceptance_criteria>
  <done>Admins get a production-grade template editor with smooth sandboxed live preview and an accurate real-PDF check, saving all config in one request.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — Screen 6 Admin Settings editor + sandboxed preview</name>
  <action>Human walkthrough of the /settings editor: Admin-only visibility, four tabs, 400ms debounced sandboxed HTML preview, real-PDF render, image upload validation, and save-all reflected on new receipts while issued receipts stay frozen. No code changes; confirm behavior matches the UI-SPEC Screen 6 contract.</action>
  <what-built>Screen 6 Admin Settings (/settings): four tabs (template HTML, brand images, §6/1x2x tax text, number format), 400ms-debounced sandboxed HTML live preview with TH Sarabun + sample data, a "Render Real PDF" button, and save-all. Admin-only nav + route. Stack: API :8000, chrome sidecar, web :3000.</what-built>
  <how-to-verify>
    1. Log in as Admin; confirm a "ตั้งค่า" nav item appears (and does NOT appear for Maker/Checker).
    2. Open /settings; edit the receipt template HTML — the right-hand preview updates ~400ms after you stop typing, in TH Sarabun, with sample (non-PII) data. Confirm the blue info banner states no-JS/no-network.
    3. Upload a signature/letterhead image (jpg/png) — the 96×96 thumbnail updates; try a non-image or >2MB file and confirm the rejection copy.
    4. Edit the §6 text and switch the 1x/2x select; edit the number-format separator/pad and confirm the live example updates.
    5. Click "เรนเดอร์ PDF จริง" — a real Chromium-rendered sample PDF appears (Thai renders correctly), matching the HTML preview closely.
    6. Click "บันทึกการตั้งค่า" — success toast confirms the change is live for new receipts; issue a fresh receipt (via 04-06 flow) and confirm it reflects the new template. Confirm an already-issued receipt is unchanged (D-56 freeze).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser preview iframe | renders admin HTML in-browser; must not execute JS or reach the network |
| client→BFF→Go Admin API | admin edits proxied server-side; Go re-enforces Admin RBAC |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-24 | Tampering / XSS | in-browser template preview | high | mitigate | iframe sandbox="allow-same-origin" only (no allow-scripts), sample data only, info banner (D-58/D-61) |
| T-04-25 | Elevation of Privilege | settings screen exposure | high | mitigate | Admin-only route guard + nav gate (client) with Go Admin RBAC as the real authority (04-07) |
| T-04-26 | Information Disclosure | preview leaking PII | medium | mitigate | Preview uses server sample fixture; no donation id sent from the client |
</threat_model>

<verification>
- `npm run build` green; iframe sandbox correct; debounced preview + real-PDF button functional
- human UI walkthrough approved (Conventions integration gate for the settings surface)
</verification>

<success_criteria>
Admins can edit and preview the full receipt config in a smooth, sandboxed, Admin-only editor and save with no deploy — confirmed by a human walkthrough.
</success_criteria>

<artifacts_produced>
- app/admin/settings/page.tsx (Admin-guarded server component)
- BFF: settings, settings/preview, settings/preview/pdf routes
- Components: SettingsTabs, TemplateEditor (sandboxed debounced preview + real-PDF toggle), ImageUploadSlot, NumberFormatEditor
- AppShell Admin-only Settings nav link; settings.* messages (th/en); shadcn tabs installed
</artifacts_produced>

<output>
Create `.planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-08-SUMMARY.md` when done
</output>
