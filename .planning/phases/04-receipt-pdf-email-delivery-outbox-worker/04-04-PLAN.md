---
phase: 04-receipt-pdf-email-delivery-outbox-worker
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - donnarec-api/internal/mailer/sender.go
  - donnarec-api/internal/mailer/dev_sender.go
  - donnarec-api/internal/mailer/dev_sender_test.go
  - donnarec-api/internal/i18n/locales/th.json
  - donnarec-api/internal/i18n/locales/en.json
  - donnarec-api/internal/i18n/receipt_messages_test.go
autonomous: true
requirements: [FR-25, FR-26, FR-23]
must_haves:
  truths:
    - "EmailSender is an interface with a single Send(ctx, Message) (SendResult, error) method (D-60 swappable seam)"
    - "DevSender writes body + attachment to a local capture location and never performs a real network send"
    - "go-i18n bundle resolves receipt + email message IDs for both th and en"
  artifacts:
    - donnarec-api/internal/mailer/sender.go
    - donnarec-api/internal/mailer/dev_sender.go
    - donnarec-api/internal/i18n/locales/th.json
    - donnarec-api/internal/i18n/locales/en.json
  key_links:
    - "worker (04-05) depends on EmailSender + the i18n receipt/email message IDs"
  prohibitions:
    - statement: "no self-hosted SMTP / real network send in this phase (D-60, CLAUDE.md)"
      status: enforced
      verification: "DevSender has no net/smtp or provider SDK import; dev_sender_test.go asserts capture-to-disk only"
---

<objective>
สร้าง EmailSender interface (D-60) + dev/local capture implementation + เพิ่ม i18n message IDs สำหรับข้อความใบเสร็จและอีเมล 2 ภาษา — seam ที่ worker ใช้ส่งอีเมลโดยยังไม่ผูก provider จริง (SES/Postmark เสียบภายหลัง)

Purpose: แยกการส่งอีเมลออกจาก worker ผ่าน interface เดียว เพื่อสลับ provider ได้ภายหลังโดยไม่แก้ worker; catalog เดียว (go-i18n) ขับทั้ง PDF label และ email subject/body ตาม donor_language (FR-23/26)
Output: internal/mailer/{sender,dev_sender}.go + tests, i18n receipt/email keys
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-RESEARCH.md
@.planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swappable EmailSender seam + bilingual receipt/email i18n</name>
  <files>donnarec-api/internal/mailer/sender.go, donnarec-api/internal/mailer/dev_sender.go, donnarec-api/internal/mailer/dev_sender_test.go, donnarec-api/internal/i18n/locales/th.json, donnarec-api/internal/i18n/locales/en.json, donnarec-api/internal/i18n/receipt_messages_test.go</files>
  <read_first>
    - donnarec-api/internal/storage/client.go (file-top "design decisions realized here" doc-comment convention to mirror in sender.go)
    - donnarec-api/internal/i18n/bundle.go + locales/th.json,en.json (existing go-i18n message format — add keys in the same shape, do not change setup)
    - .planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-RESEARCH.md (Code Examples: EmailSender interface + DevSender)
  </read_first>
  <behavior>
    - RED dev_sender_test.go: DevSender.Send with a Message carrying a PDF Attachment writes body.html + the attachment file to its OutDir and returns a SendResult with a non-zero SentAt and empty ProviderMessageID; no network call occurs.
    - RED receipt_messages_test.go: the i18n bundle Localize returns a non-empty, different string for a representative receipt/email message ID (e.g. email.subject, receipt.header) under localizer "th" vs "en".
    - GREEN: implement the interface + dev sender + add message IDs to both locale files.
  </behavior>
  <action>
    sender.go: define Message{To, Subject, BodyHTML, BodyText, Attachment}, Attachment{Filename, ContentType, Data}, SendResult{ProviderMessageID, SentAt}, and interface EmailSender{ Send(ctx, Message) (SendResult, error) } (RESEARCH Code Examples verbatim shape). File-top doc comment lists D-60 + the no-SMTP rule (PATTERNS storage/client.go header convention). dev_sender.go: DevSender{OutDir string}; Send() creates a per-message dir and writes body.html + attachment bytes (RESEARCH dev_sender.go). Zero external deps. i18n: add receipt.* (header, donor_label, amount_label, receipt_no_label, date_label) and email.* (subject, greeting, body, footer) keys to th.json and en.json using the existing go-i18n message format. Do not change internal/i18n/bundle.go setup.
  </action>
  <verify>
    <automated>cd donnarec-api && go test ./internal/mailer/... ./internal/i18n/...</automated>
  </verify>
  <acceptance_criteria>
    - dev_sender_test.go green: capture-to-disk, no provider/net import
    - receipt_messages_test.go green: th vs en differ and are non-empty for receipt/email IDs
    - `go build ./internal/mailer/... ./internal/i18n/...` exits 0
  </acceptance_criteria>
  <done>The worker has one interface to send bilingual receipt emails with a PDF attachment, backed by a dev capture implementation and no production SMTP path.</done>
</task>

</tasks>

<verification>
- `go test ./internal/mailer/... ./internal/i18n/...` green (RED→GREEN)
- DevSender captures to disk, no provider SDK import
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker→EmailSender | attachment bytes (PDF) + donor email cross into the sender implementation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Information Disclosure | DevSender capture dir | medium | mitigate | Dev-only capture writes to a configured OutDir (non-web-served); production provider deferred (D-60); no PII in logs (Pattern C) |
| T-04-06 | Spoofing | future real provider | low | transfer | SPF/DKIM/DMARC is an ops item at provider-selection time (D-60 stakeholder gate) — documented, out of scope this phase |
</threat_model>

<success_criteria>
The worker has a single interface to send bilingual receipt emails with a PDF attachment, backed by a dev capture implementation, with no production SMTP path.
</success_criteria>

<artifacts_produced>
- internal/mailer/sender.go: Message, Attachment, SendResult, EmailSender interface
- internal/mailer/dev_sender.go: DevSender (capture-to-disk)
- i18n message IDs: receipt.* and email.* in th.json + en.json
</artifacts_produced>

<output>
Create `.planning/phases/04-receipt-pdf-email-delivery-outbox-worker/04-04-SUMMARY.md` when done
</output>
