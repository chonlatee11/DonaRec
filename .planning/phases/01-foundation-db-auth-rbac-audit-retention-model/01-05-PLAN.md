---
phase: 01-foundation-db-auth-rbac-audit-retention-model
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - donnarec-api/docker-compose.yml
  - donnarec-api/internal/auth/middleware.go
  - donnarec-api/internal/config/config.go
  - donnarec-api/cmd/server/main.go
  - donnarec-api/.env.example
autonomous: false
gap_closure: true
requirements: [NFR-01, FR-34]
must_haves:
  truths:
    - "หลัง `docker compose down -v && docker compose up -d --wait` จากศูนย์ (ไม่มีการรัน make migrate-up เอง) → ทุก service healthy และ donnarec_app มีครบ 5 ตาราง (users, user_roles, retention_config, audit_log, schema_migrations)"
    - "POST /api/admin/users สำเร็จบน cold-start stack โดยไม่มี error 'relation \"audit_log\"/\"users\" does not exist'"
    - "migrate service เป็น idempotent — `docker compose up` ซ้ำบน volume เดิมไม่ fail (golang-migrate เห็น schema_migrations แล้ว no-op)"
    - "token ที่ขอผ่าน http://localhost:8080/realms/donnarec/protocol/openid-connect/token verify ผ่านที่ API (200 บน protected endpoint ที่ role ถูกต้อง) โดยไม่ต้องใช้ Host: keycloak:8080 workaround"
    - "token ที่ signature ผิด / expired / aud ผิด ยังถูก reject 401 (issuer เป็น configurable แต่ signature/aud/expiry ไม่ถูก skip)"
  artifacts:
    - path: "donnarec-api/docker-compose.yml"
      provides: "migrate init-service (image migrate/migrate) + api depends_on service_completed_successfully + OIDC_ISSUER env บน api"
      contains: "migrate/migrate"
    - path: "donnarec-api/internal/auth/middleware.go"
      provides: "verifier ที่สร้าง provider จาก discovery URL ภายใน แต่ validate issuer = expected issuer ที่ตั้งค่าได้"
      contains: "InsecureIssuerURLContext"
    - path: "donnarec-api/internal/config/config.go"
      provides: "OIDC expected-issuer env (OIDC_ISSUER) พร้อม default ที่ปลอดภัย"
      contains: "OIDC_ISSUER"
  key_links:
    - from: "donnarec-api/docker-compose.yml (api service)"
      to: "migrate init-service"
      via: "depends_on migrate condition: service_completed_successfully"
      pattern: "service_completed_successfully"
    - from: "donnarec-api/docker-compose.yml (migrate service)"
      to: "donnarec-api/migrations/"
      via: "bind mount ./migrations เข้า container migrate/migrate"
      pattern: "/migrations"
    - from: "donnarec-api/internal/auth/middleware.go"
      to: "OIDC_ISSUER config value"
      via: "oidc.InsecureIssuerURLContext(ctx, expectedIssuer) + provider.Verifier(ClientID)"
      pattern: "InsecureIssuerURLContext"
---

<objective>
ปิด 2 infra gap (severity: major) จาก 01-UAT.md (status: diagnosed) ที่ทำให้ Phase 01 stack ใช้งาน end-to-end จริงไม่ได้หลัง cold start แม้ทุก unit/RBAC logic จะผ่าน 6/6 UAT แล้ว. ทั้งสอง gap center อยู่ที่ docker-compose.yml (live stack usability) + การแก้ OIDC verifier ฝั่ง Go.

Gaps ที่ปิด:
- GAP 1 (major) — ไม่มี migration runner ใน cold-start path: live donnarec_app ว่างเปล่า (ไม่มีแม้ schema_migrations) จนกว่าจะ `make migrate-up` เอง → POST /api/admin/users + audit ล้มด้วย "relation does not exist". แก้: เพิ่ม migrate init-service (image migrate/migrate) ที่รัน up ก่อน api start (LOCKED USER DECISION).
- GAP 2 (major) — OIDC issuer hostname mismatch: API ทำ discovery ที่ http://keycloak:8080 → verifier บังคับ iss=http://keycloak:8080/...; แต่ token ที่ browser ขอผ่าน localhost:8080 มี iss=http://localhost:8080/... → 401 ทุก token. แก้: ฝั่ง go-oidc — discovery ที่ internal URL แต่ validate issuer = expected issuer ที่ configurable (LOCKED USER DECISION).

Purpose: ให้ cold-start stack ใช้งานได้จริง — login ผ่าน localhost + endpoint ที่แตะ DB ทำงานได้ — ปลดบล็อก NFR-01 (login + RBAC end-to-end) และ FR-34 (admin user mgmt) บน stack จริง
Output: docker-compose.yml (migrate service + OIDC_ISSUER env), middleware.go (configurable issuer verifier), config.go (OIDC_ISSUER), main.go (ส่ง expected issuer เข้า middleware), .env.example (เอกสาร OIDC_ISSUER); cold-start stack ที่ใช้งานได้โดยไม่มี manual workaround
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-db-auth-rbac-audit-retention-model/01-UAT.md
@.planning/phases/01-foundation-db-auth-rbac-audit-retention-model/01-04-PLAN.md

<interfaces>
<!-- สัญญา/identifier ที่ executor ต้องใช้ — สกัดจาก codebase แล้ว ไม่ต้องไป explore เพิ่ม -->

จาก donnarec-api/internal/auth/middleware.go (current OIDC setup ที่ต้องแก้):
- func NewAuthMiddleware(keycloakBaseURL, realm, clientID string, logger *zap.Logger) (*AuthMiddleware, error)
- providerURL := fmt.Sprintf("%s/realms/%s", keycloakBaseURL, realm)  // discovery URL (ภายใน docker = http://keycloak:8080/realms/donnarec)
- provider, _ := oidc.NewProvider(ctx, providerURL)
- verifier := provider.Verifier(&oidc.Config{ClientID: clientID})    // บังคับ aud = clientID อยู่แล้ว — ห้ามถอด
- import "github.com/coreos/go-oidc/v3/oidc"

จาก donnarec-api/internal/config/config.go (config loader ที่ต้องเพิ่ม field):
- type Config struct { ... KeycloakBaseURL, KeycloakRealm, KeycloakClientID string ... }
- helper ที่มีอยู่: getEnvStr(key, fallback string) string
- Load() อ่าน env → validate(); validate() เช็ค required map

จาก donnarec-api/cmd/server/main.go (call site ที่ต้องส่ง param ใหม่):
- authMW, err := auth.NewAuthMiddleware(cfg.KeycloakBaseURL, cfg.KeycloakRealm, cfg.KeycloakClientID, logger)

จาก Makefile (รูปแบบคำสั่ง migrate ที่มีอยู่ — ใช้เป็น reference สำหรับ args ของ migrate container):
- migrate -path migrations -database "$DATABASE_URL" up
</interfaces>

<notes>
- docker-compose.yml อยู่ที่ donnarec-api/ ; migrations/ อยู่ที่ donnarec-api/migrations/ → bind mount ใช้ ./migrations (relative จาก compose ถูกต้อง ต่างจาก scripts ที่อยู่ repo-root ต้องใช้ ../scripts/)
- migrations 3 ไฟล์ up สร้าง 4 ตาราง (users, user_roles, retention_config, audit_log) + golang-migrate สร้าง schema_migrations เอง = 5 ตาราง
- 01-04 ปิด 5 gap boot ไปแล้ว (KEK quoting, scripts path, realm JSON, KC healthcheck) — ห้าม regress; ใช้ Edit ราย hunk เท่านั้น อย่าเขียนทับทั้งไฟล์
- LOCKED: ห้ามผูก migration เข้า main.go startup / Go binary — ต้องเป็น service แยกใน compose (per user decision GAP 1)
- LOCKED: แก้ฝั่ง go-oidc ไม่ใช่เปลี่ยน KC hostname strategy; issuer ต้อง configurable ผ่าน env (per user decision GAP 2)
- DATABASE_URL ใน docker network ใช้ host=postgres, sslmode=disable (local-dev) — migrate container ต้องใช้ connection string เดียวกัน (host postgres ภายใน network)
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: เพิ่ม migrate init-service ใน docker-compose.yml (GAP 1)</name>
  <files>donnarec-api/docker-compose.yml</files>
  <read_first>
- donnarec-api/docker-compose.yml (current state — service postgres/keycloak/api, api depends_on)
- donnarec-api/Makefile (migrate-up target — รูปแบบ args: -path migrations -database "$DATABASE_URL" up)
- .planning/phases/01-foundation-db-auth-rbac-audit-retention-model/01-04-PLAN.md (convention การแก้ compose ราย hunk)
  </read_first>
  <action>
เพิ่ม service ใหม่ชื่อ `migrate` ใน donnarec-api/docker-compose.yml และผูก api ให้รอ migrate เสร็จก่อน (per LOCKED USER DECISION GAP 1 — แยกจาก main.go เด็ดขาด):

1. เพิ่ม service `migrate` (วางหลัง postgres ก่อน api เพื่ออ่านง่าย):
   - image: `migrate/migrate` (pin tag ที่ตรงกับ Makefile: v4.19.1 → `migrate/migrate:v4.19.1`)
   - depends_on: postgres ด้วย `condition: service_healthy`
   - volumes: bind mount `./migrations:/migrations:ro` (migrations อยู่ donnarec-api/migrations → relative ./ ถูกต้อง)
   - command/entrypoint: รัน golang-migrate up ต่อ donnarec_app — args `["-path", "/migrations", "-database", "<conn>", "up"]` โดย `<conn>` = `postgres://${DB_USER:-donnarec}:${DB_PASSWORD}@postgres:5432/donnarec_app?sslmode=disable` (host=postgres ภายใน network ตรงกับ api DATABASE_URL). ใช้ env var DATABASE_URL บน service migrate แล้วอ้างใน command หรือใส่ตรงใน command ก็ได้ — เลือกแบบที่ docker compose config parse ผ่าน
   - restart: `on-failure` หรือไม่ตั้ง restart (init-service ต้องจบแล้วหยุด — ห้าม `unless-stopped` เพราะ migrate up จบแล้ว exit 0 จะถูก restart วน). ตั้งให้ container exit 0 หลัง up สำเร็จ
   - idempotent: golang-migrate ติดตาม schema_migrations เอง → up ซ้ำบน schema ที่ migrate แล้ว = no change, exit 0 (safe ทุก up). ไม่ต้องเพิ่ม logic เอง

2. แก้ service `api` depends_on: เพิ่ม `migrate:` ด้วย `condition: service_completed_successfully` (คงของเดิม postgres: service_healthy, keycloak: service_healthy ไว้). ผลคือ api จะ start ก็ต่อเมื่อ migrate จบด้วย exit 0

3. อัปเดต usage comment ส่วนหัวไฟล์ (รายการ Services + ขั้นตอน) ให้สะท้อนว่า migration รันอัตโนมัติโดย service migrate — ลบ/แก้คำแนะนำให้รัน make migrate-up เอง (ถ้ามี)

ห้ามแตะ DATABASE_URL ของ api (sslmode=disable เป็น local-dev decision), ห้าม regress fix ของ 01-04 (KEK quoting, ../scripts/ path, KC_HEALTH_ENABLED, /dev/tcp healthcheck). ใช้ Edit ราย hunk.
  </action>
  <verify>
    <automated>cd donnarec-api && docker compose config -q && echo COMPOSE_PARSE_OK && grep -c "migrate/migrate" docker-compose.yml && grep -c "service_completed_successfully" docker-compose.yml && grep -c "/migrations" docker-compose.yml && grep -c "../scripts/create-multiple-dbs.sh" docker-compose.yml && grep -c "KC_HEALTH_ENABLED" docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
- `docker compose config -q` exit 0 (ไม่มี YAML/schema error)
- มี service `migrate` ใช้ image `migrate/migrate` (pinned tag), bind mount `/migrations`, depends_on postgres: service_healthy
- service `api` depends_on `migrate` ด้วย `condition: service_completed_successfully`
- migrate service ไม่ใช้ `restart: unless-stopped` (เพื่อไม่ให้ restart วนหลัง exit 0)
- 01-04 fixes ยังอยู่ครบ: `../scripts/create-multiple-dbs.sh` (1), `KC_HEALTH_ENABLED` (>=1), `/dev/tcp/localhost/9000` ยังคงอยู่
- DATABASE_URL ของ api ยังคง sslmode=disable เดิม
  </acceptance_criteria>
  <done>docker-compose.yml มี migrate init-service ที่รัน golang-migrate up ต่อ donnarec_app ก่อน api start; parse ผ่าน; ไม่ regress 01-04</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ทำให้ OIDC expected-issuer configurable (GAP 2) — config + middleware + main + env</name>
  <files>donnarec-api/internal/config/config.go, donnarec-api/internal/auth/middleware.go, donnarec-api/cmd/server/main.go, donnarec-api/.env.example, donnarec-api/docker-compose.yml</files>
  <read_first>
- donnarec-api/internal/auth/middleware.go (NewAuthMiddleware — provider/verifier setup ที่ต้องแก้)
- donnarec-api/internal/config/config.go (Load/validate/getEnvStr — รูปแบบ field + default)
- donnarec-api/cmd/server/main.go (บรรทัด ~76 call site auth.NewAuthMiddleware)
- donnarec-api/.env.example (รูปแบบ comment + KEYCLOAK_* block)
- donnarec-api/docker-compose.yml (api environment block — KEYCLOAK_BASE_URL อยู่ตรงนี้)
- <security_threat_model> ใน planning context (ห้าม skip signature/aud/expiry; ห้ามใช้ SkipIssuerCheck)
  </read_first>
  <action>
แก้ฝั่ง go-oidc ให้ discovery ใช้ internal URL แต่ validate issuer = expected issuer ที่ configurable (per LOCKED USER DECISION GAP 2). เป้าหมาย: token ที่ขอผ่าน localhost:8080 (browser/Phase 6) verify ผ่าน โดย API ยัง discovery ผ่าน docker network ที่ keycloak:8080 ได้ — ไม่มี Host-header workaround.

config.go:
- เพิ่ม field `KeycloakIssuer string` (หรือชื่อ OIDCIssuer) ใน Config struct
- โหลดจาก env `OIDC_ISSUER` ผ่าน getEnvStr; default ที่ปลอดภัย: ถ้าไม่ตั้ง ให้ fallback เป็น issuer ที่อิง KeycloakBaseURL + realm (`<KeycloakBaseURL>/realms/<realm>`) เพื่อคง behavior เดิม (ไม่ตั้ง = ตรวจ issuer ตาม discovery URL เดิม). ห้ามปล่อยว่างแบบที่ทำให้ issuer check ถูก skip
- เอกสาร field ใน comment ว่า dev = http://localhost:8080/realms/donnarec, prod = https://<domain>/realms/donnarec

middleware.go:
- เปลี่ยน signature NewAuthMiddleware ให้รับ expectedIssuer เพิ่ม (เช่น keycloakBaseURL, realm, clientID, expectedIssuer string, logger)
- ใช้ `oidc.InsecureIssuerURLContext(ctx, expectedIssuer)` สร้าง context แล้วส่งเข้า `oidc.NewProvider(ctx, providerURL)` โดย providerURL ยังเป็น internal discovery URL (`<keycloakBaseURL>/realms/<realm>`). กลไกนี้ทำให้ go-oidc discovery ที่ internal URL แต่ provider.issuer ถูกตั้งเป็น expectedIssuer → verifier จะ validate iss = expectedIssuer
- คง `provider.Verifier(&oidc.Config{ClientID: clientID})` ไว้เหมือนเดิม — aud check (Pitfall 3) + signature (JWKS) + expiry ยังบังคับครบ. ห้ามใช้ SkipIssuerCheck / SkipClientIDCheck / SkipExpiryCheck
- อัปเดต doc comment ของ struct/constructor ให้ระบุว่า issuer validate ต่อ expectedIssuer (configurable) แทน auto-enforce ตาม discovery URL

main.go:
- แก้ call site auth.NewAuthMiddleware ส่ง cfg.KeycloakIssuer (expected issuer) เป็น param ใหม่

.env.example:
- เพิ่ม `OIDC_ISSUER=http://localhost:8080/realms/donnarec` ใต้ KEYCLOAK block พร้อม comment อธิบายว่า = issuer ที่ token ฝั่ง browser (localhost) ถือมา; ต้องตรงกับ iss ใน token ที่ Keycloak mint; prod เปลี่ยนเป็น https domain จริง

docker-compose.yml (api environment):
- เพิ่ม `OIDC_ISSUER: ${OIDC_ISSUER:-http://localhost:8080/realms/donnarec}` ใน api environment (ค่าที่ browser/Phase 6 จะถือมา). คง KEYCLOAK_BASE_URL: http://keycloak:8080 ไว้ (ใช้เป็น discovery URL ภายใน)

ห้ามใส่ fenced code; ใช้ Edit ราย hunk; รักษา import ordering ของ Go (gofmt). หลังแก้ Go ต้อง build/test ผ่าน (verify ด้านล่าง).
  </action>
  <verify>
    <automated>cd donnarec-api && grep -c "OIDC_ISSUER" .env.example && grep -c "OIDC_ISSUER" docker-compose.yml && grep -c "InsecureIssuerURLContext" internal/auth/middleware.go && grep -vE '^\s*//' internal/auth/middleware.go | grep -cE "SkipIssuerCheck|SkipClientIDCheck|SkipExpiryCheck" | grep -qx 0 && echo NO_SKIP_FLAGS && go build ./... && go test ./internal/auth/... ./internal/config/... -count=1</automated>
  </verify>
  <acceptance_criteria>
- config.go มี field issuer ที่โหลดจาก env `OIDC_ISSUER` พร้อม safe default (fallback อิง KeycloakBaseURL+realm, ไม่ปล่อยว่าง)
- middleware.go ใช้ `oidc.InsecureIssuerURLContext` และยังเรียก `provider.Verifier(&oidc.Config{ClientID: clientID})` (aud check คงอยู่)
- ไม่มี SkipIssuerCheck / SkipClientIDCheck / SkipExpiryCheck ใน middleware.go (นอก comment) → grep filtered = 0
- main.go ส่ง expected issuer เข้า NewAuthMiddleware
- .env.example และ docker-compose.yml (api env) มี OIDC_ISSUER
- `go build ./...` exit 0; `go test ./internal/auth/... ./internal/config/...` ผ่าน (ไม่มี FAIL)
  </acceptance_criteria>
  <done>OIDC verifier discovery ที่ internal URL แต่ validate iss = expected issuer ที่ configurable ผ่าน OIDC_ISSUER; signature/aud/expiry ยังบังคับ; build + auth/config tests ผ่าน</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Cold-start + live end-to-end verification (Docker required)</name>
  <files>(ไม่มีไฟล์ใหม่ — live verification เท่านั้น)</files>
  <read_first>
- donnarec-api/docker-compose.yml (state หลัง Task 1+2)
- donnarec-api/.env.example (env ที่ต้องมีใน .env)
- .planning/phases/01-foundation-db-auth-rbac-audit-retention-model/01-UAT.md (acceptance-proof target ของทั้ง 2 gap)
- Bruno/DonaRec/ (collection RBAC ที่มีอยู่ — ใช้ verify POST /api/admin/users ได้ โดยไม่ต้องใส่ Host workaround อีกต่อไป)
  </read_first>
  <what-built>
Task 1 เพิ่ม migrate init-service (migrations รันอัตโนมัติก่อน api), Task 2 ทำให้ OIDC issuer configurable. งานอัตโนมัติทั้งหมดเสร็จแล้ว — checkpoint นี้ยืนยันบน live stack จริง (ต้องมี Docker running) ว่า cold-start ใช้งานได้ end-to-end โดยไม่มี manual workaround
  </what-built>
  <action>
รัน live verification จาก donnarec-api/ (ต้องมี Docker daemon + .env ที่มี DB_PASSWORD, KC_ADMIN_PASSWORD, DONAREC_KEK (openssl rand -hex 32), OIDC_ISSUER=http://localhost:8080/realms/donnarec):

PREREQUISITE — ต้องใช้ `--build` flag และ setup-test-users.sh:
- ใช้ `docker compose up -d --build` (ต้องมี `--build` เสมอ เพื่อ compile Go OIDC code ใหม่เข้า api image; `up -d` ไม่มี `--build` จะใช้ image เก่าก่อนแก้ไข)
- หลัง services healthy แล้ว ต้องรัน `bash Bruno/setup-test-users.sh` (จาก repo root) เพื่อสร้าง test Keycloak client + users ที่ใช้ในการ verify (script นี้สร้าง donnarec-test-cli + test users ให้อัตโนมัติ)

Client/user ที่ถูกต้องสำหรับ token:
- `client_id` ต้องใช้ `donnarec-test-cli` (สร้างโดย setup-test-users.sh; มี directAccessGrantsEnabled=true + audience mapper ฉีด aud=donnarec-backend) — ห้ามใช้ `donnarec-backend` เพราะ bearerOnly=true ออก token ไม่ได้
- user ต้องใช้ `admin-test@donnarec.local` / password `TestPass123` (seeded โดย setup-test-users.sh) — ห้ามใช้ `admin/changeme` (นั่นคือ master-realm Keycloak admin ไม่ใช่ donnarec realm)

GAP 1 proof (cold-start migration):
1. `docker compose down -v` — ล้าง volume เก่า (donnarec_app ต้องเริ่มว่าง)
2. `docker compose up -d --build --wait` — รอทุก service (postgres, keycloak, migrate→completed, api) healthy/completed โดย **ไม่รัน make migrate-up เอง** (ต้องมี `--build` เพื่อ compile Go code ใหม่)
3. ยืนยัน 5 ตาราง: `docker compose exec -T postgres psql -U donnarec -d donnarec_app -c '\dt'` ต้องเห็น users, user_roles, retention_config, audit_log และ schema_migrations
4. ยืนยัน migrate service จบ exit 0: `docker compose ps -a` แสดง migrate state = exited (0) / completed

GAP 2 proof (issuer ผ่าน localhost ไม่มี Host workaround):
5. สร้าง test client + users: `bash Bruno/setup-test-users.sh` (จาก repo root) — จำเป็นต้องรันหลัง cold-start ทุกครั้ง
6. ขอ token: POST http://localhost:8080/realms/donnarec/protocol/openid-connect/token
   body: grant_type=password&client_id=donnarec-test-cli&username=admin-test@donnarec.local&password=TestPass123
   (token จะมี iss=http://localhost:8080/realms/donnarec และ aud=donnarec-backend)
7. ยิง POST /api/admin/users ด้วย token นั้น **โดยไม่ใส่ header Host: keycloak:8080**
   body JSON: {"email":"test@example.com","display_name":"Test User","keycloak_subject":"some-uuid","roles":["maker"]}
   → ต้องได้ 201 พิสูจน์ทั้ง GAP 1 (ตารางมีจริง) และ GAP 2 (issuer ผ่าน) พร้อมกัน
8. negative: token expired / aud ผิด / signature ผิด → ยังได้ 401; maker/checker token → 403; no token → 401 (RBAC ไม่ regress)
  </action>
  <how-to-verify>
**PREREQUISITES (ต้องรันก่อน):**
- รัน `docker compose down -v && docker compose up -d --build --wait` (ต้องมี `--build` — ไม่มีจะใช้ image Go เก่า)
- รัน `bash Bruno/setup-test-users.sh` (จาก repo root) หลัง services healthy เพื่อสร้าง donnarec-test-cli + test users

1. `cd donnarec-api && docker compose down -v && docker compose up -d --build --wait` → exit 0, ทุก service healthy, migrate = completed
2. `docker compose exec -T postgres psql -U donnarec -d donnarec_app -c '\dt'` → เห็น 5 ตาราง (users, user_roles, retention_config, audit_log, schema_migrations) โดยไม่ได้รัน make migrate-up
3. ขอ token: POST localhost:8080 ด้วย `client_id=donnarec-test-cli` + `username=admin-test@donnarec.local` + `password=TestPass123` (ไม่ใช่ donnarec-backend/admin/changeme)
4. POST /api/admin/users body `{"email":"...","display_name":"...","keycloak_subject":"...","roles":["maker"]}` **ไม่มี Host header** → 201
5. negative: token expired/wrong-aud → 401; maker/checker → 403; no token → 401
  </how-to-verify>
  <acceptance_criteria>
- `docker compose up -d --wait` exit 0 จาก cold start (หลัง down -v) โดยไม่มีการรัน migrate เอง
- `psql -d donnarec_app -c '\dt'` list ครบ users, user_roles, retention_config, audit_log, schema_migrations
- migrate service exit code = 0 (completed) ใน docker compose ps -a
- POST /api/admin/users ด้วย admin token จาก localhost:8080 (NO Host: keycloak:8080) → 201, audit_log มี action=user.create
- token expired/wrong-audience/bad-signature → 401; maker/checker → 403; no-token → 401
  </acceptance_criteria>
  <resume-signal>พิมพ์ "approved" ถ้าทุกข้อผ่าน หรือระบุข้อที่ fail (พร้อม docker compose logs ของ service ที่เกี่ยว) เพื่อ re-plan</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/Phase 6 → API | client ส่ง Bearer JWT ที่ขอจาก Keycloak ผ่าน localhost:8080 — untrusted จนกว่าจะ verify |
| API → Keycloak (docker network) | API ทำ OIDC discovery + JWKS fetch ที่ http://keycloak:8080 (internal, plaintext HTTP ใน docker network) |
| migrate-service → Postgres | init container เชื่อม postgres:5432 ด้วย DB credential เพื่อ apply DDL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-G2-01 | Spoofing | OIDC verifier (configurable issuer) | mitigate | issuer becomes configurable ผ่าน OIDC_ISSUER แต่ verifier ยังบังคับ signature (JWKS), aud=clientID, expiry; ห้ามใช้ SkipIssuerCheck — validate iss = expected issuer ที่ตั้งไว้ (grep gate ยืนยันไม่มี Skip* flags) |
| T-01-G2-02 | Tampering | JWT signature | mitigate | provider.Verifier ตรวจ signature ผ่าน JWKS จาก discovery; การแก้ issuer source ไม่แตะ JWKS path → token ปลอม/แก้ signature ยัง reject |
| T-01-G2-03 | Elevation of Privilege | misconfigured OIDC_ISSUER ว่าง/ผิด | mitigate | safe default: OIDC_ISSUER ไม่ตั้ง → fallback เป็น issuer อิง KeycloakBaseURL+realm (คง behavior เดิม), ไม่ปล่อยให้ issuer check ถูก skip; documented ใน .env.example |
| T-01-G1-01 | Tampering | migrate init-service DDL | accept | DDL มาจาก migrations/ ที่ commit ใน repo (read-only mount :ro); ผู้มีสิทธิ์แก้ migrations = ผู้มีสิทธิ์ deploy อยู่แล้ว; risk เท่าเดิมกับ make migrate-up |
| T-01-G1-02 | Denial of Service | migrate restart loop | mitigate | migrate ตั้งไม่ใช้ restart: unless-stopped → exit 0 แล้วหยุด ไม่ restart วน; api รอผ่าน service_completed_successfully |
| T-01-SC | Tampering | docker image migrate/migrate | mitigate | pin tag (v4.19.1 ตรง Makefile) ไม่ใช้ :latest; image จาก official golang-migrate org บน Docker Hub |
</threat_model>

<verification>
- `cd donnarec-api && docker compose config -q` → ไม่มี error; migrate service + service_completed_successfully ปรากฏ
- `go build ./...` exit 0; `go test ./internal/auth/... ./internal/config/...` ผ่าน
- grep filtered: ไม่มี SkipIssuerCheck/SkipClientIDCheck/SkipExpiryCheck ใน middleware.go
- cold start: `docker compose down -v && docker compose up -d --wait` → exit 0, migrate completed, 5 ตารางใน donnarec_app โดยไม่รัน make migrate-up
- live: token จาก localhost:8080 + POST /api/admin/users (no Host workaround) → 201; expired/wrong-aud → 401; maker/checker → 403

Atomic commits ที่แนะนำ:
- commit 1: `fix(01-05): add migrate init-service for cold-start migrations (GAP 1)`
- commit 2: `fix(01-05): make OIDC expected issuer configurable via OIDC_ISSUER (GAP 2)`
</verification>

<success_criteria>
- GAP 1 ปิด: cold-start stack มีครบ 5 ตารางอัตโนมัติ (migrate init-service) โดยไม่ต้อง make migrate-up; POST /api/admin/users ไม่เจอ "relation does not exist"
- GAP 2 ปิด: token จาก localhost:8080 verify ผ่านที่ API โดยไม่มี Host: keycloak:8080 workaround; signature/aud/expiry ยังบังคับ (negative cases ยัง 401)
- ไม่ regress fix ของ 01-04 (KEK quoting, ../scripts/ path, KC healthcheck) และ RBAC logic (maker/checker 403, no-token 401)
- migration ไม่ผูกกับ main.go (แยกเป็น compose service ตาม LOCKED decision)
- NFR-01 (login + RBAC end-to-end) และ FR-34 (admin user mgmt) ใช้งานได้บน cold-start stack จริง
</success_criteria>

<output>
Create `.planning/phases/01-foundation-db-auth-rbac-audit-retention-model/01-05-SUMMARY.md` when done
</output>
</content>
</invoke>
