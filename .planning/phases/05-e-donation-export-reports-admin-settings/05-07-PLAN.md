---
phase: 05-e-donation-export-reports-admin-settings
plan: 07
type: execute
wave: 6
depends_on: [05-05, 05-06]
files_modified:
  - donnarec-web/app/reports/page.tsx
  - donnarec-web/app/api/bff/reports/route.ts
  - donnarec-web/app/api/bff/reports/export/route.ts
  - donnarec-web/app/api/bff/edonation-config/route.ts
  - donnarec-web/components/ReportSummaryCards.tsx
  - donnarec-web/components/ReportBreakdownTable.tsx
  - donnarec-web/components/EdonationConfigTab.tsx
  - donnarec-web/components/SettingsTabs.tsx
  - donnarec-web/messages/th.json
  - donnarec-web/messages/en.json
autonomous: true
requirements: [FR-32]
must_haves:
  truths:
    - "Screen 8 (/reports) is visible to all staff and shows a date-range filter, summary cards (total amount / count / average), and a month/day breakdown table (D-70/D-71)."
    - "Reports are exportable to xlsx/csv via a BFF route with no PII and no confirmation dialog (D-70)."
    - "A 5th Settings tab lets an Admin edit the e-Donation field mapping + near_due_days config (D-75/NFR-09), persisted via the admin edonation-config endpoint."
  artifacts:
    - donnarec-web/app/reports/page.tsx
    - donnarec-web/components/ReportSummaryCards.tsx
    - donnarec-web/components/ReportBreakdownTable.tsx
    - donnarec-web/components/EdonationConfigTab.tsx
  key_links:
    - "ReportSummaryCards/ReportBreakdownTable ← BFF /api/bff/reports ← Go /api/reports/summary."
    - "EdonationConfigTab ← BFF /api/bff/edonation-config ← Go /api/admin/edonation-config (D-75)."
  prohibitions:
    - "The reports screen must never render a PII column and must never carry a role gate on nav or route (D-71)."
    - "Report export must never show the PII-warning confirmation dialog (no PII, D-70)."
    - "The edonation-config admin tab must never bypass the Go RequireRoles(Admin) authority."
---

## Phase Goal

**As any** staff member, **I want to** view and export a donation summary report by date range, and (as an Admin) adjust the e-Donation field mapping and aging threshold without a deploy, **so that** everyone can see donation activity transparently and the export stays correct when the Revenue Department field spec is confirmed.

<objective>
Build Screen 8 (Donation Summary Reports) for all staff and add the e-Donation config admin tab (D-75) to the existing Settings screen: report filter + summary cards + month/day breakdown table + PII-free export, plus an Admin-only field-mapping/near_due_days editor.

Purpose: Deliver the user-facing surface for FR-32 (reports) and the no-deploy config editor for D-75/NFR-09 that the 05-05 and 05-02 endpoints power.
Output: `/reports` route + report components + report BFF routes + `EdonationConfigTab` in SettingsTabs + BFF config route + i18n keys.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-e-donation-export-reports-admin-settings/05-CONTEXT.md
@.planning/phases/05-e-donation-export-reports-admin-settings/05-UI-SPEC.md
@donnarec-web/lib/bff.ts
@donnarec-web/app/api/bff/donations/route.ts
@donnarec-web/components/SettingsTabs.tsx
@donnarec-web/components/AppShell.tsx
@.planning/phases/05-e-donation-export-reports-admin-settings/05-05-SUMMARY.md
@.planning/phases/05-e-donation-export-reports-admin-settings/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Report BFF routes + Reports screen (filter, summary cards, breakdown table)</name>
  <files>donnarec-web/app/api/bff/reports/route.ts, donnarec-web/app/api/bff/reports/export/route.ts, donnarec-web/app/reports/page.tsx, donnarec-web/components/ReportSummaryCards.tsx, donnarec-web/components/ReportBreakdownTable.tsx, donnarec-web/messages/th.json, donnarec-web/messages/en.json</files>
  <read_first>
    - donnarec-web/lib/bff.ts (bffForward/goFetch/passthroughGoResponse; binary passthrough for export)
    - donnarec-web/app/api/bff/donations/route.ts (BFF route shape; query passthrough)
    - donnarec-web/components (Calendar Popover date-range fields; Card; Pagination; Intl.NumberFormat('th-TH') amount formatting; group-by segmented control precedent from Phase 4 preview toggle)
    - 05-UI-SPEC.md Screen 8 (layout, ReportSummaryCards, ReportBreakdownTable, group-by toggle, export row — no confirmation)
  </read_first>
  <action>
    Create app/api/bff/reports/route.ts (GET → proxy Go /api/reports/summary, JSON passthrough) and app/api/bff/reports/export/route.ts (GET → proxy Go /api/reports/export binary, preserve Content-Type/Content-Disposition, no confirmation). Create app/reports/page.tsx (all-staff, no role gate — nav item already added in 05-06): page heading "รายงานสรุปการบริจาค", the Calendar Popover date-range filter defaulting to the current fiscal year + "แสดงรายงาน"/"ล้างการกรอง" buttons (explicit apply, not live). Build components/ReportSummaryCards.tsx (3 cards: total amount / receipt count / average per receipt, Page-Heading figures in slate-900, Intl.NumberFormat, aria-labels with full context). Build components/ReportBreakdownTable.tsx (month/day segmented toggle, columns period/count/amount right-aligned comma-formatted, Pagination for daily). Add "ส่งออกรายงาน Excel (.xlsx)"/"ส่งออกรายงาน CSV" outline buttons that download from the reports export BFF route WITHOUT a confirmation dialog. Add reports.* i18n keys to th.json/en.json.
  </action>
  <verify>
    <automated>cd donnarec-web && npx tsc --noEmit && npx eslint app/reports components/ReportSummaryCards.tsx components/ReportBreakdownTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` + eslint clean for the new files.
    - /reports renders with no role gate; summary cards + breakdown table populate from /api/bff/reports; group-by toggle switches month/day.
    - Report export downloads via the BFF route with NO confirmation dialog (contrast with Screen 7 export).
    - No PII column rendered anywhere on the reports screen.
  </acceptance_criteria>
  <done>Reports screen (filter + cards + breakdown + PII-free export) delivered for all staff via BFF; typecheck/lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: e-Donation config admin tab (D-75) in Settings</name>
  <files>donnarec-web/app/api/bff/edonation-config/route.ts, donnarec-web/components/EdonationConfigTab.tsx, donnarec-web/components/SettingsTabs.tsx</files>
  <read_first>
    - donnarec-web/components/SettingsTabs.tsx (existing 4-tab structure; how tabs mount editors; save pattern)
    - donnarec-web/components/TemplateEditor.tsx (settings field-editor + save pattern to mirror)
    - 05-UI-SPEC.md phase-note (planner may add a 5th SettingsTabs tab reusing Screen 6's contract for the e-Donation field mapping + threshold config — no new visual spec)
    - donnarec-api edonation-config endpoint (GET/PUT /api/admin/edonation-config from 05-02)
  </read_first>
  <action>
    Create app/api/bff/edonation-config/route.ts (GET + PUT → proxy Go /api/admin/edonation-config, JSON). Build components/EdonationConfigTab.tsx: an Admin editor for the e-Donation field mapping (column order/header labels th/en) + cash_type_label + near_due_days, loaded from and saved to the BFF config route, following the existing TemplateEditor save/validation pattern (dirty-state, save button, success/error toast). Add it as a 5th tab in SettingsTabs.tsx reusing the existing tab contract (no new visual pattern). Add the settings.edonation.* i18n keys. The Go RequireRoles(Admin) on /api/admin/edonation-config remains the real authority — the tab is only shown within the already-Admin-gated Settings screen.
  </action>
  <verify>
    <automated>cd donnarec-web && npx tsc --noEmit && npx eslint components/EdonationConfigTab.tsx components/SettingsTabs.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` + eslint clean.
    - A 5th Settings tab loads the current edonation_config and saves edits (field mapping, cash_type_label, near_due_days) via PUT /api/bff/edonation-config, with success/error toast.
    - The tab lives inside the existing Admin-gated Settings screen and relies on Go RequireRoles(Admin) as the authority.
    - Saving updated near_due_days is reflected by the aging view's bucket threshold (config-driven, D-68/D-75).
  </acceptance_criteria>
  <done>Admin can edit e-Donation field mapping + aging threshold via a 5th Settings tab, persisted no-deploy through the BFF; typecheck/lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js BFF | Same-origin; token forwarded server-side only |
| BFF → Go API | Report route ungated (D-71); config route gated by Go RequireRoles(Admin) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07-PII | Information Disclosure | reports screen | medium | mitigate | Report data carries no PII (Go query selects SUM/COUNT only, 05-05); FE renders no ID column |
| T-05-07-CONFIGAUTH | Elevation of Privilege | edonation-config admin tab | high | mitigate | Go RequireRoles(Admin) on /api/admin/edonation-config is the authority; tab visibility is UX only |
| T-05-07-TOKEN | Information Disclosure | access token exposure | high | mitigate | BFF forwards Bearer server-side; token never reaches browser (D-R1) |
| T-05-07-ENUM | Information Disclosure | aggregate totals to all staff | low | accept | D-71 intentional — transparent PII-free report |
</threat_model>

## Artifacts this phase produces (Plan 07)

- **Next.js route:** `/reports` (`app/reports/page.tsx`) — all-staff summary report.
- **BFF routes:** `app/api/bff/reports/route.ts`, `app/api/bff/reports/export/route.ts` (binary passthrough), `app/api/bff/edonation-config/route.ts` (GET/PUT).
- **Components:** `ReportSummaryCards`, `ReportBreakdownTable`, `EdonationConfigTab`; 5th tab in `SettingsTabs`.
- **i18n keys:** `reports.*`, `settings.edonation.*` in th.json/en.json.

<verification>
- `npx tsc --noEmit` + eslint clean across new files.
- Manual UI walkthrough of Screen 8 + the config tab deferred to `/gsd-verify-work` per the UI-SPEC.
</verification>

<success_criteria>
FR-32 user surface delivered (all-staff report view + PII-free export) and D-75/NFR-09 no-deploy config editor in place; token never client-exposed; Go RBAC authoritative.
</success_criteria>

<output>
Create `.planning/phases/05-e-donation-export-reports-admin-settings/05-07-SUMMARY.md` when done.
</output>
