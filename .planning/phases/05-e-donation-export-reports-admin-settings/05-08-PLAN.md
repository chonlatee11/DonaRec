---
phase: 05-e-donation-export-reports-admin-settings
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - donnarec-web/messages/th.json
  - donnarec-web/messages/en.json
autonomous: true
gap_closure: true
requirements: [FR-31]
must_haves:
  truths:
    - "Aging tab sr-only <caption> renders a localized Keyed-Status-Tracking label with no console MISSING_MESSAGE error, in both th and en"
  artifacts:
    - donnarec-web/messages/th.json
    - donnarec-web/messages/en.json
  key_links:
    - "AgingTable.tsx:305 t('tabAging') under useTranslations('aging') resolves aging.tabAging, now present in both locale catalogs"
---

<objective>
Close the single minor UAT gap from 05-UAT.md Test 1: the Aging tab throws a
runtime console error `MISSING_MESSAGE: Could not resolve aging.tabAging in
messages for locale th` at AgingTable.tsx:305.

Root cause: AgingTable.tsx:59 binds `const t = useTranslations("aging")`, and
line 305 calls `t("tabAging")` for the table's sr-only <caption>. That resolves
the message key `aging.tabAging`, which does NOT exist in the `aging{}` namespace
of either locale catalog. The `tabAging` key exists only under a different
namespace (`eDonationExport.tabAging`, line 298 of both files).

Fix: add a `tabAging` entry to the `aging{}` namespace in both th.json and en.json,
reusing the same wording already present under eDonationExport.tabAging so the
sr-only caption reads the localized Keyed-Status-Tracking label.

Purpose: FR-31 aging view accessibility — the accessible caption must resolve
cleanly with no runtime i18n error.
Output: two edited message catalogs; no code change (AgingTable already calls
t('tabAging') correctly against its bound namespace).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-e-donation-export-reports-admin-settings/05-UAT.md

# The caption call site (already correct — do NOT change it)
@donnarec-web/components/AgingTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tabAging key to the aging namespace in both locale catalogs</name>
  <files>donnarec-web/messages/th.json, donnarec-web/messages/en.json</files>
  <action>
    In donnarec-web/messages/th.json, inside the `"aging"` object (starts line
    323), add a `"tabAging"` member with the Thai value `"ติดตามสถานะคีย์"` —
    identical wording to the existing eDonationExport.tabAging entry (th.json:298).

    In donnarec-web/messages/en.json, inside the `"aging"` object (starts line
    323), add a `"tabAging"` member with the English value `"Keyed-Status Tracking"`
    — identical wording to the existing eDonationExport.tabAging entry (en.json:298).

    Place the new key adjacent to the existing keyed-status keys (e.g. right after
    `keyedStatusNotKeyedBadge`) so related labels stay grouped. Preserve valid JSON:
    add the trailing comma on the preceding line, keep two-space indentation, do not
    reorder or touch any other key.

    Do NOT modify AgingTable.tsx — its `useTranslations("aging")` binding and the
    `t("tabAging")` caption call (line 305) are already correct; the only defect is
    the missing catalog key. Do NOT repoint the caption at the eDonationExport
    namespace (keep the aging table's labels self-contained in the aging namespace).
  </action>
  <verify>
    <automated>node -e "for (const f of ['donnarec-web/messages/th.json','donnarec-web/messages/en.json']){const j=require('./'+f); if(!j.aging||typeof j.aging.tabAging!=='string'||!j.aging.tabAging.trim()){console.error('MISSING aging.tabAging in '+f);process.exit(1)}} console.log('OK: aging.tabAging present in both locales')"</automated>
  </verify>
  <done>
    Both th.json and en.json parse as valid JSON and each contains a non-empty
    string at aging.tabAging (th="ติดตามสถานะคีย์", en="Keyed-Status Tracking").
    Rendering the Aging tab no longer emits the MISSING_MESSAGE console error at
    AgingTable.tsx:305, and the sr-only caption reads the localized
    Keyed-Status-Tracking label.
  </done>
</task>

</tasks>

<verification>
- `node -e` guard above exits 0 for both locale files (key present, non-empty string).
- Both JSON files still parse (the require() in the guard fails loudly on malformed JSON).
- No source file other than the two message catalogs is modified (AgingTable.tsx untouched).
- Manual re-check of UAT Test 1: open /e-donation Aging tab in th locale → no
  MISSING_MESSAGE console error.
</verification>

<success_criteria>
The 05-UAT.md Test 1 gap ("Aging table sr-only caption renders the localized
Keyed-Status Tracking label") is closed: aging.tabAging resolves in both th and en,
and AgingTable.tsx:305 renders the caption with zero runtime i18n error.
</success_criteria>

<output>
Create `.planning/phases/05-e-donation-export-reports-admin-settings/05-08-SUMMARY.md` when done
</output>
