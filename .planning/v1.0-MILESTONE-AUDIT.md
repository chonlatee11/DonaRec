---
milestone: v1.0
audited: 2026-07-17
status: tech_debt
scores:
  requirements: 47/47
  phases: 6/6
  integration: 5/5
  flows: 2/2
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 06-public-donation-web-form-flow-b
    items:
      - "No single automated E2E test spans the full Flow B composite (public-submit -> Checker-approve -> gap-less receipt issued). Both halves independently tested + issuance path proven source-agnostic + human UAT #3 confirms flow_b visible/approvable. Affects FR-01/03/04/05/06/08 + FR-14/15/16. Non-blocking coverage gap on highest-value cross-flow seam per project CONVENTIONS integration-test gate."
  - phase: 01-foundation-db-auth-rbac-audit-retention-model
    items:
      - "NFR-02: HTTPS/TLS is deployment-time verification only (VALIDATION.md Manual-Only); code encryption boundary (AES-256-GCM envelope) is complete. Confirm TLS at deploy."
      - "REQUIREMENTS.md checkbox/traceability lag: NFR-02, FR-13, NFR-03 shown [ ]/Pending but VERIFICATION.md proves SATISFIED. Canonicalized to [x]/Complete during this audit."
stakeholder_gates_nonblocking:
  - "FR-24: receipt wording + 1x/2x deduction eligibility — accounting/legal sign-off (Phase 4)"
  - "NFR-02/FR-25: email provider / KMS / hosting choice (Phase 1/4)"
  - "FR-30: exact e-Donation field spec vs live RD system (Phase 5)"
  - "NFR-03: PDPA retention period ~5y vs erasure (Phase 1/6)"
---

# Milestone v1.0 — Audit Report

**Project:** DonaRec — ระบบออกใบเสร็จบริจาคอัตโนมัติสำหรับโรงพยาบาล
**Audited:** 2026-07-17
**Status:** `tech_debt` (0 blockers; 1 non-blocking coverage warning + doc canonicalization)

## Verdict

All 6 phases Complete with `status: passed` VERIFICATION. All 47 mapped requirements SATISFIED. All 5 named cross-phase seams WIRED (verified by trace, not inference). Both end-to-end flows work. **Zero blockers.** One non-blocking tech-debt warning on test coverage.

## Phase Verification Summary

| Phase | Score | Status |
|-------|-------|--------|
| 1. Foundation (DB/Auth/RBAC/Audit/Retention) | 6/6 must-haves | passed |
| 2. Gap-less Receipt Numbering ★ | 5/5 must-haves | passed |
| 3. Donation Lifecycle & Maker-Checker | 6/6 (E2E + human walkthrough 7/7) | passed |
| 4. Receipt PDF + Email (Outbox Worker) | 5/5 roadmap criteria, 0 behavior-unverified | passed |
| 5. e-Donation Export, Reports & Admin | 4/4 must-haves | passed |
| 6. Public Donation Web Form (Flow B) | 8/9 must-haves (1 human — UAT 3/3 passed) | passed |

## Requirements Coverage (3-source cross-reference)

47/47 requirements SATISFIED across VERIFICATION.md evidence + traceability. No unsatisfied requirements. No orphaned requirements.

**Documentation canonicalization applied during audit** — 3 requirements showed stale `[ ]`/Pending in REQUIREMENTS.md despite VERIFICATION.md proving SATISFIED:

| REQ | Evidence (VERIFICATION.md) | Action |
|-----|----------------------------|--------|
| NFR-02 | Phase 1: AES-256-GCM envelope + KeyProvider LOCKED; donor PII decrypt/mask usage verified Phase 3; HTTPS/TLS = deployment-verify (Manual-Only) | → `[x]` / Complete |
| FR-13 | Phase 1: AuditMiddleware covers POST/PUT/PATCH/DELETE + /reveal GET; TestAuditMiddlewareCoverage PASS | → `[x]` / Complete |
| NFR-03 | Phase 1: retention/consent/legal_hold model; consent capture done Phase 3/6 (FR-03 Complete) | → `[x]` / Complete |

## Cross-Phase Integration (5/5 WIRED)

| Seam | Path | Status |
|------|------|--------|
| Numbering | P3 `Approve` → P2 `Allocator.Allocate` (in-tx, sole call site D-35) | WIRED |
| Audit | P3 mutations → P1 `AppendAuditEntryTx` (same tx, rolls back on failure) | WIRED |
| PDF+email | P3 `EnqueueOutboxJob` → P4 worker freeze-then-send | WIRED |
| Config → PDF/number | P5 settings Save → P4 `GetReceiptTemplateConfig` + P2 `GetReceiptNumberConfig` (same tables) | WIRED |
| Export/reports | P2/P3 issued rows → P5 `SearchIssuedForExport` + `crypto.DecryptField`; `SummaryByMonth` PII-free | WIRED |
| Auth/RBAC | P1 guards → P3/P5/P6 route groups; admin gate E2E-asserted (e2e_test.go:786-798) | WIRED |

## End-to-End Flows (2/2)

- **Flow A** (Maker create → Checker approve → gap-less number + audit + outbox → PDF+email): **WIRED + E2E-TESTED** — `TestE2E_MakerCheckerIssuancePipeline` (real Keycloak tokens; SoD self-approve→403; wrong-audience→401). Single atomic tx in `service.go:557-664`.
- **Flow B** (public form → pending_review → SAME pipeline → issuance → PDF+email): **WIRED** — writes same `donations` table with `source=flow_b`; issuance path proven source-agnostic by grep (worker/PDF branch on nothing source-related); `TestPublicDonationE2E` covers public-submit half; human UAT #3 confirms staff queue visibility/approval. No-email path freezes staff-downloadable PDF (FR-28), no stranded number.

## Tech Debt

**WARNING-1 (non-blocking):** No single automated E2E test spans the full Flow B composite (public-submit → Checker-approve → gap-less receipt). The two halves are independently tested and the issuance path is proven source-agnostic; human UAT #3 confirms the manual path. Per the project's own CONVENTIONS integration-test gate (which caught 3 seam bugs in Phase 3), this cross-flow handoff is the highest-value seam still lacking automated lock-in.
**Recommended fix:** add one E2E test — `doPublicSubmission` → look up flow_b donation id → `POST /api/donations/{id}/approve` with Checker token → assert `status=issued`, non-empty `receipt_formatted`, one `issue_receipt` outbox job.

## Nyquist Compliance

Not scanned — validate-phase step hook inactive for this project.

## Bottom Line

Milestone v1.0 is functionally complete and safe to close. The single tech-debt item is a test-coverage gap on an already-proven-wired seam, not a correctness defect. Options: complete the milestone now (track WARNING-1 in backlog), or add the one composite E2E test first.
