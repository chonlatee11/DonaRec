meta {
  name: Health Check
  type: http
  seq: 1
}

get {
  url: {{apiUrl}}/healthz
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.status: eq ok
}

docs {
  liveness probe สาธารณะ — ไม่ต้องใช้ token
}
